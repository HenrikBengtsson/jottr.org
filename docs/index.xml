<?xml version="1.0" encoding="UTF-8"?>
<rss  xmlns:atom="http://www.w3.org/2005/Atom" 
      xmlns:media="http://search.yahoo.com/mrss/" 
      xmlns:content="http://purl.org/rss/1.0/modules/content/" 
      xmlns:dc="http://purl.org/dc/elements/1.1/" 
      version="2.0">
<channel>
<title>JottR</title>
<link>https://www.jottr.org/</link>
<atom:link href="https://www.jottr.org/index.xml" rel="self" type="application/rss+xml"/>
<description></description>
<generator>quarto-1.7.31</generator>
<lastBuildDate>Sat, 01 Jul 2023 16:00:00 GMT</lastBuildDate>
<item>
  <title>parallelly: Querying, Killing and Cloning Parallel Workers Running Locally or Remotely</title>
  <dc:creator>Henrik Bengtsson</dc:creator>
  <link>https://www.jottr.org/posts/2023-07-02-parallelly_1.36.0/</link>
  <description><![CDATA[ 






<div style="padding: 2ex; float: right;">
<center>
<img src="https://www.jottr.org/posts/2023-07-02-parallelly_1.36.0/resources/parallelly-logo.png" alt="The 'parallelly' hexlogo">
</center>
</div>
<p><strong><a href="https://parallelly.futureverse.org">parallelly</a></strong> 1.36.0 is on CRAN since May 2023. The <strong>parallelly</strong> package is part of the <a href="https://www.futureverse.org">Futureverse</a> and enhances the <strong>parallel</strong> package of base R, e.g.&nbsp;it adds several features you’d otherwise expect to see in <strong>parallel</strong>. The <strong>parallelly</strong> package is one of the internal work horses for the <strong><a href="https://future.futureverse.org">future</a></strong> package, but it can also be used outside of the future ecosystem.</p>
<p>In this most recent release, <strong>parallelly</strong> gained several new skills in how cluster nodes (a.k.a. parallel workers) can be managed. Most notably,</p>
<ul>
<li><p>the <a href="https://parallelly.futureverse.org/reference/isNodeAlive.html"><code>isNodeAlive()</code></a> function can now also query parallel workers running on remote machines. Previously, this was only possible to workers running on the same machine.</p></li>
<li><p>the <a href="https://parallelly.futureverse.org/reference/killNode.html"><code>killNode()</code></a> function gained the power to terminate parallel workers running also on remotes machines.</p></li>
<li><p>the new function <a href="https://parallelly.futureverse.org/reference/cloneNode.html"><code>cloneNode()</code></a> can be used to “restart” a cluster node, e.g.&nbsp;if a node was determined to no longer be alive by <code>isNodeAlive()</code>, then <code>cloneNode()</code> can be called to launch an new parallel worker on the same machine as the previous worker.</p></li>
<li><p>The <code>print()</code> functions for PSOCK clusters and PSOCK nodes reports on the status of the parallel workers.</p></li>
</ul>
<section id="examples" class="level2">
<h2 class="anchored" data-anchor-id="examples">Examples</h2>
<p>Assume we’re running a PSOCK cluster of two parallel workers - one running on the local machine and the other on a remote machine that we connect to over SSH. Here is how we can set up such a cluster using <strong>parallelly</strong>:</p>
<div class="sourceCode" id="cb1" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb1-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">library</span>(parallelly)</span>
<span id="cb1-2"></span>
<span id="cb1-3">cl <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">makeClusterPSOCK</span>(<span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">c</span>(<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"localhost"</span>, <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"server.remote.org"</span>))</span>
<span id="cb1-4"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">print</span>(cl)</span>
<span id="cb1-5"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># Socket cluster with 2 nodes where 1 node is on host 'server.remote.org' (R</span></span>
<span id="cb1-6"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># version 4.3.1 (2023-06-16), platform x86_64-pc-linux-gnu), 1 node is on host</span></span>
<span id="cb1-7"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># 'localhost' (R version 4.3.1 (2023-06-16), platform x86_64-pc-linux-gnu)</span></span></code></pre></div>
<p>We can check if these two parallel workers are running. We can check this even if they are busy processing parallel tasks. The way <code>isNodeAlive()</code> works is that it checks of the <em>process</em> is running on worker’s machine, which is something that can be done even when the worker is busy. For example, let’s check the first worker process that run on the current machine:</p>
<div class="sourceCode" id="cb2" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb2-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">print</span>(cl[[<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span>]])</span>
<span id="cb2-2"><span class="do" style="color: #5E5E5E;
background-color: null;
font-style: italic;">## RichSOCKnode of a socket cluster on local host 'localhost' with pid 2457339</span></span>
<span id="cb2-3"><span class="do" style="color: #5E5E5E;
background-color: null;
font-style: italic;">## (R version 4.3.1 (2023-06-16), x86_64-pc-linux-gnu) using socket connection</span></span>
<span id="cb2-4"><span class="do" style="color: #5E5E5E;
background-color: null;
font-style: italic;">## #3 ('&lt;-localhost:11436')</span></span>
<span id="cb2-5"></span>
<span id="cb2-6"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">isNodeAlive</span>(cl[[<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span>]])</span>
<span id="cb2-7"><span class="do" style="color: #5E5E5E;
background-color: null;
font-style: italic;">## [1] TRUE</span></span></code></pre></div>
<p>In <strong>parallelly</strong> (&gt;= 1.36.0), we can now also query the remote machine:</p>
<div class="sourceCode" id="cb3" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb3-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">print</span>(cl[[<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">2</span>]])</span>
<span id="cb3-2"><span class="do" style="color: #5E5E5E;
background-color: null;
font-style: italic;">## RichSOCKnode of a socket cluster on remote host 'server.remove.org' with</span></span>
<span id="cb3-3"><span class="do" style="color: #5E5E5E;
background-color: null;
font-style: italic;">## pid 7731 (R version 4.3.1 (2023-06-16), x86_64-pc-linux-gnu) using socket</span></span>
<span id="cb3-4"><span class="do" style="color: #5E5E5E;
background-color: null;
font-style: italic;">## connection #4 ('&lt;-localhost:11436')</span></span>
<span id="cb3-5"></span>
<span id="cb3-6"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">isNodeAlive</span>(cl[[<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">2</span>]])</span>
<span id="cb3-7"><span class="do" style="color: #5E5E5E;
background-color: null;
font-style: italic;">## [1] TRUE</span></span></code></pre></div>
<p>We can also query <em>all</em> parallel workers of the cluster at once, e.g.</p>
<div class="sourceCode" id="cb4" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb4-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">isNodeAlive</span>(cl)</span>
<span id="cb4-2"><span class="do" style="color: #5E5E5E;
background-color: null;
font-style: italic;">## [1] TRUE TRUE</span></span></code></pre></div>
<p>Now, imagine if, say, the remote parallel process terminates for some unknown reasons. For example, the code running in parallel called some code that causes the parallel R process to crash and terminate. Although this “should not” happen, we all experience it once in a while. Another example is that the machine is running out of memory, for instance due to other misbehaving processes on the same machine. When that happens, the operating system might start killing processes in order not to completely crash the machine.</p>
<p>When one of our parallel workers has crashed, it will obviously not respond to requests for processing our R tasks. Instead, we will get obscure errors like:</p>
<div class="sourceCode" id="cb5" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb5-1">y <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> parallel<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">parLapply</span>(cl, <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">X =</span> X, <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">fun =</span> slow_fcn)</span>
<span id="cb5-2"><span class="do" style="color: #5E5E5E;
background-color: null;
font-style: italic;">## Error in summary.connection(connection) : invalid connection</span></span></code></pre></div>
<p>We can see that the second parallel worker in our cluster is no longer alive by:</p>
<div class="sourceCode" id="cb6" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb6-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">isNodeAlive</span>(cl)</span>
<span id="cb6-2"><span class="do" style="color: #5E5E5E;
background-color: null;
font-style: italic;">## [1] TRUE FALSE</span></span></code></pre></div>
<p>We can also see that there is something wrong with the one of our workers if we call <code>print()</code> on our <code>RichSOCKcluster</code> and <code>RichSOCKnode</code> objects, e.g.</p>
<div class="sourceCode" id="cb7" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb7-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">print</span>(cl)</span>
<span id="cb7-2"><span class="do" style="color: #5E5E5E;
background-color: null;
font-style: italic;">## Socket cluster with 2 nodes where 1 node is on host 'server.remote.org'</span></span>
<span id="cb7-3"><span class="do" style="color: #5E5E5E;
background-color: null;
font-style: italic;">## (R version 4.3.1 (2023-06-16), platform x86_64-pc-linux-gnu), 1 node is</span></span>
<span id="cb7-4"><span class="do" style="color: #5E5E5E;
background-color: null;
font-style: italic;">## on host 'localhost' (R version 4.3.1 (2023-06-16), platform</span></span>
<span id="cb7-5"><span class="do" style="color: #5E5E5E;
background-color: null;
font-style: italic;">## x86_64-pc-linux-gnu). 1 node (#2) has a broken connection (ERROR:</span></span>
<span id="cb7-6"><span class="do" style="color: #5E5E5E;
background-color: null;
font-style: italic;">## invalid connection)</span></span></code></pre></div>
<p>and</p>
<div class="sourceCode" id="cb8" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb8-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">print</span>(cl[[<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span>]])</span>
<span id="cb8-2"><span class="do" style="color: #5E5E5E;
background-color: null;
font-style: italic;">## RichSOCKnode of a socket cluster on local host 'localhost' with pid</span></span>
<span id="cb8-3"><span class="do" style="color: #5E5E5E;
background-color: null;
font-style: italic;">## 2457339 (R version 4.3.1 (2023-06-16), x86_64-pc-linux-gnu) using</span></span>
<span id="cb8-4"><span class="do" style="color: #5E5E5E;
background-color: null;
font-style: italic;">## socket connection #3 ('&lt;-localhost:11436')</span></span>
<span id="cb8-5"></span>
<span id="cb8-6"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">print</span>(cl[[<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">2</span>]])</span>
<span id="cb8-7"><span class="do" style="color: #5E5E5E;
background-color: null;
font-style: italic;">## RichSOCKnode of a socket cluster on remote host 'server.remote.org'</span></span>
<span id="cb8-8"><span class="do" style="color: #5E5E5E;
background-color: null;
font-style: italic;">## with pid 7731 (R version 4.3.1 (2023-06-16), x86_64-pc-linux-gnu)</span></span>
<span id="cb8-9"><span class="do" style="color: #5E5E5E;
background-color: null;
font-style: italic;">## using socket connection #4 ('ERROR: invalid connection')</span></span></code></pre></div>
<p>If we end up with a broken parallel worker like this, we can since <strong>parallelly</strong> 1.36.0 use <code>cloneNode()</code> to re-create the original worker. In our example, we can do:</p>
<div class="sourceCode" id="cb9" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb9-1">cl[[<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">2</span>]] <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">cloneNode</span>(cl[[<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">2</span>]])</span>
<span id="cb9-2"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">print</span>(cl[[<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">2</span>]])</span>
<span id="cb9-3"><span class="do" style="color: #5E5E5E;
background-color: null;
font-style: italic;">## RichSOCKnode of a socket cluster on remote host 'server.remote.org'</span></span>
<span id="cb9-4"><span class="do" style="color: #5E5E5E;
background-color: null;
font-style: italic;">## with pid 19808 (R version 4.3.1 (2023-06-16), x86_64-pc-linux-gnu)</span></span>
<span id="cb9-5"><span class="do" style="color: #5E5E5E;
background-color: null;
font-style: italic;">## using socket connection #4 ('&lt;-localhost:11436')</span></span></code></pre></div>
<p>to get a working parallel cluster, e.g.</p>
<div class="sourceCode" id="cb10" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb10-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">isNodeAlive</span>(cl)</span>
<span id="cb10-2"><span class="do" style="color: #5E5E5E;
background-color: null;
font-style: italic;">## [1] TRUE TRUE</span></span></code></pre></div>
<p>and</p>
<div class="sourceCode" id="cb11" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb11-1">y <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> parallel<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">parLapply</span>(cl, <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">X =</span> X, <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">fun =</span> slow_fcn)</span>
<span id="cb11-2"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">str</span>(y)</span>
<span id="cb11-3"><span class="do" style="color: #5E5E5E;
background-color: null;
font-style: italic;">## List of 8</span></span>
<span id="cb11-4"><span class="do" style="color: #5E5E5E;
background-color: null;
font-style: italic;">##  $ : num 1</span></span>
<span id="cb11-5"><span class="do" style="color: #5E5E5E;
background-color: null;
font-style: italic;">##  $ : num 1.41</span></span>
<span id="cb11-6"><span class="do" style="color: #5E5E5E;
background-color: null;
font-style: italic;">##  $ : num 1.73</span></span></code></pre></div>
<p>We can also use <code>cloneNode()</code> to launch <em>additional</em> workers of the same kind. For example, say we want to launch two more local workers and one more remote worker, and append them to the current cluster. One way to achieve that is:</p>
<div class="sourceCode" id="cb12" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb12-1">cl <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">c</span>(cl, <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">cloneNode</span>(cl[<span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">c</span>(<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span>,<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span>,<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">2</span>)]))</span>
<span id="cb12-2"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">print</span>(cl)</span>
<span id="cb12-3"><span class="do" style="color: #5E5E5E;
background-color: null;
font-style: italic;">## Socket cluster with 5 nodes where 3 nodes are on host 'localhost'</span></span>
<span id="cb12-4"><span class="do" style="color: #5E5E5E;
background-color: null;
font-style: italic;">## (R version 4.3.1 (2023-06-16), platform x86_64-pc-linux-gnu), 2</span></span>
<span id="cb12-5"><span class="do" style="color: #5E5E5E;
background-color: null;
font-style: italic;">## nodes are on host 'server.remote.org' (R version 4.3.1 (2023-06-16),</span></span>
<span id="cb12-6"><span class="do" style="color: #5E5E5E;
background-color: null;
font-style: italic;">## platform x86_64-pc-linux-gnu)</span></span></code></pre></div>
<p>Now, consider we launching many heavy parallel tasks, where some of them run on remote machines. However, after some time, we realize that we have launched tasks that will take much longer to resolve than we first anticipated. If we don’t want to wait for this to resolve by itself, we can choose to terminate some or all of the workers using <code>killNode()</code>. For example,</p>
<div class="sourceCode" id="cb13" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb13-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">killNode</span>(cl)</span>
<span id="cb13-2"><span class="do" style="color: #5E5E5E;
background-color: null;
font-style: italic;">## [1] TRUE TRUE TRUE TRUE TRUE</span></span></code></pre></div>
<p>will kill all parallel workers in our cluster, even if they are busy running tasks. We can confirm that these worker processes are no longer alive by calling:</p>
<div class="sourceCode" id="cb14" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb14-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">isNodeAlive</span>(cl)</span>
<span id="cb14-2"><span class="do" style="color: #5E5E5E;
background-color: null;
font-style: italic;">## [1] FALSE FALSE FALSE FALSE FALSE</span></span></code></pre></div>
<p>If we would attempt to use the cluster, we’d get the “Error in unserialize(node$con) : error reading from connection” as we saw previously. After having killed our cluster, we can re-launch it using <code>cloneNode()</code>, e.g.</p>
<div class="sourceCode" id="cb15" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb15-1">cl <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">cloneNode</span>(cl)</span>
<span id="cb15-2"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">isNodeAlive</span>(cl)</span>
<span id="cb15-3"><span class="do" style="color: #5E5E5E;
background-color: null;
font-style: italic;">## [1] TRUE TRUE TRUE TRUE TRUE</span></span></code></pre></div>
</section>
<section id="the-new-cluster-managing-skills-enhances-the-future-ecosystem" class="level2">
<h2 class="anchored" data-anchor-id="the-new-cluster-managing-skills-enhances-the-future-ecosystem">The new cluster managing skills enhances the future ecosystem</h2>
<p>When we use the <a href="https://future.futureverse.org/reference/cluster.html"><code>cluster</code></a> and <a href="https://future.futureverse.org/reference/multisession.html"><code>multisession</code></a> parallel backends of the <strong>future</strong> package, we rely on the <strong>parallelly</strong> package internally. Thanks to these new abilities, the Futureverse can now give more informative error message whenever we fail to launch a future or when we fail to retrieve the results of one. For example, if a parallel worker has terminated, we might get:</p>
<div class="sourceCode" id="cb16" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb16-1">f <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">future</span>(<span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">slow_fcn</span>(<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">42</span>))</span>
<span id="cb16-2"><span class="do" style="color: #5E5E5E;
background-color: null;
font-style: italic;">## Error: ClusterFuture (&lt;none&gt;) failed to call grmall() on cluster</span></span>
<span id="cb16-3"><span class="do" style="color: #5E5E5E;
background-color: null;
font-style: italic;">## RichSOCKnode #1 (PID 29701 on 'server.remote.org'). The reason reported</span></span>
<span id="cb16-4"><span class="do" style="color: #5E5E5E;
background-color: null;
font-style: italic;">## was 'error reading from connection'. Post-mortem diagnostic: No process</span></span>
<span id="cb16-5"><span class="do" style="color: #5E5E5E;
background-color: null;
font-style: italic;">## exists with this PID on the remote host, i.e. the remote worker is no</span></span>
<span id="cb16-6"><span class="do" style="color: #5E5E5E;
background-color: null;
font-style: italic;">## longer alive</span></span></code></pre></div>
<p>That post-mortem diagnostic is often enough to realize something quite exceptional has happened. It also gives us enough information to troubleshooting the problem further, e.g.&nbsp;if we keep seeing the same problem occurring over and over for a particular machine, it might suggest that there is an issue on that machine and we want to exclude it from further processing.</p>
<p>We could imagine that the <strong>future</strong> package would not only give us information on why things went wrong, but it could theoretically also try to fix the problem automatically. For instance, it could automatically re-create the crashed worker using <code>cloneNode()</code>, and re-launch the future. It is on the roadmap to add such robustness to the future ecosystem later on. However, there are several things to consider when doing so. For instance, what should happen if it was not a glitch, but that there is one parallel task that keeps crashing the parallel workers over and over? Most certainly, we want to only retry a fixed number of times, before giving up, otherwise we might get stuck in a never ending procedure. But even so, what if the problematic parallel code brings down the machine where it runs? If we have automatic restart of workers and parallel tasks, we might end up bringing down multiple machines before we notice the problem. So, although it appears fairly straightforward to handle crashed workers automatically, we need to come up with a robust, well-behaving strategy for doing so before we can implement it.</p>
<p>I hope you find this useful. If you have questions or comments on <strong>parallelly</strong>, or the Futureverse in general, please use the <a href="https://github.com/HenrikBengtsson/future/discussions/">Futureverse Discussion forum</a>.</p>
<p>Henrik</p>
</section>
<section id="links" class="level2">
<h2 class="anchored" data-anchor-id="links">Links</h2>
<ul>
<li><strong>parallelly</strong> package: <a href="https://cran.r-project.org/package=parallelly">CRAN</a>, <a href="https://github.com/HenrikBengtsson/parallelly">GitHub</a>, <a href="https://parallelly.futureverse.org">pkgdown</a></li>
<li><strong>future</strong> package: <a href="https://cran.r-project.org/package=future">CRAN</a>, <a href="https://github.com/HenrikBengtsson/future">GitHub</a>, <a href="https://future.futureverse.org">pkgdown</a></li>
<li><strong>Futureverse</strong>: <a href="https://www.futureverse.org" class="uri">https://www.futureverse.org</a></li>
</ul>


</section>

 ]]></description>
  <category>R</category>
  <category>package</category>
  <category>parallel</category>
  <category>parallelly</category>
  <category>parallel processing</category>
  <category>compute clusters</category>
  <category>HPC</category>
  <guid>https://www.jottr.org/posts/2023-07-02-parallelly_1.36.0/</guid>
  <pubDate>Sat, 01 Jul 2023 16:00:00 GMT</pubDate>
</item>
<item>
  <title>%dofuture% - a Better foreach() Parallelization Operator than %dopar%</title>
  <dc:creator>Henrik Bengtsson</dc:creator>
  <link>https://www.jottr.org/posts/2023-06-26-doFuture-1.0.0/</link>
  <description><![CDATA[ 






<div style="margin: 2ex; width: 100%;">
<center>
<img src="https://www.jottr.org/posts/2023-06-26-doFuture-1.0.0/resources/dopar-to-dofuture.png" alt="Two lines of code, where the first line shows 'y <- foreach(...) %dopar% { ... }'. The second line 'y <- foreach(...) %dofuture% { ... }'. The %dopar% operator is crossed out and there is a line down to %dofuture% directly below." style="width: 80%; border: 1px solid black;">
</center>
</div>
<p><strong><a href="https://doFuture.futureverse.org">doFuture</a></strong> 1.0.0 is on CRAN since March 2023. It introduces a new <strong><a href="https://cran.r-project.org/package=foreach">foreach</a></strong> operator <code>%dofuture%</code>, which makes it even easier to use <code>foreach()</code> to parallelize via the <strong>future</strong> ecosystem. This new operator is designed to be an alternative to the existing <code>%dopar%</code> operator for <code>foreach()</code> - an alternative that works in similar ways but better. If you already use <code>foreach()</code> together with futures, or plan on doing so, I recommend using <code>%dofuture%</code> instead of <code>%dopar%</code>. I’ll explain why I think so below.</p>
<section id="introduction" class="level2">
<h2 class="anchored" data-anchor-id="introduction">Introduction</h2>
<p>The traditional way to parallelize with <code>foreach()</code> is to use the <code>%dopar%</code> infix operator together with a registered foreach adaptor. The popular <strong><a href="https://cran.r-project.org/package=doParallel">doParallel</a></strong> package provides <code>%dopar%</code> backends for parallelizing on the local machine. Here is an example that uses four local workers:</p>
<div class="sourceCode" id="cb1" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb1-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">library</span>(foreach)</span>
<span id="cb1-2">workers <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> parallel<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">makeCluster</span>(<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">4</span>)</span>
<span id="cb1-3">doParallel<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">registerDoParallel</span>(<span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">cl =</span> workers)</span>
<span id="cb1-4"></span>
<span id="cb1-5">xs <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">rnorm</span>(<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1000</span>)</span>
<span id="cb1-6">y <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">foreach</span>(<span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">x =</span> xs, <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">.export =</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"slow_fcn"</span>) <span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">%dopar%</span> {</span>
<span id="cb1-7">  <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">slow_fcn</span>(x)</span>
<span id="cb1-8">}</span></code></pre></div>
<p>I highly suggest Futureverse for parallelization due to its advantages, such as relaying standard output, messages, warnings, and errors that were generated on the parallel workers in the main R process, support for near-live progress updates, and more descriptive backend error messages. Almost from the very beginning of the Futureverse, you have been able to use futures with <code>foreach()</code> and <code>%dopar%</code> via the <strong>doFuture</strong> package. For instance, we can rewrite the above example to use futures as:</p>
<div class="sourceCode" id="cb2" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb2-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">library</span>(foreach)</span>
<span id="cb2-2">doFuture<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">registerDoFuture</span>()</span>
<span id="cb2-3">future<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">plan</span>(multisession, <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">workers =</span> <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">4</span>)</span>
<span id="cb2-4"></span>
<span id="cb2-5">xs <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">rnorm</span>(<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1000</span>)</span>
<span id="cb2-6">y <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">foreach</span>(<span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">x =</span> xs, <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">.export =</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"slow_fcn"</span>) <span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">%dopar%</span> {</span>
<span id="cb2-7">  <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">slow_fcn</span>(x)</span>
<span id="cb2-8">}</span></code></pre></div>
<p>In this blog post, I am proposing to move to</p>
<div class="sourceCode" id="cb3" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb3-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">library</span>(foreach)</span>
<span id="cb3-2">future<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">plan</span>(multisession, <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">workers =</span> <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">4</span>)</span>
<span id="cb3-3"></span>
<span id="cb3-4">xs <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">rnorm</span>(<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1000</span>)</span>
<span id="cb3-5">y <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">foreach</span>(<span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">x =</span> xs, <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">.export =</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"slow_fcn"</span>) <span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">%dofuture%</span> {</span>
<span id="cb3-6">  <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">slow_fcn</span>(x)</span>
<span id="cb3-7">}</span></code></pre></div>
<p>instead. So, why is that better? It is because:</p>
<ol type="1">
<li><p><code>%dofuture%</code> removes the need to register a foreach backend, i.e.&nbsp;no more <code>registerDoMC()</code>, <code>registerDoParallel()</code>, <code>registerDoFuture()</code>, etc.</p></li>
<li><p><code>%dofuture%</code> is unaffected by any foreach backends that the end-user has registered.</p></li>
<li><p><code>%dofuture%</code> uses a consistent <code>foreach()</code> “options” argument, regardless of parallel backend used, and <em>not</em> different ones for different backends, e.g.&nbsp;<code>.options.multicore</code>, <code>.options.snow</code>, and <code>.options.mpi</code>.</p></li>
<li><p><code>%dofuture%</code> is guaranteed to always parallelizes via the Futureverse, using whatever <code>plan()</code> the end-user has specified. It also means that you, as a developer, have full control of the parallelization code.</p></li>
<li><p><code>%dofuture%</code> can generate proper parallel random number generation (RNG). There is no longer a need to use <code>%dorng%</code> of the <strong><a href="https://cran.r-project.org/package=doRNG">doRNG</a></strong> package.</p></li>
<li><p><code>%dofuture%</code> automatically identifies global variables and packages that are needed by the parallel workers.</p></li>
<li><p><code>%dofuture%</code> relays errors generated in parallel as-is such that they can be handled using standard R methods, e.g.&nbsp;<code>tryCatch()</code>.</p></li>
<li><p><code>%dofuture%</code> truly outputs standard output and messages, warnings, and other types of conditions generated in parallel as-is such that they can be handled using standard R methods, e.g.&nbsp;<code>capture.output()</code> and <code>withCallingHandlers()</code>.</p></li>
<li><p><code>%dofuture%</code> supports near-live progress updates via the <strong><a href="https://progressr.futureverse.org">progressr</a></strong> package.</p></li>
<li><p><code>%dofuture%</code> gives more informative error messages, which helps troubleshooting, if a parallel worker crashes.</p></li>
</ol>
<p>Below are the details.</p>
</section>
<section id="problems-of-dopar-that-dofuture-addresses" class="level2">
<h2 class="anchored" data-anchor-id="problems-of-dopar-that-dofuture-addresses">Problems of <code>%dopar%</code> that <code>%dofuture%</code> addresses</h2>
<p>Let me discuss a few of the unfortunate drawbacks that comes with <code>%dopar%</code>. Most of these stem from a slightly too lax design. Although convenient, the flexible design prevents us from having full control and writing code that can parallelize on any parallel backend.</p>
<section id="problem-1.-dopar-requires-registering-a-foreach-adaptor" class="level3">
<h3 class="anchored" data-anchor-id="problem-1.-dopar-requires-registering-a-foreach-adaptor">Problem 1. <code>%dopar%</code> requires registering a foreach adaptor</h3>
<p>If we write code that others will use, say, an R package, then we can never know what compute resources the user has, or will have in the future. Traditionally, this means that one user might want to use <strong>doParallel</strong> for parallelization, another <strong>doMC</strong>, and yet another, maybe, <strong>doRedis</strong>. Because of this, we must not have any calls to one of the many <code>registerDoNnn()</code> functions in our code. If we do, we lock users into a specific parallel backend. We could of course support a few different backends, but we are still locking users into a small set of parallel backends. If someone develops a new backend in the future, our code has to be updated before users can take advantage the new backends.</p>
<p>One can argue that <code>doFuture::registerDoFuture()</code> somewhat addresses this problem. On one hand, when used, it does lock the user into the future framework. On the other hand, the user has many parallel backends to choose from in the Futureverse, including backends that will be developed in the future. In this sense, the lock-in is less severe, especially since we do not have to update our code for new backends to be supported. Also, to avoid destructive side effects, <code>registerDoFuture()</code> allows you to change the foreach backend used inside your functions temporarily, e.g.</p>
<div class="sourceCode" id="cb4" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb4-1"><span class="do" style="color: #5E5E5E;
background-color: null;
font-style: italic;">## Temporarily use futures</span></span>
<span id="cb4-2">oldDoPar <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">registerDoFuture</span>()</span>
<span id="cb4-3"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">on.exit</span>(<span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">with</span>(oldDoPar, foreach<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">setDoPar</span>(<span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">fun=</span>fun, <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">data=</span>data, <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">info=</span>info)), <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">add =</span> <span class="cn" style="color: #8f5902;
background-color: null;
font-style: inherit;">TRUE</span>)</span></code></pre></div>
<p>This avoids changing the foreach backend that the user might already have set elsewhere.</p>
<p>That said, I never wanted to say that people <em>should use</em> <code>registerDoFuture()</code> whenever using <code>%dopar%</code>, because I think that would be against the philosophy behind the <strong>foreach</strong> framework. The <strong>foreach</strong> ecosystem is designed to separate the <code>foreach()</code> + <code>%dopar%</code> code, describing what to parallelize, from the <code>registerDoNnn()</code> call, describing how and where to parallelize.</p>
<p>Using <code>%dofuture%</code>, instead of <code>%dopar%</code> with user-controlled foreach backend, avoids this dilemma. With <code>%dofuture%</code> the developer is in full control of the parallelization code.</p>
</section>
<section id="problem-2.-chunking-and-load-balancing-differ-among-foreach-backends" class="level3">
<h3 class="anchored" data-anchor-id="problem-2.-chunking-and-load-balancing-differ-among-foreach-backends">Problem 2. Chunking and load-balancing differ among foreach backends</h3>
<p>When using parallel map-reduce functions such as <code>mclapply()</code>, <code>parLapply()</code> of the <strong>parallel</strong> package, or <code>foreach()</code> with <code>%dopar%</code>, the tasks are partitioned into subsets and distributed to the parallel workers for processing. This partitioning is often referred to as “chunking”, because we chunk up the elements into smaller chunks, and then each chunk is processed by one parallel worker. There are different strategies to chunk up the elements. One approach is to use uniformly sized chunks and have each worker process one chunk. Another approach is to use chunks with a single element, and have each worker process one or more chunks.</p>
<p>The chunks may be pre-assigned (“prescheduled”) to the parallel workers up-front, which is referred to as <em>static load balancing</em>. An alternative is to assign chunks to workers on-the-fly as the workers become available, which is referred to as <em>dynamic load balancing</em>.</p>
<p>If the processing time differ a lot between elements, it is beneficial to use dynamic load balancing together with small chunk sizes.</p>
<p>However, if we dig into the documentation and source code of the different foreach backends, we will find that they use different chunking and load-balancing strategies. For example, assume we are running on a Linux machine, which supports forked processing. Then, if we use</p>
<div class="sourceCode" id="cb5" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb5-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">library</span>(foreach)</span>
<span id="cb5-2">doParallel<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">registerDoParallel</span>(<span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">ncores =</span> <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">8</span>)</span>
<span id="cb5-3"></span>
<span id="cb5-4">y <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">foreach</span>(<span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">x =</span> X, <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">.export =</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"slow_fcn"</span>) <span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">%dopar%</span> {</span>
<span id="cb5-5">  <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">slow_fcn</span>(x)</span>
<span id="cb5-6">}</span></code></pre></div>
<p>the data will be processed by eight fork-based parallel workers using <em>dynamic load balancing with single-element chunks</em>. However, if we use PSOCK clusters:</p>
<div class="sourceCode" id="cb6" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb6-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">library</span>(foreach)</span>
<span id="cb6-2">cl <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> parallel<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">makeCluster</span>(<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">8</span>)</span>
<span id="cb6-3">doParallel<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">registerDoParallel</span>(<span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">cl =</span> cl)</span>
<span id="cb6-4"></span>
<span id="cb6-5">y <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">foreach</span>(<span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">x =</span> X, <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">.export =</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"slow_fcn"</span>) <span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">%dopar%</span> {</span>
<span id="cb6-6">  <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">slow_fcn</span>(x)</span>
<span id="cb6-7">}</span></code></pre></div>
<p>the data will be processed by eight PSOCK-based parallel workers using <em>static load balancing with uniformly sized chunks</em>.</p>
<p>Which of these two chunking and load-balancing strategies is the most efficient one depends on how much the processing time of <code>slow_fcn(x)</code> varies with different values of <code>x</code>. For example, and without going into details, if the processing times differ a lot, dynamic load balancing often makes better use of the parallel workers and results in a shorter overall processing time.</p>
<p>Regardless of which is faster, the problem with different foreach backends using different strategies is that, as a developer with little control over the registered foreach backend, you have equally poor control over the chunking and load-balancing strategies.</p>
<p>Using <code>%dofuture%</code>, avoids this problem. If you use <code>%dofuture%</code>, then dynamic load balancing will always be used for processing the data, regardless of which parallel future backend is in place, with the option to control the chunk size. As a side note, <code>%dopar%</code> with <code>registerDoFuture()</code> will also do this.</p>
</section>
<section id="problem-3.-different-foreach-backends-use-different-foreach-options" class="level3">
<h3 class="anchored" data-anchor-id="problem-3.-different-foreach-backends-use-different-foreach-options">Problem 3. Different foreach backends use different <code>foreach()</code> options</h3>
<p>In the previous section, I did not mention that for some foreach backends it is indeed possible to control whether static or dynamic load balancing should be used, and what the chunk sizes should be. This can be controlled by special <code>.options.*</code> arguments for <code>foreach()</code>. However, each foreach backend has their own <code>.options.*</code> argument, e.g.&nbsp;you might find that some use <code>.options.multicore</code>, others <code>.options.snow</code>, or something else. Because they are different, we cannot write code that works with any type of foreach backend.</p>
<p>To give two examples, when using <strong>doParallel</strong> and <code>registerDoParallel(cores = 8)</code>, we can replace the default dynamic load balancing with static load balancing as:</p>
<div class="sourceCode" id="cb7" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb7-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">library</span>(foreach)</span>
<span id="cb7-2">doParallel<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">registerDoParallel</span>(<span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">ncores =</span> <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">8</span>)</span>
<span id="cb7-3"></span>
<span id="cb7-4">y <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">foreach</span>(<span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">x =</span> X, <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">.export =</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"slow_fcn"</span>,</span>
<span id="cb7-5">             <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">.options.multicore =</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">list</span>(<span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">preschedule =</span> <span class="cn" style="color: #8f5902;
background-color: null;
font-style: inherit;">TRUE</span>)) <span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">%dopar%</span> {</span>
<span id="cb7-6">  <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">slow_fcn</span>(x)</span>
<span id="cb7-7">}</span></code></pre></div>
<p>This change will also switch from chunks with a single element to (eight) chunks with similar size.</p>
<p>If we instead would use <code>registerDoParallel(cl)</code>, which gives us the vice versa situation, we can switch out the static load balancing with dynamic load balancing by using:</p>
<div class="sourceCode" id="cb8" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb8-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">library</span>(foreach)</span>
<span id="cb8-2">cl <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> parallel<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">makeCluster</span>(<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">8</span>)</span>
<span id="cb8-3">doParallel<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">registerDoParallel</span>(<span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">cl =</span> cl)</span>
<span id="cb8-4"></span>
<span id="cb8-5">y <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">foreach</span>(<span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">x =</span> X, <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">.export =</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"slow_fcn"</span>,</span>
<span id="cb8-6">             <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">.options.snow =</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">list</span>(<span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">preschedule =</span> <span class="cn" style="color: #8f5902;
background-color: null;
font-style: inherit;">FALSE</span>)) <span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">%dopar%</span> {</span>
<span id="cb8-7">  <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">slow_fcn</span>(x)</span>
<span id="cb8-8">}</span></code></pre></div>
<p>This will also switch from uniformly sized chunks to single-element chunks.</p>
<p>As we can see, the fact that we have to use different <code>foreach()</code> “options” arguments (here <code>.options.multicore</code> and <code>.options.snow</code>) for different foreach backends prevents us from writing code that works with any foreach backend.</p>
<p>Of course, we could specify “options” arguments for known foreach backends and hope we haven’t missed any and that no new ones are showing up later, e.g.</p>
<div class="sourceCode" id="cb9" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb9-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">library</span>(foreach)</span>
<span id="cb9-2">doParallel<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">registerDoParallel</span>(<span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">cores =</span> <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">8</span>)</span>
<span id="cb9-3"></span>
<span id="cb9-4">y <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">foreach</span>(<span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">x =</span> X,</span>
<span id="cb9-5">             <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">.export =</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"slow_fcn"</span>,</span>
<span id="cb9-6">             <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">.options.multicore =</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">list</span>(<span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">preschedule =</span> <span class="cn" style="color: #8f5902;
background-color: null;
font-style: inherit;">TRUE</span>),</span>
<span id="cb9-7">             <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">.options.snow      =</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">list</span>(<span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">preschedule =</span> <span class="cn" style="color: #8f5902;
background-color: null;
font-style: inherit;">TRUE</span>),</span>
<span id="cb9-8">             <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">.options.future    =</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">list</span>(<span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">preschedule =</span> <span class="cn" style="color: #8f5902;
background-color: null;
font-style: inherit;">TRUE</span>),</span>
<span id="cb9-9">             <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">.options.mpi       =</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">list</span>(<span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">chunkSize =</span> <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span>)      ) <span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">%dopar%</span> {</span>
<span id="cb9-10">  <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">slow_fcn</span>(x)</span>
<span id="cb9-11">}</span></code></pre></div>
<p>Regardlessly, this still limits the end-user to a set of commonly used foreach backends, and our code can never be agile to foreach backends that are developed at a later time.</p>
<p>Using <code>%dofuture%</code> avoids these problems. It supports argument <code>.options.future</code> in a consistent way across all future backends, which means that your code will be the same regardless of parallel backend. By the core design of the Futureverse, any new future backends developed later one will automatically work with your <strong>foreach</strong> code if you use <code>%dofuture%</code>.</p>
</section>
<section id="problem-4.-global-variables-are-not-always-identified-by-foreach" class="level3">
<h3 class="anchored" data-anchor-id="problem-4.-global-variables-are-not-always-identified-by-foreach">Problem 4. Global variables are not always identified by <code>foreach()</code></h3>
<p>When parallelizing code, the parallel workers must have access to all functions and variables required to evaluate the parallel code. As we have seen the above examples, you can use the <code>.export</code> argument to help <code>foreach()</code> to export the necessary objects to each of the parallel workers.</p>
<p>However, a developer who uses <code>doMC::registerDoMC()</code>, or equivalently <code>doParallel::registerDoParallel(cores)</code>, might forget to specify the <code>.export</code> argument. This can happen because the mechanisms of forked processing makes all objects available to the parallel workers. If they test their code using only these foreach backends, they will not notice that <code>.export</code> is not declared. The same may happen if the developer assumes <code>doFuture::registerDoFuture()</code> is used. However, without specifying <code>.export</code>, the code will <em>not</em> work on other types of foreach backends, e.g.&nbsp;<code>doParallel::registerDoParallel(cl)</code> and <code>doMPI::registerDoMPI()</code>. If an R package forgets to specify the <code>.export</code> argument, and is not comprehensively tested, then it will be the end-user, for instance on MS Windows, that runs into the bug.</p>
<p>When using <code>%dofuture%</code>, global variables and required packages are by default automatically identified and exported to the parallel workers by the future framework. This is done the same way regardless of parallel backend.</p>
</section>
<section id="problem-5.-easy-to-forget-parallel-random-number-generation" class="level3">
<h3 class="anchored" data-anchor-id="problem-5.-easy-to-forget-parallel-random-number-generation">Problem 5. Easy to forget parallel random number generation</h3>
<p>The <strong>foreach</strong> package and <code>%dopar%</code> do not have built-in support for parallel random number generation (RNG). Statistical sound parallel RNG is critical for many statistical analyses. If not done, then the results can be biases and incorrect conclusions might be drawn. The <strong><a href="https://cran.r-project.org/package=doRNG">doRNG</a></strong> package comes to rescue when using <code>%dopar%</code>. It provides the operator <code>%dorng%</code>, which will use <code>%dopar%</code> internally while automatically setting up parallel RNG. Whenever you use <code>%dopar%</code> and find yourself needing parallel RNG, I recommend to simply replace <code>%dopar%</code> with <code>%dorng%</code>. The <strong>doRNG</strong> package also provides <code>registerDoRNG()</code>, which I do not recommend, because as a developer you do not have full control whether that is registered or not.</p>
<p>Because <strong>foreach</strong> does not have built-in support for parallel RNG, it is easy to forget that it should be used. A developer who is aware of the importance of using proper parallel RNG will find out about <strong>doRNG</strong> and how to best use it, but a developer who is not aware of the problem, can easily miss it and publish an R package that produces potentially incorrect results.</p>
<p>However, when using the future framework will detect if we forget to use parallel RNG. When this happens, a warning will alert us to the problem and suggest how to fix it. This is the case if you use <code>doFuture::registerDoFuture()</code>, and it’s also the case when using <code>%dofuture%</code>. For example,</p>
<div class="sourceCode" id="cb10" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb10-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">library</span>(doFuture)</span>
<span id="cb10-2"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">plan</span>(multisession, <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">workers =</span> <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">3</span>)</span>
<span id="cb10-3"></span>
<span id="cb10-4">y <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">foreach</span>(<span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">ii =</span> <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">:</span><span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">4</span>) <span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">%dofuture%</span> {</span>
<span id="cb10-5">  <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">runif</span>(ii)</span>
<span id="cb10-6">}</span></code></pre></div>
<p>produces</p>
<pre><code>Warning messages:
1: UNRELIABLE VALUE: Iteration 1 of the foreach() %dofuture% { ... },
part of chunk #1 ('doFuture2-1'), unexpectedly generated random numbers
without declaring so. There is a risk that those random numbers are not
statistically sound and the overall results might be invalid. To fix
this, specify foreach() argument '.options.future = list(seed = TRUE)'.
This ensures that proper, parallel-safe random numbers are produced via
the L'Ecuyer-CMRG method. To disable this check, set option
'doFuture.rng.onMisuse' to "ignore". </code></pre>
<p>To fix this, we can specify <code>foreach()</code> argument <code>.options.future = list(seed = TRUE)</code> to declare that we need to draw random number in parallel, i.e.</p>
<div class="sourceCode" id="cb12" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb12-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">library</span>(doFuture)</span>
<span id="cb12-2"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">plan</span>(multisession, <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">workers =</span> <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">3</span>)</span>
<span id="cb12-3"></span>
<span id="cb12-4">y <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">foreach</span>(<span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">ii =</span> <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">:</span><span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">4</span>, <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">.options.future =</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">list</span>(<span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">seed =</span> <span class="cn" style="color: #8f5902;
background-color: null;
font-style: inherit;">TRUE</span>)) <span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">%dofuture%</span> {</span>
<span id="cb12-5">  <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">runif</span>(ii)</span>
<span id="cb12-6">}</span></code></pre></div>
<p>This makes sure that statistical sound random numbers are generated.</p>
</section>
</section>
<section id="migrating-from-dopar-to-dofuture-is-straightforward" class="level2">
<h2 class="anchored" data-anchor-id="migrating-from-dopar-to-dofuture-is-straightforward">Migrating from %dopar% to %dofuture% is straightforward</h2>
<p>If you already have code that uses <code>%dopar%</code> and want to start using <code>%dofuture%</code> instead, then it only takes are few changes, which are all straightforward and quick:</p>
<ol type="1">
<li><p>Replace <code>%dopar%</code> with <code>%dofuture%</code>.</p></li>
<li><p>Replace <code>%dorng%</code> with <code>%dofuture%</code> and set <code>.options.future =    list(seed = TRUE)</code>.</p></li>
<li><p>Replace <code>.export = &lt;character vector of global variables&gt;</code> with <code>.options.future = list(globals = &lt;character vector of global    variables&gt;)</code>.</p></li>
<li><p>Drop any other <code>registerDoNnn()</code> calls inside your function, if you use them.</p></li>
<li><p>Update your documentation to mention that the parallel backend should be set using <code>future::plan()</code> and no longer via different <code>registerDoNnn()</code> calls.</p></li>
</ol>
<p>In brief, if you use <code>%dofuture%</code> instead of <code>%dopar%</code>, your life as a developer will be easier and so will the end-user’s be too.</p>
<p>If you have questions or comments on <strong>doFuture</strong> and <code>%dofuture%</code>, or the Futureverse in general, please use the <a href="https://github.com/HenrikBengtsson/future/discussions/">Futureverse Discussion forum</a>.</p>
<p>Happy futuring!</p>
<p>Henrik</p>
</section>
<section id="links" class="level2">
<h2 class="anchored" data-anchor-id="links">Links</h2>
<ul>
<li><strong>doFuture</strong> package: <a href="https://cran.r-project.org/package=doFuture">CRAN</a>, <a href="https://github.com/HenrikBengtsson/doFuture">GitHub</a>, <a href="https://doFuture.futureverse.org">pkgdown</a></li>
<li><strong>Futureverse</strong>: <a href="https://www.futureverse.org" class="uri">https://www.futureverse.org</a></li>
</ul>


</section>

 ]]></description>
  <category>R</category>
  <category>R</category>
  <category>package</category>
  <category>parallel</category>
  <category>foreach</category>
  <category>doFuture</category>
  <category>%dofuture%</category>
  <category>%dopar%</category>
  <category>parallel processing</category>
  <category>compute clusters</category>
  <category>HPC</category>
  <guid>https://www.jottr.org/posts/2023-06-26-doFuture-1.0.0/</guid>
  <pubDate>Mon, 26 Jun 2023 17:00:00 GMT</pubDate>
</item>
<item>
  <title>Edmonton R User Group Meetup: Futureverse - A Unifying Parallelization Framework in R for Everyone</title>
  <dc:creator>Henrik Bengtsson</dc:creator>
  <link>https://www.jottr.org/posts/2023-05-22-futureverse-yegrug/</link>
  <description><![CDATA[ 






<div style="margin: 2ex; width: 100%;">
<center>
<img src="https://www.jottr.org/posts/2023-05-22-futureverse-yegrug/resources/YEGRUG_20230522.jpeg" alt="The YEGRUG poster slide for the Futureverse presentation on 2023-05-22" style="width: 80%; border: 1px solid black;">
</center>
</div>
<p>Below are the slides from my presentation at the <a href="https://www.meetup.com/edmonton-r-user-group-yegrug/events/fxvdbtyfchbhc/">Edmonton R User Group Meetup (YEGRUG)</a> on May 22, 2023:</p>
<p>Title: Futureverse - A Unifying Parallelization Framework in R for Everyone<br>
Speaker: Henrik Bengtsson<br>
Slides: <a href="https://docs.google.com/presentation/d/e/2PACX-1vQfbnVRHZhIkEAd3_pNG14N5JQqE0jqCohSq-m-uWAcA7StF-BuHdOz0IGDhcRI3K681DxoXoqA7pwp/pub?start=true&amp;loop=false&amp;delayms=60000">HTML</a>, <a href="../../presentations/yegrug2023/BengtssonH_20230522-Futureverse-YEGRUG.pdf">PDF</a> (46 slides)<br>
Video: <a href="https://www.youtube.com/watch?v=6Dp6zMelrmg">official recording</a> (~60 minutes)</p>
<p>Thank you Péter Sólymos and the YEGRUG for the invitate and the opportunity!</p>
<p>/Henrik</p>
<section id="links" class="level2">
<h2 class="anchored" data-anchor-id="links">Links</h2>
<ul>
<li>YEGRUG: https://yegrug.github.io/</li>
<li><strong>Futureverse</strong> website: https://www.futureverse.org/</li>
<li><strong>future</strong> package <a href="https://cran.r-project.org/package=future">CRAN</a>, <a href="https://github.com/HenrikBengtsson/future">GitHub</a>, <a href="https://future.futureverse.org/">pkgdown</a></li>
</ul>


</section>

 ]]></description>
  <category>R</category>
  <category>R</category>
  <category>package</category>
  <category>future</category>
  <category>YEGRUG</category>
  <category>Edmonton R User Group Meetup</category>
  <category>talk</category>
  <category>presentation</category>
  <category>slides</category>
  <category>asynchronous</category>
  <category>parallel processing</category>
  <category>remote evaluation</category>
  <category>compute clusters</category>
  <category>HPC</category>
  <guid>https://www.jottr.org/posts/2023-05-22-futureverse-yegrug/</guid>
  <pubDate>Tue, 23 May 2023 01:00:00 GMT</pubDate>
</item>
<item>
  <title>parallelly 1.34.0: Support for CGroups v2, Killing Parallel Workers, and more</title>
  <dc:creator>Henrik Bengtsson</dc:creator>
  <link>https://www.jottr.org/posts/2023-01-18-parallelly_1.34.0-cgroups2-and-more/</link>
  <description><![CDATA[ 






<div style="padding: 2ex; float: right;">
<center>
<img src="https://www.jottr.org/posts/2023-01-18-parallelly_1.34.0-cgroups2-and-more/resources/parallelly-logo.png" alt="The 'parallelly' hexlogo">
</center>
</div>
<p>With the recent releases of <strong><a href="https://parallelly.futureverse.org">parallelly</a></strong> 1.33.0 (2022-12-13) and 1.34.0 (2023-01-13), <a href="https://parallelly.futureverse.org/reference/availableCores.html"><code>availableCores()</code></a> and <a href="https://parallelly.futureverse.org/reference/availableWorkers.html"><code>availableWorkers()</code></a> gained better support for Linux CGroups, options for avoiding running out of R connections when setting up <strong>parallel</strong>-style clusters, and <code>killNode()</code> for forcefully terminating one or more parallel workers. I summarize these updates below. For other updates, please see the <a href="https://parallelly.futureverse.org/news/index.html">NEWS</a>.</p>
<section id="added-support-for-cgroups-v2" class="level2">
<h2 class="anchored" data-anchor-id="added-support-for-cgroups-v2">Added support for CGroups v2</h2>
<p><a href="https://parallelly.futureverse.org/reference/availableCores.html"><code>availableCores()</code></a> and <a href="https://parallelly.futureverse.org/reference/availableWorkers.html"><code>availableWorkers()</code></a> gained support for Linux Control Groups v2 (CGroups v2), besides CGroups v1, which has been supported since <strong><a href="https://parallelly.futureverse.org">parallelly</a></strong> 1.31.0 (2022-04-07) and partially since 1.22.0 (2020-12-12). This means that if you use <code>availableCores()</code> and <code>availableWorkers()</code> in your R code, it will better respect the number of CPU cores that the Linux system has made available to you. Not all systems use CGroups, but it is becoming more popular, so if the Linux system you run on does not use it right now, it is likely it will at some point.</p>
</section>
<section id="avoid-running-out-of-r-connections" class="level2">
<h2 class="anchored" data-anchor-id="avoid-running-out-of-r-connections">Avoid running out of R connections</h2>
<p>If you run parallel code on a machine with a many CPU cores, there’s a risk that you run out of available R connections, which are needed when setting up <strong>parallel</strong> cluster nodes. This is because R has a limit of a maximum 125 connections being used at the same time(*) and each cluster node consumes one R connection. If you try to set up more parallel workers than this, you will get an error. The <strong>parallelly</strong> package already has built-in protection against this, e.g.</p>
<div class="sourceCode" id="cb1" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb1-1"><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">&gt;</span> cl <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> parallelly<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">makeClusterPSOCK</span>(<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">192</span>)</span>
<span id="cb1-2">Error<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">:</span> Cannot create <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">192</span> parallel PSOCK nodes. Each node needs</span>
<span id="cb1-3">one connection, but there are only <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">124</span> connections left out of</span>
<span id="cb1-4">the maximum <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">128</span> available on this R installation</span></code></pre></div>
<p>This error is <em>instant</em> and with no parallel workers being launched. In contrast, if you use <strong>parallel</strong>, you will only get an error after R has launched the first 124 cluster nodes and fails to launch the 125:th one, e.g.</p>
<div class="sourceCode" id="cb2" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb2-1"><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">&gt;</span> cl <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> parallel<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">makePSOCKcluster</span>(<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">192</span>)</span>
<span id="cb2-2">Error <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">in</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">socketAccept</span>(<span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">socket =</span> socket, <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">blocking =</span> <span class="cn" style="color: #8f5902;
background-color: null;
font-style: inherit;">TRUE</span>, <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">open =</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"a+b"</span>,  <span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">:</span> </span>
<span id="cb2-3">  all connections are <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">in</span> use</span></code></pre></div>
<p>Now, assume you use:</p>
<div class="sourceCode" id="cb3" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb3-1"><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">&gt;</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">library</span>(parallelly)</span>
<span id="cb3-2"><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">&gt;</span> nworkers <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">availableCores</span>()</span>
<span id="cb3-3"><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">&gt;</span> cl <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">makeClusterPSOCK</span>(ncores)</span></code></pre></div>
<p>to set up a maximum-sized cluster on the current machine. This works as long as <code>availableCores()</code> returns something less than 125. However, if you are on machine with, say, 192 CPU cores, you will get the above error. You could do something like:</p>
<div class="sourceCode" id="cb4" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb4-1"><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">&gt;</span> nworkers <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">availableCores</span>()</span>
<span id="cb4-2"><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">&gt;</span> nworkers <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">max</span>(nworkers, <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">125</span>L)</span></code></pre></div>
<p>to work around this problem. Or, if you want to be more agile to what R supports, you could do:</p>
<div class="sourceCode" id="cb5" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb5-1"><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">&gt;</span> nworkers <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">availableCores</span>()</span>
<span id="cb5-2"><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">&gt;</span> nworkers <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">max</span>(nworkers, <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">freeConnections</span>())</span></code></pre></div>
<p>With the latest versions of <strong>parallelly</strong>, you can simplify this to:</p>
<div class="sourceCode" id="cb6" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb6-1"><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">&gt;</span> nworkers <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">availableCores</span>(<span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">constraints =</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"connections"</span>)</span></code></pre></div>
<p>The <code>availableWorkers()</code> function also supports <code>constraints = "connections"</code>.</p>
<p>(*) The only way to increase this limit is to change the R source code and build R from source, cf.&nbsp;<a href="https://parallelly.futureverse.org/reference/availableConnections.html"><code>freeConnections()</code></a>.</p>
</section>
<section id="forcefully-terminate-psock-cluster-nodes" class="level2">
<h2 class="anchored" data-anchor-id="forcefully-terminate-psock-cluster-nodes">Forcefully terminate PSOCK cluster nodes</h2>
<p>The <code>parallel::stopCluster()</code> should be used for stopping a parallel cluster. This works by asking the clusters node to shut themselves down. However, a parallel worker will only shut down this way when it receives the message, which can only happen when the worker is done processing any parallel tasks. So, if a worker runs a very long-running task, which can take minutes, hours, or even days, it will not shut down until after that completes.</p>
<p>This far, we had to turn to special operating-system tools to kill the R process for that cluster worker. With <strong>parallelly</strong> 1.33.0, you can now use <code>killNode()</code> to kill any parallel worker that runs on the local machine and that was launched by <a href="https://parallelly.futureverse.org/reference/makeClusterPSOCK.html"><code>makeClusterPSOCK()</code></a>. For example,</p>
<div class="sourceCode" id="cb7" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb7-1"><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">&gt;</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">library</span>(parallelly)</span>
<span id="cb7-2"><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">&gt;</span> cl <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">makeClusterPSOCK</span>(<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">10</span>)</span>
<span id="cb7-3"><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">&gt;</span> cl</span>
<span id="cb7-4">Socket cluster with <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">10</span> nodes where <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">10</span> nodes are on host <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">'localhost'</span></span>
<span id="cb7-5">(R version <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">4</span>.<span class="fl" style="color: #AD0000;
background-color: null;
font-style: inherit;">2.2</span> (<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">2022-10-31</span>), platform x86_64<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span>pc<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span>linux<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span>gnu)</span>
<span id="cb7-6"><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">&gt;</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">which</span>(<span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">isNodeAlive</span>(cl))</span>
<span id="cb7-7"> [<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span>]  <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span>  <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">2</span>  <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">3</span>  <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">4</span>  <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">5</span>  <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">6</span>  <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">7</span>  <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">8</span>  <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">9</span> <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">10</span></span>
<span id="cb7-8"> </span>
<span id="cb7-9"><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">&gt;</span> success <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">killNode</span>(cl[<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">:</span><span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">3</span>])</span>
<span id="cb7-10"><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">&gt;</span> success</span>
<span id="cb7-11">[<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span>] <span class="cn" style="color: #8f5902;
background-color: null;
font-style: inherit;">TRUE</span> <span class="cn" style="color: #8f5902;
background-color: null;
font-style: inherit;">TRUE</span> <span class="cn" style="color: #8f5902;
background-color: null;
font-style: inherit;">TRUE</span></span>
<span id="cb7-12"><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">&gt;</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">which</span>(<span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">isNodeAlive</span>(cl))</span>
<span id="cb7-13">[<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span>]  <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">4</span>  <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">5</span>  <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">6</span>  <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">7</span>  <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">8</span>  <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">9</span> <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">10</span></span>
<span id="cb7-14"><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">&gt;</span> cl <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> cl[<span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">isNodeAlive</span>(cl)]</span>
<span id="cb7-15">Socket cluster with <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">7</span> nodes where <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">7</span> nodes are on host <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">'localhost'</span></span>
<span id="cb7-16">(R version <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">4</span>.<span class="fl" style="color: #AD0000;
background-color: null;
font-style: inherit;">2.2</span> (<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">2022-10-31</span>), platform x86_64<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span>pc<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span>linux<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span>gnu)</span></code></pre></div>
<p>Over and out,</p>
<p>Henrik</p>
</section>
<section id="links" class="level2">
<h2 class="anchored" data-anchor-id="links">Links</h2>
<ul>
<li><strong>parallelly</strong> package: <a href="https://cran.r-project.org/package=parallelly">CRAN</a>, <a href="https://github.com/HenrikBengtsson/parallelly">GitHub</a>, <a href="https://parallelly.futureverse.org">pkgdown</a></li>
</ul>


</section>

 ]]></description>
  <category>R</category>
  <category>R</category>
  <category>package</category>
  <category>parallel</category>
  <category>parallelly</category>
  <category>HPC</category>
  <category>parallel processing</category>
  <guid>https://www.jottr.org/posts/2023-01-18-parallelly_1.34.0-cgroups2-and-more/</guid>
  <pubDate>Wed, 18 Jan 2023 22:00:00 GMT</pubDate>
</item>
<item>
  <title>progressr 0.13.0: cli + progressr = ♥</title>
  <dc:creator>Henrik Bengtsson</dc:creator>
  <link>https://www.jottr.org/posts/2023-01-10-progressr-cli/</link>
  <description><![CDATA[ 






<p><strong><a href="https://progressr.futureverse.org">progressr</a></strong> 0.13.0 is on CRAN. In the recent releases, <strong>progressr</strong> gained support for using <strong><a href="https://cli.r-lib.org/">cli</a></strong> to generate progress bars. Vice versa, <strong>cli</strong> can now report on progress via the <strong>progressr</strong> framework. Here are the details. For other updates to <strong>progressr</strong>, see <a href="https://progressr.futureverse.org/news/index.html">NEWS</a>.</p>
<div style="padding: 2ex; float: right;">
<center>
<img src="https://www.jottr.org/posts/2023-01-10-progressr-cli/resources/three_in_chinese.gif" alt="Three strokes writing three in Chinese">
</center>
</div>
<p>The <strong>progressr</strong> package, part of the <a href="https://www.futureverse.org">futureverse</a>, provides a minimal API for reporting progress updates in R. The design is to separate the representation of progress updates from how they are presented. What type of progress to signal is controlled by the developer. How these progress updates are rendered is controlled by the end user. For instance, some users may prefer visual feedback, such as a horizontal progress bar in the terminal, whereas others may prefer auditory feedback. The <strong>progressr</strong> package works also when processing R in parallel or distributed using the <strong><a href="https://future.futureverse.org">future</a></strong> framework.</p>
<section id="use-cli-progress-bars-for-progressr-reporting" class="level2">
<h2 class="anchored" data-anchor-id="use-cli-progress-bars-for-progressr-reporting">Use ‘cli’ progress bars for ‘progressr’ reporting</h2>
<p>In <strong>progressr</strong> (&gt;= 0.12.0) [2022-12-13], you can report on progress using <strong>cli</strong> progress bar. To do this, just set:</p>
<div class="sourceCode" id="cb1" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb1-1">progressr<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">handlers</span>(<span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">global =</span> <span class="cn" style="color: #8f5902;
background-color: null;
font-style: inherit;">TRUE</span>)  <span class="do" style="color: #5E5E5E;
background-color: null;
font-style: italic;">## automatically report on progress</span></span>
<span id="cb1-2">progressr<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">handlers</span>(<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"cli"</span>)          <span class="do" style="color: #5E5E5E;
background-color: null;
font-style: italic;">## ... using a 'cli' progress bar</span></span></code></pre></div>
<p>With these globals settings (e.g.&nbsp;in your <code>~/.Rprofile</code> file; see below), R reports progress as:</p>
<div class="sourceCode" id="cb2" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb2-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">library</span>(progressr)</span>
<span id="cb2-2">y <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">slow_sum</span>(<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">:</span><span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">10</span>)</span></code></pre></div>
<div class="quarto-figure quarto-figure-center">
<figure class="figure">
<p><img src="https://www.jottr.org/posts/2023-01-10-progressr-cli/resources/handler_cli-default-slow_sum.svg" class="img-fluid figure-img"></p>
<figcaption>Animation of a one-line, green-blocks cli progress bar in the terminal growing from 0% to 100% with an ETA estimate at the end</figcaption>
</figure>
</div>
<p>Just like regular <strong>cli</strong> progress bars, you can customize these in the same way. For instance, if you use the following from one of the <strong>cli</strong> examples:</p>
<div class="sourceCode" id="cb3" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb3-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">options</span>(<span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">cli.progress_bar_style =</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">list</span>(</span>
<span id="cb3-2">  <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">complete =</span> cli<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">col_yellow</span>(<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"\u2605"</span>),</span>
<span id="cb3-3">  <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">incomplete =</span> cli<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">col_grey</span>(<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"\u00b7"</span>)</span>
<span id="cb3-4">))</span></code></pre></div>
<p>you’ll get:</p>
<div class="quarto-figure quarto-figure-center">
<figure class="figure">
<p><img src="https://www.jottr.org/posts/2023-01-10-progressr-cli/resources/handler_cli-default-slow_sum-yellow-starts.svg" class="img-fluid figure-img"></p>
<figcaption>Animation of a one-line, yellow-starts cli progress bar in the terminal growing from 0% to 100% with an ETA estimate at the end</figcaption>
</figure>
</div>
</section>
<section id="configure-cli-to-report-progress-via-progressr" class="level2">
<h2 class="anchored" data-anchor-id="configure-cli-to-report-progress-via-progressr">Configure ‘cli’ to Report Progress via ‘progressr’</h2>
<p>You might have heard that <strong><a href="https://purrr.tidyverse.org/">purrr</a></strong> recently gained support for reporting on progress. If you didn’t, you can read about it in the tidyverse blog post ‘<a href="https://www.tidyverse.org/blog/2022/12/purrr-1-0-0/#progress-bars">purrr 1.0.0</a>’ on 2022-12-20. The gist is to pass <code>.progress = TRUE</code> to the <strong>purrr</strong> function of interest, and it’ll show a progress bar while it runs. For example, assume we the following slow function for calculating the square root:</p>
<div class="sourceCode" id="cb4" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb4-1">slow_sqrt <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">function</span>(x) { <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">Sys.sleep</span>(<span class="fl" style="color: #AD0000;
background-color: null;
font-style: inherit;">0.1</span>); <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">sqrt</span>(x) }</span></code></pre></div>
<p>If we call</p>
<div class="sourceCode" id="cb5" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb5-1">y <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> purrr<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">map</span>(<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">:</span><span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">30</span>, slow_sqrt, <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">.progress =</span> <span class="cn" style="color: #8f5902;
background-color: null;
font-style: inherit;">TRUE</span>)</span></code></pre></div>
<p>we’ll see a progress bar appearing after about two seconds:</p>
<div class="quarto-figure quarto-figure-center">
<figure class="figure">
<p><img src="https://www.jottr.org/posts/2023-01-10-progressr-cli/resources/handler_cli-default.svg" class="img-fluid figure-img"></p>
<figcaption>Animation of a one-line, green-blocks cli progress bar in the terminal growing from 0% to 100% with an ETA estimate at the end</figcaption>
</figure>
</div>
<p>This progress bar is produced by the <strong>cli</strong> package. Now, the neat thing with the <strong>cli</strong> package is that you can tell it to pass on the progress reporting to another progress framework, including that of the <strong>progressr</strong> package. To do this, set the R option:</p>
<div class="sourceCode" id="cb6" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb6-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">options</span>(<span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">cli.progress_handlers =</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"progressr"</span>)</span></code></pre></div>
<p>This causes <em>all</em> <strong>cli</strong> progress updates to be reported via <strong>progressr</strong>, so if you, for instance, already have set:</p>
<div class="sourceCode" id="cb7" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb7-1">progressr<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">handlers</span>(<span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">global =</span> <span class="cn" style="color: #8f5902;
background-color: null;
font-style: inherit;">TRUE</span>)</span>
<span id="cb7-2">red_heart <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> cli<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">col_red</span>(cli<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span>symbol<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">$</span>heart)</span>
<span id="cb7-3"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">handlers</span>(<span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">handler_txtprogressbar</span>(<span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">char =</span> red_heart))</span></code></pre></div>
<p>the above <code>purrr::map()</code> call will report on progress in the terminal using a classical R progress bar tweaked to use red hearts to fill the bar;</p>
<div class="quarto-figure quarto-figure-center">
<figure class="figure">
<p><img src="https://www.jottr.org/posts/2023-01-10-progressr-cli/resources/handler_txtprogressbar-custom-hearts.svg" class="img-fluid figure-img"></p>
<figcaption>Animation of a one-line, text-based red-hearts progress bar in the terminal growing from 0% to 100%</figcaption>
</figure>
</div>
<p>As another example, if you set:</p>
<div class="sourceCode" id="cb8" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb8-1">progressr<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">handlers</span>(<span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">global =</span> <span class="cn" style="color: #8f5902;
background-color: null;
font-style: inherit;">TRUE</span>)</span>
<span id="cb8-2">progressr<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">handlers</span>(<span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">c</span>(<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"beepr"</span>, <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"cli"</span>, <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"rstudio"</span>))</span></code></pre></div>
<p>R will report progress <em>concurrently</em> via audio using different <strong><a href="https://cran.r-project.org/package=beepr">beepr</a></strong> sounds, via the terminal as a <strong>cli</strong> progress bar, and the RStudio’s built-in progress bar - whenever progress is reported via the <strong>progressr</strong> framework <em>or</em> the <strong>cli</strong> framework.</p>
</section>
<section id="customize-progress-reporting-when-r-starts" class="level2">
<h2 class="anchored" data-anchor-id="customize-progress-reporting-when-r-starts">Customize progress reporting when R starts</h2>
<p>To safely configure the above for all your <em>interactive</em> R sessions, I recommend adding something like the following to your <code>~/.Rprofile</code> file (or in a standalone file using the <strong><a href="https://cran.r-project.org/package=startup">startup</a></strong> package):</p>
<div class="sourceCode" id="cb9" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb9-1"><span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">if</span> (<span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">interactive</span>() <span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">&amp;&amp;</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">requireNamespace</span>(<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"progressr"</span>, <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">quietly =</span> <span class="cn" style="color: #8f5902;
background-color: null;
font-style: inherit;">TRUE</span>)) {</span>
<span id="cb9-2">  <span class="do" style="color: #5E5E5E;
background-color: null;
font-style: italic;">## progressr reporting without need for with_progress()</span></span>
<span id="cb9-3">  progressr<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">handlers</span>(<span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">global =</span> <span class="cn" style="color: #8f5902;
background-color: null;
font-style: inherit;">TRUE</span>)</span>
<span id="cb9-4"></span>
<span id="cb9-5">  <span class="do" style="color: #5E5E5E;
background-color: null;
font-style: italic;">## Use 'cli', if installed ...</span></span>
<span id="cb9-6">  <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">if</span> (<span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">requireNamespace</span>(<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"cli"</span>, <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">quietly =</span> <span class="cn" style="color: #8f5902;
background-color: null;
font-style: inherit;">TRUE</span>)) {</span>
<span id="cb9-7">    progressr<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">handlers</span>(<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"cli"</span>)</span>
<span id="cb9-8">    <span class="do" style="color: #5E5E5E;
background-color: null;
font-style: italic;">## Hand over all 'cli' progress reporting to 'progressr'</span></span>
<span id="cb9-9">    <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">options</span>(<span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">cli.progress_handlers =</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"progressr"</span>)</span>
<span id="cb9-10">  } <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">else</span> {</span>
<span id="cb9-11">    <span class="do" style="color: #5E5E5E;
background-color: null;
font-style: italic;">## ... otherwise use the one that comes with R</span></span>
<span id="cb9-12">    progressr<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">handlers</span>(<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"txtprogressbar"</span>)</span>
<span id="cb9-13">  }</span>
<span id="cb9-14">  </span>
<span id="cb9-15">  <span class="do" style="color: #5E5E5E;
background-color: null;
font-style: italic;">## Use 'beepr', if installed ...</span></span>
<span id="cb9-16">  <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">if</span> (<span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">requireNamespace</span>(<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"beepr"</span>, <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">quietly =</span> <span class="cn" style="color: #8f5902;
background-color: null;
font-style: inherit;">TRUE</span>)) {</span>
<span id="cb9-17">    progressr<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">handlers</span>(<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"beepr"</span>, <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">append =</span> <span class="cn" style="color: #8f5902;
background-color: null;
font-style: inherit;">TRUE</span>)</span>
<span id="cb9-18">  }</span>
<span id="cb9-19">  </span>
<span id="cb9-20">  <span class="do" style="color: #5E5E5E;
background-color: null;
font-style: italic;">## Reporting via RStudio, if running in the RStudio Console,</span></span>
<span id="cb9-21">  <span class="do" style="color: #5E5E5E;
background-color: null;
font-style: italic;">## but not the terminal</span></span>
<span id="cb9-22">  <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">if</span> ((<span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">Sys.getenv</span>(<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"RSTUDIO"</span>) <span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">==</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"1"</span>) <span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">&amp;&amp;</span> </span>
<span id="cb9-23">      <span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">!</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">nzchar</span>(<span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">Sys.getenv</span>(<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"RSTUDIO_TERM"</span>))) {</span>
<span id="cb9-24">    progressr<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">handlers</span>(<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"rstudio"</span>, <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">append =</span> <span class="cn" style="color: #8f5902;
background-color: null;
font-style: inherit;">TRUE</span>)</span>
<span id="cb9-25">  }</span>
<span id="cb9-26">}</span></code></pre></div>
<p>See the <strong><a href="https://progressr.futureverse.org">progressr</a></strong> website for other, additional ways of reporting on progress.</p>
<p>Now, go make some progress!</p>
</section>
<section id="other-posts-on-progressr-reporting" class="level2">
<h2 class="anchored" data-anchor-id="other-posts-on-progressr-reporting">Other posts on progressr reporting</h2>
<ul>
<li><a href="../../2022/06/03/progressr-0.10.1/">progressr 0.10.1: Plyr Now Supports Progress Updates also in Parallel</a>, 2022-06-03</li>
<li><a href="../../2021/06/11/progressr-0.8.0/">progressr 0.8.0 - RStudio’s Progress Bar, Shiny Progress Updates, and Absolute Progress</a>, 2021-06-11</li>
<li><a href="../../2020/07/04/progressr-erum2020-slides/">e-Rum 2020 Slides on Progressr</a>, 2020-07-04</li>
<li>See also <a href="../../tags/#progressr-list">‘progressr’</a> tag.</li>
</ul>
</section>
<section id="links" class="level2">
<h2 class="anchored" data-anchor-id="links">Links</h2>
<ul>
<li><strong>progressr</strong> package: <a href="https://cran.r-project.org/package=progressr">CRAN</a>, <a href="https://github.com/HenrikBengtsson/progressr">GitHub</a>, <a href="https://progressr.futureverse.org">pkgdown</a></li>
<li><strong>cli</strong> package: <a href="https://cran.r-project.org/package=cli">CRAN</a>, <a href="https://github.com/r-lib/cli">GitHub</a>, <a href="https://cli.r-lib.org/">pkgdown</a></li>
<li><strong>future</strong> package: <a href="https://cran.r-project.org/package=future">CRAN</a>, <a href="https://github.com/HenrikBengtsson/future">GitHub</a>, <a href="https://future.futureverse.org">pkgdown</a></li>
</ul>


</section>

 ]]></description>
  <category>R</category>
  <category>R</category>
  <category>package</category>
  <category>progressr</category>
  <category>cli</category>
  <category>purrr</category>
  <category>future</category>
  <guid>https://www.jottr.org/posts/2023-01-10-progressr-cli/</guid>
  <pubDate>Wed, 11 Jan 2023 03:00:00 GMT</pubDate>
</item>
<item>
  <title>Please Avoid detectCores() in your R Packages</title>
  <dc:creator>Henrik Bengtsson</dc:creator>
  <link>https://www.jottr.org/posts/2022-12-05-avoid-detectCores/</link>
  <description><![CDATA[ 






<p>The <code>detectCores()</code> function of the <strong>parallel</strong> package is probably one of the most used functions when it comes to setting the number of parallel workers to use in R. In this blog post, I’ll try to explain why using it is not always a good idea. Already now, I am going to make a bold request and ask you to:</p>
<blockquote class="blockquote">
<p>Please <em>avoid</em> using <code>parallel::detectCores()</code> in your package!</p>
</blockquote>
<p>By reading this blog post, I hope you become more aware of the different problems that arise from using <code>detectCores()</code> and how they might affect you and the users of your code.</p>
<figure style="margin-top: 3ex;" class="figure">
<p><img src="https://www.jottr.org/posts/2022-12-05-avoid-detectCores/resources/detectCores_bad_vs_good.png" alt="Screenshots of two terminal-based, colored graphs each showing near 100% load on all 24 CPU cores. The load bars to the left are mostly red, whereas the ones to the right are most green. There is a shrug emoji, with the text 'do you want this?' pointing to the left and the text 'or that?' pointing to the right, located inbetween the two graphs." style="width: 100%; margin: 0; margin-bottom: 2ex;" class="figure-img"></p>
<figcaption style="font-style: italic">
<p>Figure&nbsp;1: Using <code>detectCores()</code> risks overloading the machine where R runs, even more so if there are other things already running. The machine seen at the left is heavily loaded, because too many parallel processes compete for the 24 CPU cores available, which results in an extensive amount of kernel context switching (red), which wastes precious CPU cycles. The machine to the right is near-perfectly loaded at 100%, where none of the processes use more than they may use (mostly green).</p>
</figcaption>
</figure>
<section id="tldr" class="level2">
<h2 class="anchored" data-anchor-id="tldr">TL;DR</h2>
<p>If you don’t have time to read everything, but will take my word that we should avoid <code>detectCores()</code>, then the quick summary is that you basically have two choices for the number of parallel workers to use by default;</p>
<ol type="1">
<li><p>Have your code run with a single core by default (i.e.&nbsp;sequentially), or</p></li>
<li><p>replace all <code>parallel::detectCores()</code> with <a href="https://parallelly.futureverse.org/reference/availableCores.html"><code>parallelly::availableCores()</code></a>.</p></li>
</ol>
<p>I’m in the conservative camp and recommend the first alternative. Using sequential processing by default, where the user has to make an explicit choice to run in parallel, significantly lowers the risk for clogging up the CPUs (left panel in Figure&nbsp;1), especially when there are other things running on the same machine.</p>
<p>The second alternative is useful if you’re not ready to make the move to run sequentially by default. The <code>availableCores()</code> function of the <strong><a href="https://parallelly.futureverse.org">parallelly</a></strong> package is fully backward compatible with <code>detectCores()</code>, while it avoids the most common problems that comes with <code>detectCores()</code>, plus it is agile to a lot more CPU-related settings, including settings that the end-user, the systems administrator, job schedulers and Linux containers control. It is designed to take care of common overuse issues so that you do not have to spend time worry about them.</p>
</section>
<section id="background" class="level2">
<h2 class="anchored" data-anchor-id="background">Background</h2>
<p>There are several problems with using <code>detectCores()</code> from the <strong>parallel</strong> package for deciding how many parallel workers to use. But before we get there, I want you to know that we find this function commonly used in R script and R packages, and frequently suggested in tutorials. So, do not feel ashamed if you use it.</p>
<p>If we scan the code of the R packages on CRAN (e.g.&nbsp;by <a href="https://github.com/search?q=org%3Acran+language%3Ar+%22detectCores%28%29%22&amp;type=code">searching GitHub</a><sup>1</sup>), or on Bioconductor (e.g.&nbsp;by <a href="https://code.bioconductor.org/search/search?q=detectCores%28%29)">searching Bioc::CodeSearch</a>) we find many cases where <code>detectCores()</code> is used. Here are some variants we see in the wild:</p>
<div class="sourceCode" id="cb1" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb1-1">cl <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">makeCluster</span>(<span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">detectCores</span>())</span>
<span id="cb1-2">cl <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">makeCluster</span>(<span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">detectCores</span>() <span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span> <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span>)</span>
<span id="cb1-3">y <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">mclapply</span>(..., <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">mc.cores =</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">detectCores</span>())</span>
<span id="cb1-4"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">registerDoParallel</span>(<span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">detectCores</span>())</span></code></pre></div>
<p>We also find functions that let the user choose the number of workers via some argument, which defaults to <code>detectCores()</code>. Sometimes the default is explicit, as in:</p>
<div class="sourceCode" id="cb2" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb2-1">fast_fcn <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">function</span>(x, <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">ncores =</span> parallel<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">detectCores</span>()) {</span>
<span id="cb2-2">  <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">if</span> (ncores <span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">&gt;</span> <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span>) {</span>
<span id="cb2-3">    cl <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">makeCluster</span>(ncores)</span>
<span id="cb2-4">    ...</span>
<span id="cb2-5">  }</span>
<span id="cb2-6">}</span></code></pre></div>
<p>and sometimes it’s implicit, as in:</p>
<div class="sourceCode" id="cb3" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb3-1">fast_fcn <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">function</span>(x, <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">ncores =</span> <span class="cn" style="color: #8f5902;
background-color: null;
font-style: inherit;">NULL</span>) {</span>
<span id="cb3-2">  <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">if</span> (<span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">is.null</span>(ncores)) </span>
<span id="cb3-3">    ncores <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> parallel<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">detectCores</span>() <span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span> <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span></span>
<span id="cb3-4">  <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">if</span> (ncores <span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">&gt;</span> <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span>) {</span>
<span id="cb3-5">    cl <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">makeCluster</span>(ncores)</span>
<span id="cb3-6">    ...</span>
<span id="cb3-7">  }</span>
<span id="cb3-8">}</span></code></pre></div>
<p>As we will see next, all the above examples are potentially buggy and might result in run-time errors.</p>
</section>
<section id="common-mistakes-when-using-detectcores" class="level2">
<h2 class="anchored" data-anchor-id="common-mistakes-when-using-detectcores">Common mistakes when using detectCores()</h2>
<section id="issue-1-detectcores-may-return-a-missing-value" class="level3">
<h3 class="anchored" data-anchor-id="issue-1-detectcores-may-return-a-missing-value">Issue 1: detectCores() may return a missing value</h3>
<p>A small, but important detail about <code>detectCores()</code> that is often missed is the following section in <code>help("detectCores", package = "parallel")</code>:</p>
<blockquote class="blockquote">
<p><strong>Value</strong></p>
<p>An integer, <strong>NA if the answer is unknown</strong>.</p>
</blockquote>
<p>Because of this, we cannot rely on:</p>
<div class="sourceCode" id="cb4" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb4-1">ncores <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">detectCores</span>()</span></code></pre></div>
<p>to always work, i.e.&nbsp;we might end up with errors like:</p>
<div class="sourceCode" id="cb5" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb5-1">ncores <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">detectCores</span>()</span>
<span id="cb5-2">workers <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> parallel<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">makeCluster</span>(ncores)</span>
<span id="cb5-3">Error <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">in</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">makePSOCKcluster</span>(<span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">names =</span> spec, ...) <span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">:</span> </span>
<span id="cb5-4">  numeric <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">'names'</span> must be <span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">&gt;=</span> <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span></span></code></pre></div>
<p>We need to account for this, especially as package developers. One way to handle it is simply by using:</p>
<div class="sourceCode" id="cb6" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb6-1">ncores <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">detectCores</span>()</span>
<span id="cb6-2"><span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">if</span> (<span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">is.na</span>(ncores)) ncores <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span>L</span></code></pre></div>
<p>or, by using the following shorter, but also harder to understand, one-liner:</p>
<div class="sourceCode" id="cb7" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb7-1">ncores <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">max</span>(<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span>L, <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">detectCores</span>(), <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">na.rm =</span> <span class="cn" style="color: #8f5902;
background-color: null;
font-style: inherit;">TRUE</span>)</span></code></pre></div>
<p>This construct is guaranteed to always return at least one core.</p>
<p><em>Shameless advertisement for the <strong><a href="https://parallelly.futureverse.org">parallelly</a></strong> package</em>: In contrast to <code>detectCores()</code>, <code>parallelly::availableCores()</code> handles the above case automatically, and it guarantees to always return at least one core.</p>
</section>
<section id="issue-2-detectcores-may-return-one" class="level3">
<h3 class="anchored" data-anchor-id="issue-2-detectcores-may-return-one">Issue 2: detectCores() may return one</h3>
<p>Although it’s rare to run into hardware with single-core CPUs these days, you might run into a virtual machine (VM) configured to have a single core. Because of this, you cannot reliably use:</p>
<div class="sourceCode" id="cb8" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb8-1">ncores <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">detectCores</span>() <span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span> <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span>L</span></code></pre></div>
<p>or</p>
<div class="sourceCode" id="cb9" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb9-1">ncores <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">detectCores</span>() <span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span> <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">2</span>L</span></code></pre></div>
<p>in your code. If you use these constructs, a user of your code might end up with zero or a negative number of cores here, which another way we can end up with an error downstream. A real-world example of this problem can be found in continous integration (CI) services, e.g.&nbsp;<code>detectCores()</code> returns 2 in GitHub Actions jobs. So, we need to account also for this case, which we can do by using the above <code>max()</code> solution, e.g.</p>
<div class="sourceCode" id="cb10" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb10-1">ncores <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">max</span>(<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span>L, <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">detectCores</span>() <span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span> <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">2</span>L, <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">na.rm =</span> <span class="cn" style="color: #8f5902;
background-color: null;
font-style: inherit;">TRUE</span>)</span></code></pre></div>
<p>This is guaranteed to always return at least one.</p>
<p><em>Shameless advertisement for the <strong><a href="https://parallelly.futureverse.org">parallelly</a></strong> package</em>: In contrast, <code>parallelly::availableCores()</code> handles this case via argument <code>omit</code>, which makes it easier to understand the code, e.g.</p>
<div class="sourceCode" id="cb11" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb11-1">ncores <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">availableCores</span>(<span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">omit =</span> <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">2</span>)</span></code></pre></div>
<p>This construct is guaranteed to return at least one core, e.g.&nbsp;if there are one, two, or three CPU cores on this machine, <code>ncores</code> will be one in all three cases.</p>
</section>
<section id="issue-3-detectcores-may-return-too-many-cores" class="level3">
<h3 class="anchored" data-anchor-id="issue-3-detectcores-may-return-too-many-cores">Issue 3: detectCores() may return too many cores</h3>
<p>When we use PSOCK, SOCK, or MPI clusters as defined by the <strong>parallel</strong> package, the communication between the main R session and the parallel workers is done via R socket connection. Low-level functions <code>parallel::makeCluster()</code>, <code>parallelly::makeClusterPSOCK()</code>, and legacy <code>snow::makeCluster()</code> create these types of clusters. In turn, there are higher-level functions that rely on these low-level functions, e.g.&nbsp;<code>doParallel::registerDoParallel()</code> uses <code>parallel::makeCluster()</code> if you are on MS Windows, <code>BiocParallel::SnowParam()</code> uses <code>snow::makeCluster()</code>, and <code>plan(multisession)</code> and <code>plan(cluster)</code> of the <strong><a href="https://future.futureverse.org">future</a></strong> package uses <code>parallelly::makeClusterPSOCK()</code>.</p>
<p>R has a limit in the number of connections it can have open at any time. As of R 4.2.2, <a href="https://github.com/HenrikBengtsson/Wishlist-for-R/issues/28">the limit is 125 open connections</a>. Because of this, we can use at most 125 parallel PSOCK, SOCK, or MPI workers. In practice, this limit is lower, because some connections may already be in use elsewhere. To find the current number of free connections, we can use <a href="https://parallelly.futureverse.org/reference/availableConnections.html"><code>parallelly::freeConnections()</code></a>. If we try to launch a cluster with too many workers, there will not be enough connections available for the communication and the setup of the cluster will fail. For example, a user running on a 192-core machine will get errors such as:</p>
<div class="sourceCode" id="cb12" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb12-1"><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">&gt;</span> cl <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> parallel<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">makeCluster</span>(<span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">detectCores</span>())</span>
<span id="cb12-2">Error <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">in</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">socketAccept</span>(<span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">socket =</span> socket, <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">blocking =</span> <span class="cn" style="color: #8f5902;
background-color: null;
font-style: inherit;">TRUE</span>, <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">open =</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"a+b"</span>,  <span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">:</span> </span>
<span id="cb12-3">  all connections are <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">in</span> use</span></code></pre></div>
<p>and</p>
<div class="sourceCode" id="cb13" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb13-1"><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">&gt;</span> cl <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> parallelly<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">makeClusterPSOCK</span>(<span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">detectCores</span>())</span>
<span id="cb13-2">Error<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">:</span> Cannot create <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">192</span> parallel PSOCK nodes. Each node needs</span>
<span id="cb13-3">one connection, but there are only <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">124</span> connections left out of</span>
<span id="cb13-4">the maximum <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">128</span> available on this R installation</span></code></pre></div>
<p>Thus, if we use <code>detectCores()</code>, our R code will not work on larger, modern machines. This is a problem that will become more and more common as more users get access to more powerful computers. Hopefully, R will increase this connection limit in a future release, but until then, you as the developer are responsible to handle also this case. To make your code agile to this limit, also if R increases it, you can use:</p>
<div class="sourceCode" id="cb14" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb14-1">ncores <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">max</span>(<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span>L, <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">detectCores</span>(), <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">na.rm =</span> <span class="cn" style="color: #8f5902;
background-color: null;
font-style: inherit;">TRUE</span>)</span>
<span id="cb14-2">ncores <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">min</span>(parallelly<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">freeConnections</span>(), ncores)</span></code></pre></div>
<p>This is guaranteed to return at least zero (sic!) and never more than what is required to create a PSOCK, SOCK, and MPI cluster with than many parallel workers.</p>
<p><em>Shameless advertisement for the <strong><a href="https://parallelly.futureverse.org">parallelly</a></strong> package</em>: In the upcoming <strong>parallelly</strong> 1.33.0 version, you can use <code>parallelly::availableCores(constraints = "connections")</code> to limit the result to the current number of available R connections. In addition, you can control the maximum number of cores that <code>availableCores()</code> returns by setting R option <code>parallelly.availableCores.system</code>, or environment variable <code>R_PARALLELLY_AVAILABLECORES_SYSTEM</code>, e.g.&nbsp;<code>R_PARALLELLY_AVAILABLECORES_SYSTEM=120</code>.</p>
</section>
</section>
<section id="issue-4-detectcores-does-not-give-the-number-of-allowed-cores" class="level2">
<h2 class="anchored" data-anchor-id="issue-4-detectcores-does-not-give-the-number-of-allowed-cores">Issue 4: detectCores() does not give the number of “allowed” cores</h2>
<p>There’s a note in <code>help("detectCores", package = "parallel")</code> that touches on the above problems, but also on other important limitations that we should know of:</p>
<blockquote class="blockquote">
<p><strong>Note</strong></p>
<p>This [= <code>detectCores()</code>] is not suitable for use directly for the <code>mc.cores</code> argument of <code>mclapply</code> nor specifying the number of cores in <code>makeCluster</code>. First because it may return <code>NA</code>, second because it does not give the number of <em>allowed</em> cores, and third because on Sparc Solaris and some Windows boxes it is not reasonable to try to use all the logical CPUs at once.</p>
</blockquote>
<p><strong>When is this relevant? The answer is: Always!</strong> This is because as package developers, we cannot really know when this occurs, because we never know on what type of hardware and system our code will run. So, we have to account for these unknowns too.</p>
<p>Let’s look at some real-world case where using <code>detectCores()</code> can become a real issue.</p>
<section id="a.-a-personal-computer" class="level3">
<h3 class="anchored" data-anchor-id="a.-a-personal-computer">4a. A personal computer</h3>
<p>A user might want to run other software tools at the same time while running the R analysis. A very common pattern we find in R code is to save one core for other purposes, say, browsing the web, e.g.</p>
<div class="sourceCode" id="cb15" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb15-1">ncores <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">detectCores</span>() <span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span> <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span>L</span></code></pre></div>
<p>This is a good start. It is the first step toward your software tool acknowledging that there might be other things running on the same machine. However, contrary to end-users, we as package developers cannot know how many cores the user needs, or wishes, to set aside. Because of this, it is better to let the user make this decision.</p>
<p>A related scenario is when the user wants to run two concurrent R sessions on the same machine, both using your code. If your code assumes it can use all cores on the machine (i.e.&nbsp;<code>detectCores()</code> cores), the user will end up running the machine at 200% of its capacity. Whenever we use over 100% of the available CPU resources, we get penalized and waste our computational cycles on overhead from context switching, sub-optimal memory access, and more. This is where we end up with the situation illustrated in the left part of Figure&nbsp;1.</p>
<p>Note also that users might not know that they use an R function that runs on all cores by default. They might not even be aware that this is a problem. Now, imagine if the user runs three or four such R sessions, resulting in a 300-400% CPU load. This is when things start to run slowly. The computer will be sluggish, maybe unresponsive, and mostly likely going to get very hot (“we’re frying the computer”). By the time the four concurrent R processes complete, the user might have been able to finish six to eight similar processes if they would not have been fighting each other for the limited CPU resources.</p>
<!--
If this happens on a shared system, the user might get an email from
the systems adminstrator asking you why they are "trying to fry the
computer".  The user gets blamed for something that is our fault - it
is us that decided to run on `detectCores()` CPU cores by default.

This leads us to another scenario where a user might run into a case
where the CPUs are overwhelmed because a software tool assumes it has
exclusive right to all cores.  
-->
</section>
<section id="b.-a-shared-computer" class="level3">
<h3 class="anchored" data-anchor-id="b.-a-shared-computer">4b. A shared computer</h3>
<p>In the academia and the industry, it is common that several users share the same compute server or set of compute nodes. It might be as simple as they SSH into a shared machine with many cores and large amounts of memory to run their analysis there. On such setups, load balancing between users is often based on an honor system, where each user checks how many resources are available before launching an analysis. This helps to make sure they don’t end up using too many cores, or too much memory, slowing down the computer for everyone else.</p>
<div style="width: 38%; float: right;">
<figure style="margin-left: 2ex; margin-top: 1ex;" class="figure">
<img src="https://www.jottr.org/posts/2022-12-05-avoid-detectCores/resources/detectCores_bad.png" alt="The left-handside graph of Figure 1, which shows mostly red bars at near 100% load for 24 CPU cores." style="width: 100%; margin: 0; margin-bottom: 2ex;" class="figure-img">
<figcaption>
Figure 2: Overusing the CPU cores brings everything to a halt.
</figcaption>
</figure>
</div>
<p>Now, imagine they run a software tool that uses all CPU cores by default. In that case, there is a significant risk they will step on the other users’ processes, slowing everything down for everyone, especially if there is already a big load on the machine. From my experience in academia, this happens frequently. The user causing the problem is often not aware, because they just launch the problematic software with the default settings, leave it running, with a plan to coming back to it a few hours or a few days later. In the meantime, other users might wonder why their command-line prompts become sluggish or even non-responsive, and their analyses suddenly take forever to complete. Eventually, someone or something alerts the systems administrators to the problem, who end up having to drop everything else and start troubleshooting. This often results in them terminating the wild-running processes and reaching out to the user who runs the problematic software, which leads to a large amount of time and resources being wasted among users and administrators. All this is only because we designed our R package to use all cores by default. This is not a made-up toy story; it is a very likely scenario that happens on shared servers if you make <code>detectCores()</code> the default in your R code.</p>
<p><em>Shameless advertisement for the <strong><a href="https://parallelly.futureverse.org">parallelly</a></strong> package</em>: In contrast to <code>detectCores()</code>, if you use <code>parallelly::availableCores()</code> the user, or the systems administrator, can limit the default number of CPU cores returned by setting environment variable <code>R_PARALLELLY_AVAILABLECORES_FALLBACK</code>. For instance, by setting it to <code>R_PARALLELLY_AVAILABLECORES_FALLBACK=2</code> centrally, <code>availableCores()</code> will, unless there are other settings that allow the process to use more, return two cores regardless how many CPU cores the machine has. This will lower the damage any single process can inflict on the system. It will take many such processes running at the same time in order for them to have an overall a negative impact. The risk for that to happen by mistake is much lower than when using <code>detectCores()</code> by default.</p>
</section>
<section id="c.-a-shared-compute-cluster-with-many-machines" class="level3">
<h3 class="anchored" data-anchor-id="c.-a-shared-compute-cluster-with-many-machines">4c. A shared compute cluster with many machines</h3>
<p>Other, larger compute systems, often referred to as high-performance compute (HPC) cluster, have a job scheduler for running scripts in batches distributed across multiple machines. When users submit their scripts to the scheduler’s job queue, they request how many cores and how much memory each job requires. For example, a user on a Slurm cluster can request that their <code>run_my_rscript.sh</code> script gets to run with 48 CPU cores and 256 GiB of RAM by submitting it to the scheduler as:</p>
<div class="sourceCode" id="cb16" style="background: #f1f3f5;"><pre class="sourceCode sh code-with-copy"><code class="sourceCode bash"><span id="cb16-1"><span class="ex" style="color: null;
background-color: null;
font-style: inherit;">sbatch</span> <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">--cpus-per-task</span><span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span>48 <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">--mem</span><span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span>256G run_my_rscript.sh</span></code></pre></div>
<p>The scheduler keeps track of all running and queued jobs, and when enough compute slots are freed up, it will launch the next job in the queue, giving it the compute resources it requested. This is a very convenient and efficient way to batch process a large amount of analyses coming from many users.</p>
<p>However, just like with a shared server, it is important that the software tools running this way respect the compute resources that the job scheduler allotted to the job. The <code>detectCores()</code> function does <em>not</em> know about job schedulers - all it does is return the number of CPU cores on the current machine regardless of how many cores the job has been allotted by the scheduler. So, if your R package uses <code>detectCores()</code> cores by default, then it will overuse the CPUs and slow things down for everyone running on the same compute node. Again, when this happens, it often slows everything done and triggers lots of wasted user and admin efforts spent on troubleshooting and communication back and forth.</p>
<p><em>Shameless advertisement for the <strong><a href="https://parallelly.futureverse.org">parallelly</a></strong> package</em>: In contrast, <code>parallelly::availableCores()</code> respects the number of CPU slots that the job scheduler has given to the job. It recognizes environment variables set by our most common HPC schedulers, including Fujitsu Technical Computing Suite (PJM), Grid Engine (SGE), Load Sharing Facility (LSF), PBS/Torque, and Simple Linux Utility for Resource Management (Slurm).</p>
</section>
<section id="d.-running-r-via-cgroups-on-in-a-linux-container" class="level3">
<h3 class="anchored" data-anchor-id="d.-running-r-via-cgroups-on-in-a-linux-container">4d. Running R via CGroups on in a Linux container</h3>
<p>This far, we have been concerned about the overuse of the CPU cores affecting other processes and other users running on the same machine. Some systems are configured to protect against misbehaving software from affecting other users. In Linux, this can be done with so-called control groups (“cgroups”), where a process gets allotted a certain amount of CPU cores. If the process uses too many parallel workers, they cannot break out from the sandbox set up by cgroups. From the outside, it will look like the process uses its maximum amount of allocated CPU cores. Some HPC job schedulers have this feature enabled, but not all of them. You find the same feature for Linux containers, e.g.&nbsp;we can limit the number of CPU cores, or throttle the CPU load, using command-line options when you launch a Docker container, e.g.&nbsp;<code>docker run --cpuset-cpus=0-2,8 …</code> or <code>docker run --cpu=3.4 …</code>.</p>
<p>So, if you are a user on a system where compute resources are compartmentalized this way, you run a much lower risk for wreaking havoc on a shared system. That is good news, but if you run too many parallel workers, that is, try to use more cores than available to you, then you will clog up your own analysis. The behavior would be the same as if you request 96 parallel workers on your local eight-core notebook (the scenario in the left panel of Figure&nbsp;1), with the exception that you will not overheat the computer.</p>
<p>The problem with <code>detectCores()</code> is that it returns the number of CPU cores on the hardware, regardless of the cgroups settings. So, if your R process is limited to eight cores by cgroups, and you use <code>ncores = detectCores()</code> on a 96-core machine, you will end up running 96 parallel workers fighting for the resources on eight cores. A real-world example of this happens for those of you who have a free account on RStudio Cloud. In that case, you are given only a single CPU core to run your R code on, but the underlying machine typically has 16 cores. If you use <code>detectCores()</code> there, you will end up creating 16 parallel workers, running on the same CPU core, which is a very ineffecient way to run the code.</p>
<p><em>Shameless advertisement for the <strong><a href="https://parallelly.futureverse.org">parallelly</a></strong> package</em>: In contrast to <code>detectCores()</code>, <code>parallelly::availableCores()</code> respects cgroups, and will return eight cores instead of 96 in the above example, and a single core on a free RStudio Cloud account.</p>
</section>
</section>
<section id="my-opinionated-recommendation" class="level2">
<h2 class="anchored" data-anchor-id="my-opinionated-recommendation">My opinionated recommendation</h2>
<div style="width: 38%; float: right;">
<figure style="margin-left: 2ex; margin-top: 1ex;" class="figure">
<img src="https://www.jottr.org/posts/2022-12-05-avoid-detectCores/resources/detectCores_good.png" alt="The right-handside graph of Figure 1, which shows mostly green bars at near 100% load for 24 CPU cores." style="width: 100%; margin: 0; margin-bottom: 2ex;" class="figure-img">
<figcaption>
Figure 3: If we avoid overusing the CPU cores, then everything will run much smoother and much faster.
</figcaption>
</figure>
</div>
<p>As developers, I think we should at least be aware of these problems, and acknowledge that they exist and they are indeed real problem that people run into “out there”. We should also accept that we cannot predict on what type of compute environment our R code will run on. Unfortunately, I don’t have a magic solution that addresses all the problems reported here. That said, I think the best we can do is to be conservative and don’t make hard-coded decisions on parallelization in our R packages and R scripts.</p>
<p>Because of this, I argue that <strong>the safest is to design your R package to run sequentially by default (e.g.&nbsp;<code>ncores = 1L</code>), and leave it to the user to decide on the number of parallel workers to use.</strong></p>
<p>The <strong>second-best alternative</strong> that I can come up with, is to replace <code>detectCores()</code> with <code>availableCores()</code>, e.g.&nbsp;<code>ncores = parallelly::availableCores()</code>. It is designed to respect common system and R settings that control the number of allowed CPU cores. It also respects R options and environment variables commonly used to limit CPU usage, including those set by our most common HPC job schedulers. In addition, it is possible to control the <em>fallback</em> behavior so that it uses only a few cores when nothing else being set. For example, if the environment variable <code>R_PARALLELLY_AVAILABLECORES_FALLBACK</code> is set to <code>2</code>, then <code>availableCores()</code> returns two cores by default, unless other settings allowing more are available. A conservative systems administrator may want to set <code>export R_PARALLELLY_AVAILABLECORES_FALLBACK=1</code> in <code>/etc/profile.d/single-core-by-default.sh</code>. To see other benefits from using <code>availableCores()</code>, see <a href="https://parallelly.futureverse.org" class="uri">https://parallelly.futureverse.org</a>.</p>
<p>Believe it or not, there’s actually more to be said on this topic, but I think this is already more than a mouthful, so I will save that for another blog post. If you made it this far, I applaud you and I thank you for your interest. If you agree, or disagree, or have additional thoughts around this, please feel free to reach out on the <a href="https://github.com/HenrikBengtsson/future/discussions/">Future Discussions Forum</a>.</p>
<p>Over and out,</p>
<p>Henrik</p>
<p><small><sup>1</sup> Searching code on GitHub, requires you to log in to GitHub.</small></p>
<p>UPDATE 2022-12-06: <a href="https://github.com/HenrikBengtsson/future/discussions/656">Alex Chubaty pointed out another problem</a>, where <code>detectCores()</code> can be too large on modern machines, e.g.&nbsp;machines with 128 or 192 CPU cores. I’ve added Section ‘Issue 3: detectCores() may return too many cores’ explaining and addressing this problem.</p>
<p>UPDATE 2022-12-11: Mention upcoming <code>parallelly::availableCores(constraints = "connections")</code>.</p>


</section>

 ]]></description>
  <category>R</category>
  <category>R</category>
  <category>package</category>
  <category>parallel</category>
  <category>parallelly</category>
  <category>detectCores</category>
  <category>availableCores</category>
  <category>parallel processing</category>
  <category>compute clusters</category>
  <category>HPC</category>
  <guid>https://www.jottr.org/posts/2022-12-05-avoid-detectCores/</guid>
  <pubDate>Tue, 06 Dec 2022 05:00:00 GMT</pubDate>
</item>
<item>
  <title>useR! 2022: My ‘Futureverse: Profile Parallel Code’ Slides</title>
  <dc:creator>Henrik Bengtsson</dc:creator>
  <link>https://www.jottr.org/posts/2022-06-22-future_useR2022/</link>
  <description><![CDATA[ 






<figure style="margin-top: 3ex;" class="figure">
<img src="https://www.jottr.org/posts/2022-06-22-future_useR2022/resources/BengtssonH_20220622-Future-useR2022_slide18.png" alt="Screenshot of Slide #18 in my presentation. A graphical time-chart representation of the events that takes place when calling the following code in R: plan(cluster, workers = 2); fs <- lapply(1:2, function(x) future(slow(x)); vs <- value(fs); There are two futures displayed in the time chart. Each future is represented by a blue, horizontal 'lifespan' bar. The second future starts slightly after the first one. Each future is evaluated in a separate worker, which is represented as pink horizontal 'evaluate' bar. The two 'lifespan' and the two 'evaluation' bars are overlapping indicating they run in parallel." style="width: 100%; margin: 0;" class="figure-img">
<figcaption>
Figure 1: A time chart of logged events for two futures resolved by two parallel workers. This is a screenshot of Slide #18 in my talk.
</figcaption>
</figure>
<p><img src="https://www.jottr.org/posts/2022-06-22-future_useR2022/resources/user2022-logo_450x300.webp" alt="The useR 2022 logo" style="width: 30%; float: right; margin: 2ex;"></p>
<p>Below are the slides for my <em>Futureverse: Profile Parallel Code</em> talk that I presented at the <a href="https://user2022.r-project.org/">useR! 2022</a> conference online and hosted by the Department of Biostatistics at Vanderbilt University Medical Center.</p>
<p>Title: Futureverse: Profile Parallel Code<br>
Speaker: Henrik Bengtsson<br>
Session: <a href="https://user2022.r-project.org/program/talks/#session-21-parallel-computing">#21: Parallel Computing</a>, chaired by Ilias Moutsopoulos<br>
Slides: <a href="https://docs.google.com/presentation/d/e/2PACX-1vTnpyj7qvyKr-COHaJAYjoGveoOJPYrstTmvC4farFk2vdwWb8O79kA5tn7klTS67_uoJJdKFPgKNql/pub?start=true&amp;loop=false&amp;delayms=60000&amp;slide=id.gf778290f24_0_165">HTML</a>, <a href="../../presentations/useR2022/BengtssonH_20220622-Future-useR2022.pdf">PDF</a> (24 slides)<br>
Video: <a href="https://www.youtube.com/watch?v=_lrPgNqT3SM&amp;t=2528s">official recording</a> (27m30s long starting at 42m10s)</p>
<p>Abstract:</p>
<p>“In this presentation, I share recent enhancements that allow developers and end-users to profile R code running in parallel via the future framework. With these new, frequently requested features, we can study how and where our computational resources are used. With the help of visualization (e.g., ggplot2 and Shiny), we can identify bottlenecks in our code and parallel setup. For example, if we find that some parallel workers are more idle than expected, we can tweak settings to improve the overall CPU utilization and thereby increase the total throughput and decrease the turnaround time (latency). These new benchmarking tools work out of the box on existing code and packages that build on the future package, including future.apply, furrr, and doFuture.</p>
<p>The future framework, available on CRAN since 2016, has been used by hundreds of R packages and is among the top 1% of most downloaded packages. It is designed to unify and leverage common parallelization frameworks in R and to make new and existing R code faster with minimal efforts of the developer. The futureverse allows you, the developer, to stay with your favorite programming style, and end-users are free to choose the parallel backend to use (e.g., on a local machine, across multiple machines, in the cloud, or on a high-performance computing (HPC) cluster).”</p>
<hr>
<p>I want to send out a big thank you to useR! organizers, staff, and volunteers, and everyone else who contributed to this event.</p>
<p>/Henrik</p>
<section id="links" class="level2">
<h2 class="anchored" data-anchor-id="links">Links</h2>
<ul>
<li>useR! 2022: https://user2022.r-project.org/</li>
<li><strong>futureverse</strong> website: https://www.futureverse.org/</li>
<li><strong>future</strong> package <a href="https://cran.r-project.org/package=future">CRAN</a>, <a href="https://github.com/HenrikBengtsson/future">GitHub</a>, <a href="https://future.futureverse.org/">pkgdown</a></li>
</ul>


</section>

 ]]></description>
  <category>R</category>
  <category>R</category>
  <category>package</category>
  <category>future</category>
  <category>useR</category>
  <category>talk</category>
  <category>presentation</category>
  <category>slides</category>
  <category>asynchronous</category>
  <category>parallel processing</category>
  <category>remote evaluation</category>
  <category>compute clusters</category>
  <category>HPC</category>
  <category>profiling</category>
  <category>video</category>
  <guid>https://www.jottr.org/posts/2022-06-22-future_useR2022/</guid>
  <pubDate>Fri, 24 Jun 2022 00:00:00 GMT</pubDate>
</item>
<item>
  <title>parallelly: Support for Fujitsu Technical Computing Suite High-Performance Compute (HPC) Environments</title>
  <link>https://www.jottr.org/posts/2022-06-09-parallelly_1.32.0-fujitsu-hpc/</link>
  <description><![CDATA[ 






<div style="padding: 2ex; float: right;">
<center>
<img src="https://www.jottr.org/posts/2022-06-09-parallelly_1.32.0-fujitsu-hpc/resources/parallelly-logo.png" alt="The 'parallelly' hexlogo">
</center>
</div>
<p><strong><a href="https://parallelly.futureverse.org">parallelly</a></strong> 1.32.0 is now on CRAN. One of the major updates is that <code>availableCores()</code> and <code>availableWorkers()</code>, and therefore also the <strong>future</strong> framework, gained support for the ‘Fujitsu Technical Computing Suite’ job scheduler. For other updates, please see <a href="https://parallelly.futureverse.org/news/index.html">NEWS</a>.</p>
<p>The <strong>parallelly</strong> package enhances the <strong>parallel</strong> package - our built-in R package for parallel processing - by improving on existing features and by adding new ones. Somewhat simplified, <strong>parallelly</strong> provides the things that you would otherwise expect to find in the <strong>parallel</strong> package. The <strong><a href="https://future.futureverse.org">future</a></strong> package relies on the <strong>parallelly</strong> package internally for local and remote parallelization.</p>
<section id="support-for-the-fujitsu-technical-computing-suite" class="level2">
<h2 class="anchored" data-anchor-id="support-for-the-fujitsu-technical-computing-suite">Support for the Fujitsu Technical Computing Suite</h2>
<p>Functions <a href="https://parallelly.futureverse.org/reference/availableCores.html"><code>availableCores()</code></a> and <a href="https://parallelly.futureverse.org/reference/availableWorkers.html"><code>availableWorkers()</code></a> now support the Fujitsu Technical Computing Suite. Fujitsu Technical Computing Suite is a high-performance compute (HPC) job scheduler, which is popular in Japan among other places, e.g.&nbsp;at RIKEN and Kyushu University.</p>
<p>Specifically, these functions now recognize environment variables <code>PJM_VNODE_CORE</code>, <code>PJM_PROC_BY_NODE</code>, and <code>PJM_O_NODEINF</code> set by the Fujitsu Technical Computing Suite scheduler. For example, if we submit a job script with:</p>
<div class="sourceCode" id="cb1" style="background: #f1f3f5;"><pre class="sourceCode sh code-with-copy"><code class="sourceCode bash"><span id="cb1-1"><span class="ex" style="color: null;
background-color: null;
font-style: inherit;">$</span> pjsub <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">-L</span> vnode=4 <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">-L</span> vnode-core=10 script.sh</span></code></pre></div>
<p>the scheduler will allocate four slots with ten cores each on one or more compute nodes. For example, we might get:</p>
<div class="sourceCode" id="cb2" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb2-1">parallelly<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">availableCores</span>()</span>
<span id="cb2-2"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">#&gt; [1] 10</span></span>
<span id="cb2-3"></span>
<span id="cb2-4">parallelly<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">availableWorkers</span>()</span>
<span id="cb2-5"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">#&gt;  [1] "node032" "node032" "node032" "node032" "node032"</span></span>
<span id="cb2-6"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">#&gt;  [6] "node032" "node032" "node032" "node032" "node032"</span></span>
<span id="cb2-7"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">#&gt; [11] "node032" "node032" "node032" "node032" "node032"</span></span>
<span id="cb2-8"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">#&gt; [16] "node032" "node032" "node032" "node032" "node032"</span></span>
<span id="cb2-9"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">#&gt; [21] "node032" "node032" "node032" "node032" "node032"</span></span>
<span id="cb2-10"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">#&gt; [26] "node032" "node032" "node032" "node032" "node032"</span></span>
<span id="cb2-11"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">#&gt; [31] "node109" "node109" "node109" "node109" "node109"</span></span>
<span id="cb2-12"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">#&gt; [36] "node109" "node109" "node109" "node109" "node109"</span></span></code></pre></div>
<p>In this example, the scheduler allocated three 10-core slots on compute node <code>node032</code> and one 10-core slot on compute node <code>node109</code>, totalling 40 CPU cores, as requested. Because of this, users on these systems can now use <a href="https://parallelly.futureverse.org/reference/makeClusterPSOCK.html"><code>makeClusterPSOCK()</code></a> to set up a parallel PSOCK cluster as:</p>
<div class="sourceCode" id="cb3" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb3-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">library</span>(parallelly)</span>
<span id="cb3-2">cl <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">makeClusterPSOCK</span>(<span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">availableWorkers</span>(), <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">rshcmd =</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"pjrsh"</span>)</span></code></pre></div>
<p>As shown above, this code picks up whatever <code>vnode</code> and <code>vnode-core</code> configuration were requested via the <code>pjsub</code> submission, and launch 40 parallel R workers via the <code>pjrsh</code> tool part of the Fujitsu Technical Computing Suite.</p>
<p>This also means that we can use:</p>
<div class="sourceCode" id="cb4" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb4-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">library</span>(future)</span>
<span id="cb4-2"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">plan</span>(cluster, <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">rshcmd =</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"pjrsh"</span>)</span></code></pre></div>
<p>when using the <strong>future</strong> framework, which uses <code>makeClusterPSOCK()</code> and <code>availableWorkers()</code> internally.</p>
</section>
<section id="avoid-having-to-specify-rshcmd-pjrsh" class="level2">
<h2 class="anchored" data-anchor-id="avoid-having-to-specify-rshcmd-pjrsh">Avoid having to specify rshcmd = “pjrsh”</h2>
<p>To avoid having to manually specify argument <code>rshcmd = "pjrsh"</code> manually, we can set it via environment variable <a href="https://parallelly.futureverse.org/reference/parallelly.options.html"><code>R_PARALLELLY_MAKENODEPSOCK_RSHCMD</code></a> (sic!) before launching R, e.g.</p>
<div class="sourceCode" id="cb5" style="background: #f1f3f5;"><pre class="sourceCode sh code-with-copy"><code class="sourceCode bash"><span id="cb5-1"><span class="bu" style="color: null;
background-color: null;
font-style: inherit;">export</span> <span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">R_PARALLELLY_MAKENODEPSOCK_RSHCMD</span><span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span>pjrsh</span></code></pre></div>
<p>To make this persistent, the user can add this line to their <code>~/.bashrc</code> shell startup script. Alternatively, the system administrator can add it to a <code>/etc/profile.d/*.sh</code> file of their choice.</p>
<p>With this environment variable set, it’s sufficient to do:</p>
<pre><code>library(parallelly)
cl &lt;- makeClusterPSOCK(availableWorkers())</code></pre>
<p>and</p>
<div class="sourceCode" id="cb7" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb7-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">library</span>(future)</span>
<span id="cb7-2"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">plan</span>(cluster)</span></code></pre></div>
<p>In addition to not having to remember using <code>rshcmd = "pjrsh"</code>, a major advantage of this approach is that the same R script works also on other systems, including the user’s local machine and HPC environments such as Slurm and SGE.</p>
<p>Over and out, and welcome to all Fujitsu Technical Computing Suite users!</p>
</section>
<section id="links" class="level2">
<h2 class="anchored" data-anchor-id="links">Links</h2>
<ul>
<li><strong>parallelly</strong> package: <a href="https://cran.r-project.org/package=parallelly">CRAN</a>, <a href="https://github.com/HenrikBengtsson/parallelly">GitHub</a>, <a href="https://parallelly.futureverse.org">pkgdown</a></li>
<li><strong>future</strong> package: <a href="https://cran.r-project.org/package=future">CRAN</a>, <a href="https://github.com/HenrikBengtsson/future">GitHub</a>, <a href="https://future.futureverse.org">pkgdown</a></li>
</ul>


</section>

 ]]></description>
  <category>R</category>
  <category>package</category>
  <category>future</category>
  <category>parallel</category>
  <category>parallelly</category>
  <category>HPC</category>
  <category>parallel processing</category>
  <guid>https://www.jottr.org/posts/2022-06-09-parallelly_1.32.0-fujitsu-hpc/</guid>
  <pubDate>Thu, 09 Jun 2022 20:00:00 GMT</pubDate>
</item>
<item>
  <title>parallelly 1.32.0: makeClusterPSOCK() Didn’t Work with Chinese and Korean Locales</title>
  <dc:creator>Henrik Bengtsson</dc:creator>
  <link>https://www.jottr.org/posts/2022-06-08-parallelly_1.32.0-chinese-korean-bugfix/</link>
  <description><![CDATA[ 






<div style="padding: 2ex; float: right;">
<center>
<img src="https://www.jottr.org/posts/2022-06-08-parallelly_1.32.0-chinese-korean-bugfix/resources/parallelly-logo.png" alt="The 'parallelly' hexlogo">
</center>
</div>
<p><strong><a href="https://parallelly.futureverse.org">parallelly</a></strong> 1.32.0 is on CRAN. This release fixes an important bug that affected users running with the Simplified Chinese, Traditional Chinese (Taiwan), or Korean locale. The bug caused <code>makeClusterPSOCK()</code>, and therefore also <code>future::plan("multisession")</code>, to fail with an error. For other updates, please see <a href="https://parallelly.futureverse.org/news/index.html">NEWS</a>.</p>
<p>The <strong>parallelly</strong> package enhances the <strong>parallel</strong> package - our built-in R package for parallel processing - by improving on existing features and by adding new ones. Somewhat simplified, <strong>parallelly</strong> provides the things that you would otherwise expect to find in the <strong>parallel</strong> package. The <strong><a href="https://future.futureverse.org">future</a></strong> package relies on the <strong>parallelly</strong> package internally for local and remote parallelization.</p>
<section id="important-bug-fix-for-chinese-and-korean-users" class="level2">
<h2 class="anchored" data-anchor-id="important-bug-fix-for-chinese-and-korean-users">Important bug fix for Chinese and Korean users</h2>
<p>It turns out that <a href="https://parallelly.futureverse.org/reference/makeClusterPSOCK.html"><code>makeClusterPSOCK()</code></a> has never<sup>[1]</sup> worked for users that have their computers set to use a Korean (<code>LANGUAGE=ko</code>), a Simplified Chinese (<code>LANGUAGE=zh_CN</code>), or a Traditional Chinese (Taiwan) (<code>LANGUAGE=zh_TW</code>) locale. For example,</p>
<div class="sourceCode" id="cb1" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb1-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">Sys.setLanguage</span>(<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"zh_CN"</span>)</span>
<span id="cb1-2"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">library</span>(parallelly)</span>
<span id="cb1-3">cl <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> parallelly<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">makeClusterPSOCK</span>(<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">2</span>)</span>
<span id="cb1-4"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">#&gt; 错误: ‘node$session_info$process$pid == pid’ is not TRUE</span></span>
<span id="cb1-5"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">#&gt; 此外: Warning message:</span></span>
<span id="cb1-6"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">#&gt; In add_cluster_session_info(cl[ii]) : 强制改变过程中产生了NA</span></span></code></pre></div>
<p>The workaround was to pass <code>validate = FALSE</code>, e.g.</p>
<div class="sourceCode" id="cb2" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb2-1">cl <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> parallelly<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">makeClusterPSOCK</span>(<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">2</span>, <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">validate =</span> <span class="cn" style="color: #8f5902;
background-color: null;
font-style: inherit;">FALSE</span>)</span></code></pre></div>
<p>This bug was because of an internal assertion that made incorrect assumptions about what <code>print()</code> for <code>SOCK0node</code> and <code>SOCKnode</code> object would output. It worked with most locales, but not with the above three. I have fixed this in the most recent release of <strong>parallelly</strong>.</p>
<p>Since the ‘multisession’ strategy of the <strong><a href="https://future.futureverse.org">future</a></strong> framework relies on <code>makeClusterPSOCK()</code>, this bug affected also the <strong>future</strong> package, e.g.</p>
<div class="sourceCode" id="cb3" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb3-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">Sys.setLanguage</span>(<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"ko"</span>)</span>
<span id="cb3-2"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">library</span>(future)</span>
<span id="cb3-3"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">plan</span>(multisession)</span>
<span id="cb3-4"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">#&gt; 에러: 'node$session_info$process$pid == pid' is not TRUE</span></span>
<span id="cb3-5"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">#&gt; 추가정보: 경고메시지(들): </span></span>
<span id="cb3-6"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">#&gt; add_cluster_session_info(cl[ii])에서: 강제형변환에 의해 생성된 NA 입니다</span></span></code></pre></div>
<p>So, if you run into these errors, upgrade to the latest version of <strong>parallelly</strong>, e.g.&nbsp;<code>update.packages()</code>, restart R, and it will work as you would expect.</p>
<!--
Source: https://chinesefor.us/lessons/say-sorry-chinese-apologize-duibuqi/ and https://www.wikihow.com/Apologize-in-Korean
-->
<p>To prevent this from happening again, I am now making sure to always check the package with also these locales, in addition to English. CRAN already checks packages <a href="https://cran.r-project.org/web/checks/check_flavors.html">with different English and German locales</a>.</p>
<p>I am sorry, 对不起, 미안해요, about this. Hopefully, it’ll work smoother from now on.</p>
<p>Happy parallelization!</p>
</section>
<section id="links" class="level2">
<h2 class="anchored" data-anchor-id="links">Links</h2>
<ul>
<li><strong>parallelly</strong> package: <a href="https://cran.r-project.org/package=parallelly">CRAN</a>, <a href="https://github.com/HenrikBengtsson/parallelly">GitHub</a>, <a href="https://parallelly.futureverse.org">pkgdown</a></li>
<li><strong>future</strong> package: <a href="https://cran.r-project.org/package=future">CRAN</a>, <a href="https://github.com/HenrikBengtsson/future">GitHub</a>, <a href="https://future.futureverse.org">pkgdown</a></li>
</ul>
<p><sup>[1]</sup> The last time it worked was with <strong>future</strong> 1.4.0 (2017-03-13), when this function was still part of the <strong>future</strong> package.</p>


</section>

 ]]></description>
  <category>R</category>
  <category>package</category>
  <category>future</category>
  <category>parallel</category>
  <category>parallelly</category>
  <category>HPC</category>
  <category>parallel processing</category>
  <category>locale</category>
  <category>i18n</category>
  <guid>https://www.jottr.org/posts/2022-06-08-parallelly_1.32.0-chinese-korean-bugfix/</guid>
  <pubDate>Wed, 08 Jun 2022 21:00:00 GMT</pubDate>
</item>
<item>
  <title>progressr 0.10.1: Plyr Now Supports Progress Updates also in Parallel</title>
  <dc:creator>Henrik Bengtsson</dc:creator>
  <link>https://www.jottr.org/posts/2022-06-03-progressr-plyr-parallel/</link>
  <description><![CDATA[ 






<div style="padding: 2ex; float: right;">
<center>
<img src="https://www.jottr.org/posts/2022-06-03-progressr-plyr-parallel/resources/three_in_chinese.gif" alt="Three strokes writing three in Chinese">
</center>
</div>
<p><strong><a href="https://progressr.futureverse.org">progressr</a></strong> 0.10.1 is on CRAN. I dedicate this release to all <strong><a href="https://cran.r-project.org/package=plyr">plyr</a></strong> users and developers out there.</p>
<p>The <strong>progressr</strong> package provides a minimal API for reporting progress updates in R. The design is to separate the representation of progress updates from how they are presented. What type of progress to signal is controlled by the developer. How these progress updates are rendered is controlled by the end user. For instance, some users may prefer visual feedback, such as a horizontal progress bar in the terminal, whereas others may prefer auditory feedback. The <strong>progressr</strong> package works also when processing R in parallel or distributed using the <strong><a href="https://future.futureverse.org">future</a></strong> framework.</p>
<section id="plyr-future-progressr-parallel-progress-reporting" class="level2">
<h2 class="anchored" data-anchor-id="plyr-future-progressr-parallel-progress-reporting"><strong>plyr</strong> + <strong>future</strong> + <strong>progressr</strong> ⇒ parallel progress reporting</h2>
<p>The major update in this release, is that <strong><a href="https://cran.r-project.org/package=plyr">plyr</a></strong> (&gt;= 1.8.7) now has built-in support for the <strong>progressr</strong> package when running in parallel. For example,</p>
<div class="sourceCode" id="cb1" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb1-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">library</span>(plyr)</span>
<span id="cb1-2"></span>
<span id="cb1-3"><span class="do" style="color: #5E5E5E;
background-color: null;
font-style: italic;">## Parallelize on the local machine</span></span>
<span id="cb1-4">future<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">plan</span>(<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"multisession"</span>)</span>
<span id="cb1-5">doFuture<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">registerDoFuture</span>()</span>
<span id="cb1-6"></span>
<span id="cb1-7"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">library</span>(progressr)</span>
<span id="cb1-8"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">handlers</span>(<span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">global =</span> <span class="cn" style="color: #8f5902;
background-color: null;
font-style: inherit;">TRUE</span>)</span>
<span id="cb1-9"></span>
<span id="cb1-10">y <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">llply</span>(<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">:</span><span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">100</span>, <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">function</span>(x) {</span>
<span id="cb1-11">  <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">Sys.sleep</span>(<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span>)</span>
<span id="cb1-12">  <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">sqrt</span>(x)</span>
<span id="cb1-13">}, <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">.progress =</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"progressr"</span>, <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">.parallel =</span> <span class="cn" style="color: #8f5902;
background-color: null;
font-style: inherit;">TRUE</span>)</span>
<span id="cb1-14"></span>
<span id="cb1-15"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">#&gt;   |============                                  |  28%</span></span></code></pre></div>
<p>Previously, <strong>plyr</strong> only had built-in support for progress reporting when running sequentially. Note that the <strong>progressr</strong> is the only package that supports progress reporting when using <code>.parallel = TRUE</code> in <strong>plyr</strong>.</p>
<p>Also, whenever using <strong>progressr</strong>, the user has plenty of options for where and how progress is reported. For example, <code>handlers("rstudio")</code> uses the progress bar in the RStudio job interface, <code>handlers("progress")</code> uses terminal progress bars of the <strong>progress</strong> package, and <code>handlers("beep")</code> reports on progress using sounds. It’s also possible to report progress in the Shiny. See my blog post <a href="../../2021/06/11/progressr-0.8.0/">‘progressr 0.8.0 - RStudio’s Progress Bar, Shiny Progress Updates, and Absolute Progress’</a> for more information.</p>
</section>
<section id="theres-actually-a-better-way" class="level2">
<h2 class="anchored" data-anchor-id="theres-actually-a-better-way">There’s actually a better way</h2>
<p>I actually recommend another way for reporting on progress with <strong>plyr</strong> map-reduce functions, which is more in line with the design philosophy of <strong>progressr</strong>:</p>
<blockquote class="blockquote">
<p>The developer is responsible for providing progress updates, but it’s only the end user who decides if, when, and how progress should be presented. No exceptions will be allowed.</p>
</blockquote>
<p>Please see Section ‘plyr::llply(…, .parallel = TRUE) with doFuture’ in the <a href="https://progressr.futureverse.org/articles/progressr-intro.html">‘progressr: An Introduction’</a> vignette for this alternative approach, which has worked for long time already. But, of course, adding <code>.progress = "progressr"</code> to your already existing <strong>plyr</strong> <code>.parallel = TRUE</code> code is as simple as it gets.</p>
<p>Now, make some progress!</p>
</section>
<section id="other-posts-on-progress-reporting" class="level2">
<h2 class="anchored" data-anchor-id="other-posts-on-progress-reporting">Other posts on progress reporting</h2>
<ul>
<li><a href="../../2021/06/11/progressr-0.8.0/">progressr 0.8.0 - RStudio’s Progress Bar, Shiny Progress Updates, and Absolute Progress</a>, 2021-06-11</li>
<li><a href="../../2020/07/04/progressr-erum2020-slides/">e-Rum 2020 Slides on Progressr</a>, 2020-07-04</li>
<li>See also <a href="../../tags/#progressr-list">‘progressr’</a> tag.</li>
</ul>
</section>
<section id="links" class="level2">
<h2 class="anchored" data-anchor-id="links">Links</h2>
<ul>
<li><strong>progressr</strong> package: <a href="https://cran.r-project.org/package=progressr">CRAN</a>, <a href="https://github.com/HenrikBengtsson/progressr">GitHub</a>, <a href="https://progressr.futureverse.org">pkgdown</a></li>
<li><strong>plyr</strong> package: <a href="https://cran.r-project.org/package=plyr">CRAN</a>, <a href="https://github.com/hadley/plyr">GitHub</a>, <a href="http://plyr.had.co.nz/">pkgdown-ish</a></li>
<li><strong>future</strong> package: <a href="https://cran.r-project.org/package=future">CRAN</a>, <a href="https://github.com/HenrikBengtsson/future">GitHub</a>, <a href="https://future.futureverse.org">pkgdown</a></li>
</ul>


</section>

 ]]></description>
  <category>R</category>
  <category>package</category>
  <category>parallel</category>
  <category>progressr</category>
  <category>plyr</category>
  <category>future</category>
  <category>parallel processing</category>
  <guid>https://www.jottr.org/posts/2022-06-03-progressr-plyr-parallel/</guid>
  <pubDate>Fri, 03 Jun 2022 20:00:00 GMT</pubDate>
</item>
<item>
  <title>parallelly 1.31.1: Better at Inferring Number of CPU Cores with Cgroups and Linux Containers</title>
  <dc:creator>Henrik Bengtsson</dc:creator>
  <link>https://www.jottr.org/posts/2022-04-22-parallelly_1.31.1/</link>
  <description><![CDATA[ 






<div style="padding: 2ex; float: right;">
<center>
<img src="https://www.jottr.org/posts/2022-04-22-parallelly_1.31.1/resources/parallelly-logo.png" alt="The 'parallelly' hexlogo">
</center>
</div>
<p><strong><a href="https://parallelly.futureverse.org">parallelly</a></strong> 1.31.1 is on CRAN. The <strong>parallelly</strong> package enhances the <strong>parallel</strong> package - our built-in R package for parallel processing - by improving on existing features and by adding new ones. Somewhat simplified, <strong>parallelly</strong> provides the things that you would otherwise expect to find in the <strong>parallel</strong> package. The <strong><a href="https://future.futureverse.org">future</a></strong> package relies on the <strong>parallelly</strong> package internally for local and remote parallelization.</p>
<p>Since my <a href="../../2021/11/22/parallelly-1.29.0/">previous post on <strong>parallelly</strong></a> in November 2021, I’ve fixed a few bugs and added some new features to the package;</p>
<ul>
<li><p><code>availableCores()</code> detects more cgroups settings, e.g.&nbsp;it now detects the number of CPUs available to your RStudio Cloud session</p></li>
<li><p><code>makeClusterPSOCK()</code> gained argument <code>default_packages</code> to control which packages to attach at startup on the R workers</p></li>
<li><p><code>makeClusterPSOCK()</code> gained <code>rscript_sh</code> to explicitly control what type of shell quotes to use on the R workers</p></li>
</ul>
<p>Below is a detailed description of these new features. Some of them, and some of the bug fixes, were added to version 1.30.0, while others to versions 1.31.0 and 1.31.1.</p>
<section id="availablecores-detects-more-cgroups-settings" class="level2">
<h2 class="anchored" data-anchor-id="availablecores-detects-more-cgroups-settings">availableCores() detects more cgroups settings</h2>
<p><em><a href="https://www.wikipedia.org/wiki/Cgroups">Cgroups</a></em>, short for control groups, is a low-level feature in Linux to control which and how much resources a process may use. This prevents individual processes from taking up all resources. For example, an R process can be limited to use at most four CPU cores, even if the underlying hardware has 48 CPU cores. Imagine we parallelize with <code>parallel::detectCores()</code> background workers, e.g.</p>
<div class="sourceCode" id="cb1" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb1-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">library</span>(future)</span>
<span id="cb1-2"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">plan</span>(multisession, <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">workers =</span> parallel<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">detectCores</span>())</span></code></pre></div>
<p>This will spawn 48 background R processes. Without cgroups, these 48 parallel R workers will run across all 48 CPU cores on the machine, competing with all other software and all other users running on the same machine. With cgroups limiting us to, say, four CPU cores, there will still be 48 parallel R workers running, but they will now run isolated on only four CPU cores, leaving the other 44 CPU cores alone.</p>
<p>Of course, running 48 parallel workers on four CPU cores is not very efficient. There will be a lot of wasted CPU cycles due to context switching. The problem is that we use <code>parallel::detectCores()</code> here, which is what gives us 48 workers. If we instead use <a href="https://parallelly.futureverse.org/reference/availableCores.html"><code>availableCores()</code></a> of <strong>parallelly</strong>;</p>
<div class="sourceCode" id="cb2" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb2-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">library</span>(future)</span>
<span id="cb2-2"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">plan</span>(multisession, <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">workers =</span> parallelly<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">availableCores</span>())</span></code></pre></div>
<p>we get four parallel workers, which reflects the four CPU cores that cgroups gives us. Basic support for this was introduced in <strong>parallelly</strong> 1.22.0 (2020-12-12), by querying <code>nproc</code>. This required that <code>nproc</code> was installed on the system, and although it worked in many cases, it did not work for all cgroups configurations. Specifically, it would not work when cgroups was <em>throttling</em> the CPU usage rather than limiting the process to a specific set of CPU cores. To illustrate this, assume we run R via Docker using <a href="https://www.rocker-project.org/">Rocker</a>:</p>
<div class="sourceCode" id="cb3" style="background: #f1f3f5;"><pre class="sourceCode sh code-with-copy"><code class="sourceCode bash"><span id="cb3-1"><span class="ex" style="color: null;
background-color: null;
font-style: inherit;">$</span> docker run <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">--cpuset-cpus</span><span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span>0-2,8 rocker/r-base</span></code></pre></div>
<p>then cgroups will isolate the Linux container to run on CPU cores 0, 1, 2, and 8 of the host. In this case <code>nproc</code>, e.g.&nbsp;<code>system("nproc")</code> from within R, returns four (4), and therefore also <code>parallelly::availableCores()</code>. Starting with <strong>parallelly</strong> 1.31.0, <code>parallelly::availableCores()</code> detects this also when <code>nproc</code> is not installed on the system. An alternative to limit the CPU resources, is to throttle the average CPU load. Using Docker, this can be done as:</p>
<div class="sourceCode" id="cb4" style="background: #f1f3f5;"><pre class="sourceCode sh code-with-copy"><code class="sourceCode bash"><span id="cb4-1"><span class="ex" style="color: null;
background-color: null;
font-style: inherit;">$</span> docker run <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">--cpus</span><span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span>3.5 rocker/r-base</span></code></pre></div>
<p>In this case, cgroups will throttle our R process to consume at most 350% worth of CPU on the host, where 100% corresponds to a single CPU. Here, <code>nproc</code> is of no use and simply gives the number of CPUs on the host (e.g.&nbsp;48). Starting with <strong>parallelly</strong> 1.31.0, <code>parallelly::availableCores()</code> can detect that cgroups throttles R to an average load of 3.5 CPUs. Since we cannot run 3.5 parallel workers, <code>parallelly::availableCores()</code> rounds down to the nearest integer and return three (3). The <a href="https://rstudio.cloud/">RStudio Cloud</a> is one example where CPU throttling is used, so if you work in RStudio Cloud, use <code>parallelly::availableCores()</code> and you will be good.</p>
<p>While talking about RStudio Cloud, if you use a free account, you have access to only a single CPU core (“nCPUs = 1”). In this case, <code>plan(multisession, workers = parallelly::availableCores())</code>, or equivalently, <code>plan(multisession)</code>, will fall back to sequential processing, because there is no point in running in parallel on a single core. If you still want to <em>prototype</em> parallel processing in a single-core environment, say with two cores, you can set option <code>parallelly.availableCores.min = 2</code>. This makes <code>availableCores()</code> return two (2).</p>
</section>
<section id="makeclusterpsock-gained-more-skills" class="level2">
<h2 class="anchored" data-anchor-id="makeclusterpsock-gained-more-skills">makeClusterPSOCK() gained more skills</h2>
<p>Since <strong>parallelly</strong> 1.29.0, <a href="https://parallelly.futureverse.org/reference/makeClusterPSOCK.html"><code>makeClusterPSOCK()</code></a> has gained arguments <code>default_packages</code> and <code>rscript_sh</code>.</p>
<section id="new-argument-default_packages" class="level3">
<h3 class="anchored" data-anchor-id="new-argument-default_packages">New argument <code>default_packages</code></h3>
<p>Argument <code>default_packages</code> controls which R packages are attached on each worker during startup. Previously, it was only possible, via logical argument <code>methods</code> to control whether or not the <strong>methods</strong> package should be attached - an argument that stems from <code>parallel::makePSOCKcluster()</code>. With the new <code>default_packages</code> argument, we have full control of which packages are attached. For instance, if we want to go minimal, we can do:</p>
<div class="sourceCode" id="cb5" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb5-1">cl <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> parallelly<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">makeClusterPSOCK</span>(<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span>, <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">default_packages =</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"base"</span>)</span></code></pre></div>
<p>This will result in one R worker with only the <strong>base</strong> package <em>attached</em>;</p>
<div class="sourceCode" id="cb6" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb6-1"><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">&gt;</span> parallel<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">clusterEvalQ</span>(cl, { <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">search</span>() })</span>
<span id="cb6-2">[[<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span>]]</span>
<span id="cb6-3">[<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span>] <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">".GlobalEnv"</span>   <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"Autoloads"</span>    <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"package:base"</span></span></code></pre></div>
<p>Having said that, note that more packages are <em>loaded</em>;</p>
<div class="sourceCode" id="cb7" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb7-1"><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">&gt;</span> parallel<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">clusterEvalQ</span>(cl, { <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">loadedNamespaces</span>() })</span>
<span id="cb7-2">[[<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span>]]</span>
<span id="cb7-3">[<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span>] <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"compiler"</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"parallel"</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"utils"</span>    <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"base"</span>    </span></code></pre></div>
<p>Like <strong>base</strong>, <strong>compiler</strong> is a package that R always loads. The <strong>parallel</strong> package is loaded because it provides the code for running the background R workers. The <strong>utils</strong> package is loaded because <code>makeClusterPSOCK()</code> validates that the workers are functional by collecting extra information from the R workers that later may be useful when reporting on errors. To skip this, pass argument <code>validate = FALSE</code>.</p>
</section>
<section id="new-argument-rscript_sh" class="level3">
<h3 class="anchored" data-anchor-id="new-argument-rscript_sh">New argument <code>rscript_sh</code></h3>
<p>The new argument <code>rscript_sh</code> can be used in the rare case where one launches remote R workers on non-Unix machines from a Unix-like machine. For example, if we, from a Linux machine launch remote MS Windows workers, we need to use <code>rscript_sh = "cmd"</code>.</p>
<p>That covers the most important additions to <strong>parallelly</strong>. For bug fixes and minor updates, please see <a href="https://parallelly.futureverse.org/news/index.html">NEWS</a>.</p>
<p>Over and out!</p>
</section>
</section>
<section id="links" class="level2">
<h2 class="anchored" data-anchor-id="links">Links</h2>
<ul>
<li><strong>parallelly</strong> package: <a href="https://cran.r-project.org/package=parallelly">CRAN</a>, <a href="https://github.com/HenrikBengtsson/parallelly">GitHub</a>, <a href="https://parallelly.futureverse.org">pkgdown</a></li>
<li><strong>future</strong> package: <a href="https://cran.r-project.org/package=future">CRAN</a>, <a href="https://github.com/HenrikBengtsson/future">GitHub</a>, <a href="https://future.futureverse.org">pkgdown</a></li>
</ul>


</section>

 ]]></description>
  <category>R</category>
  <category>package</category>
  <category>future</category>
  <category>parallel</category>
  <category>parallelly</category>
  <category>HPC</category>
  <category>parallel processing</category>
  <category>cgroups</category>
  <category>Linux containers</category>
  <guid>https://www.jottr.org/posts/2022-04-22-parallelly_1.31.1/</guid>
  <pubDate>Fri, 22 Apr 2022 18:00:00 GMT</pubDate>
</item>
<item>
  <title>future 1.24.0: Forwarding RNG State also for Stand-Alone Futures</title>
  <dc:creator>Henrik Bengtsson</dc:creator>
  <link>https://www.jottr.org/posts/2022-02-22-future_1.24.0/</link>
  <description><![CDATA[ 






<p><strong><a href="https://future.futureverse.org">future</a></strong> 1.24.0 is on CRAN. It comes with one significant update related to random number generation, further deprecation of legacy future strategies, a slight improvement to <code>plan()</code> and <code>tweaks()</code>, and some bug fixes. Below are the most important changes.</p>
<figure style="padding: 2ex; float: right;" class="figure">
<center>
<img src="https://www.jottr.org/posts/2022-02-22-future_1.24.0/resources/xkcd_221-random_number.png" alt="A one-box XKCD comic with the following handwritten code:
  
  int getRandomNumber()
  {
    return 4;  // chosen by fair dice roll.
               // guaranteed to be random.
  }
  " class="figure-img">
</center>
<figcaption style="font-size: small; font-style: italic;">
One of many possible random number generators. This one was carefully designed by <a href="https://xkcd.com/221/">XKCD</a> [CC BY-NC 2.5].
</figcaption>
</figure>
<section id="future-seed-true-updates-rng-state" class="level2">
<h2 class="anchored" data-anchor-id="future-seed-true-updates-rng-state">future(…, seed = TRUE) updates RNG state</h2>
<p>In <strong>future</strong> (&lt; 1.24.0), using <a href="https://future.futureverse.org/reference/future.html"><code>future(..., seed = TRUE)</code></a> would <em>not</em> forward the state of the random number generator (RNG). For example, if we generated random numbers in individual futures this way, they would become <em>identical</em>, e.g.</p>
<div class="sourceCode" id="cb1" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb1-1">f <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">future</span>(<span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">rnorm</span>(<span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">n =</span> <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span>L), <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">seed =</span> <span class="cn" style="color: #8f5902;
background-color: null;
font-style: inherit;">TRUE</span>)</span>
<span id="cb1-2"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">value</span>(f)</span>
<span id="cb1-3"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">#&gt; [1] -1.424997</span></span>
<span id="cb1-4"></span>
<span id="cb1-5">f <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">future</span>(<span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">rnorm</span>(<span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">n =</span> <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span>L), <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">seed =</span> <span class="cn" style="color: #8f5902;
background-color: null;
font-style: inherit;">TRUE</span>)</span>
<span id="cb1-6"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">value</span>(f)</span>
<span id="cb1-7"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">#&gt; [1] -1.424997</span></span></code></pre></div>
<p>This was a deliberate, conservative design, because it is not obvious exactly how the RNG state should be forwarded in this case, especially if we consider random numbers may be generated also in the main R session. The more I dug into the problem, the further down I ended up in a rabbit hole. Because of this, I have held back on addressing this problem and leaving it to the developer to solve it, i.e.&nbsp;they had to roll their own RNG streams designed for parallel processing, and populate each future with a unique seed from those RNG streams, i.e.&nbsp;<code>future(..., seed = &lt;seed&gt;)</code>. This is how <strong><a href="https://future.apply.futureverse.org">future.apply</a></strong> and <strong><a href="https://furrr.futureverse.org">furrr</a></strong> already do it internally.</p>
<p>However, I understand that design was confusing, and if not understood, it could silently lead to RNG mistakes and correlated, and even identical random numbers. I also sometimes got confused about this when I needed to do something quickly with individual futures and random numbers. I even considered making <code>seed = TRUE</code> an error until resolved, and, looking back, maybe I should have done so.</p>
<p>Anyway, because it is rather tedious to roll your own L’Ecuyer-CMRG RNG streams, I decided to update <code>future(..., seed = TRUE)</code> to provide a good-enough solution internally, where it forwards the RNG state and then provides the future with an RNG substream based on the updated RNG state. In <strong>future</strong> (&gt;= 1.24.0), we now get:</p>
<div class="sourceCode" id="cb2" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb2-1">f <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">future</span>(<span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">rnorm</span>(<span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">n =</span> <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span>L), <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">seed =</span> <span class="cn" style="color: #8f5902;
background-color: null;
font-style: inherit;">TRUE</span>)</span>
<span id="cb2-2">v <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">value</span>(f)</span>
<span id="cb2-3"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">print</span>(v)</span>
<span id="cb2-4"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">#&gt; [1] -1.424997</span></span>
<span id="cb2-5"></span>
<span id="cb2-6">f <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">future</span>(<span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">rnorm</span>(<span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">n =</span> <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span>L), <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">seed =</span> <span class="cn" style="color: #8f5902;
background-color: null;
font-style: inherit;">TRUE</span>)</span>
<span id="cb2-7">v <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">value</span>(f)</span>
<span id="cb2-8"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">print</span>(v)</span>
<span id="cb2-9"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">#&gt; [1] -1.985136</span></span></code></pre></div>
<p>This update only affects code that currently uses <code>future(..., seed = TRUE)</code>. It does <em>not</em> affect code that relies on <strong>future.apply</strong> or <strong>furrr</strong>, which already worked correctly. That is, you can keep using <code>y &lt;- future_lapply(..., future.seed = TRUE)</code> and <code>y &lt;- future_map(..., .options = furrr_options(seed = TRUE))</code>.</p>
</section>
<section id="deprecating-future-strategies-transparent-and-remote" class="level2">
<h2 class="anchored" data-anchor-id="deprecating-future-strategies-transparent-and-remote">Deprecating future strategies ‘transparent’ and ‘remote’</h2>
<p>It’s on the <a href="https://futureverse.org/roadmap.html">roadmap</a> to provide mechanisms for the developer to declare what resources a particular future needs and for the end-user to specify multiple parallel-backend alternatives, so that the future can be processed on a worker that best can meet its resource requirements. In order to support this, we need to restrict the future backend API further, which has been in the works over the last couple of years in collaboration with existing package developers.</p>
<p>In this release, I am formally deprecating future strategies <code>transparent</code> and <code>remote</code>. When used, they now produce an informative warning. The <code>transparent</code> strategy is deprecated in favor of using <code>sequential</code> with argument <code>split = TRUE</code> set. If you still use <code>remote</code>, please migrate to <code>cluster</code>, which since a long time can achieve everything that <code>remote</code> can do.</p>
<p>On a related note, if you are still using <code>multiprocess</code>, which is deprecated in <strong>future</strong> (&gt;= 1.20.0) since 2020-11-03, please migrate to <code>multisession</code> so you won’t get surprised when <code>multiprocess</code> becomes defunct.</p>
<p>For the other updates, please see the <a href="https://future.futureverse.org/news/index.html">NEWS</a>.</p>
<p>Happy futuring!</p>
<p>Henrik</p>
</section>
<section id="other-posts-on-random-numbers-in-parallel-processing" class="level2">
<h2 class="anchored" data-anchor-id="other-posts-on-random-numbers-in-parallel-processing">Other posts on random numbers in parallel processing</h2>
<ul>
<li><p><a href="../../2020/09/22/push-for-statistical-sound-rng/">future 1.19.1 - Making Sure Proper Random Numbers are Produced in Parallel Processing</a>, 2020-09-22</p></li>
<li><p><a href="../../2020/09/21/detect-when-the-random-number-generator-was-used/">Detect When the Random Number Generator Was Used</a>, 2020-09-21</p></li>
<li><p><a href="../../2017/02/19/future-rng/">future 1.3.0: Reproducible RNGs, future_lapply() and More</a>, 2017-02-19</p></li>
</ul>
</section>
<section id="links" class="level2">
<h2 class="anchored" data-anchor-id="links">Links</h2>
<ul>
<li><strong>future</strong> package: <a href="https://cran.r-project.org/package=future">CRAN</a>, <a href="https://github.com/HenrikBengtsson/future">GitHub</a>, <a href="https://future.futureverse.org">pkgdown</a></li>
<li><strong>future.apply</strong> package: <a href="https://cran.r-project.org/package=future.apply">CRAN</a>, <a href="https://github.com/HenrikBengtsson/future.apply">GitHub</a>, <a href="https://future.apply.futureverse.org">pkgdown</a></li>
<li><strong>furrr</strong> package: <a href="https://cran.r-project.org/package=furrr">CRAN</a>, <a href="https://github.com/HenrikBengtsson/furrr">GitHub</a>, <a href="https://furrr.futureverse.org">pkgdown</a></li>
</ul>


</section>

 ]]></description>
  <category>R</category>
  <category>package</category>
  <category>future</category>
  <category>parallel</category>
  <category>parallel processing</category>
  <category>RNG</category>
  <guid>https://www.jottr.org/posts/2022-02-22-future_1.24.0/</guid>
  <pubDate>Tue, 22 Feb 2022 21:00:00 GMT</pubDate>
</item>
<item>
  <title>Future Improvements During 2021</title>
  <dc:creator>Henrik Bengtsson</dc:creator>
  <link>https://www.jottr.org/posts/2022-01-07-future-during-2021/</link>
  <description><![CDATA[ 






<p><img src="https://www.jottr.org/posts/2022-01-07-future-during-2021/resources/paragliding_mount_tamalpais_20220101.jpg" alt="First person view while paragliding during a sunny day with blue skies. The pilot's left hand with a glove can be seen pulling the left break with lines going up to the white, left wing tip above. The pilot is in a left turn high above the mountain side with open patched of grass among the tree. Two other paragliders further down can be seen in the distance. Down below, to the left, there is a long ocean beach slowly curving up towards a point in the horizon. Inside the beach, there is a lagoon. Part of the mountain ridge can be seen to the right." style="width: 100%; margin: 0; margin-bottom: 2ex;"></p>
<p>Happy New Year! I made some updates to the future framework during 2021 that involve overall improvements and essential preparations to go forward with some exciting new features that I’m keen to work on during 2022.</p>
<p>The <a href="https://futureverse.org">future framework</a> makes it easy to parallelize existing R code - often with only a minor change of code. The goal is to lower the barriers so that anyone can quickly and safely speed up their existing R code in a worry-free manner.</p>
<p><strong><a href="https://future.futureverse.org">future</a></strong> 1.22.1 was released in August 2021, followed by <strong>future</strong> 1.23.0 at the end of October 2021. Below, I summarize the updates that came with those two releases:</p>
<ul>
<li>New features</li>
<li>Performance improvements</li>
<li>Cleanups to make room for new features</li>
<li>Significant changes preparing for the future</li>
<li>Roadmap ahead</li>
</ul>
<p>There were also several updates to the related <strong><a href="https://parallelly.futureverse.org">parallelly</a></strong> and <strong><a href="https://progressr.futureverse.org">progressr</a></strong> packages, which you can read about in earlier blog posts under the <a href="../../tags/#parallelly-list">#parallelly</a> and <a href="../../tags/#progressr-list">#progressr</a> blog tags.</p>
<section id="new-features" class="level2">
<h2 class="anchored" data-anchor-id="new-features">New features</h2>
<section id="futuresessioninfo-for-troubleshooting-and-issue-reporting" class="level3">
<h3 class="anchored" data-anchor-id="futuresessioninfo-for-troubleshooting-and-issue-reporting">futureSessionInfo() for troubleshooting and issue reporting</h3>
<p>Function <a href="https://future.futureverse.org/reference/futureSessionInfo.html"><code>futureSessionInfo()</code></a> was added to <strong>future</strong> 1.22.0. It outputs information useful for troubleshooting problems related to the future framework. It also runs some basic tests to validate that the current future backend works as expected. If you have problems getting futures to work on your machine, please run this function before reporting issues at <a href="https://github.com/HenrikBengtsson/future/discussions">Future Discussions</a>. Here’s an example:</p>
<div class="sourceCode" id="cb1" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb1-1"><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">&gt;</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">library</span>(future)</span>
<span id="cb1-2"><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">&gt;</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">plan</span>(multisession, <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">workers =</span> <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">2</span>)</span>
<span id="cb1-3"><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">&gt;</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">futureSessionInfo</span>()</span>
<span id="cb1-4"><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">**</span><span class="er" style="color: #AD0000;
background-color: null;
font-style: inherit;">*</span> Package versions</span>
<span id="cb1-5">future <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span>.<span class="fl" style="color: #AD0000;
background-color: null;
font-style: inherit;">23.0</span>, parallelly <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span>.<span class="fl" style="color: #AD0000;
background-color: null;
font-style: inherit;">30.0</span>, parallel <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">4</span>.<span class="fl" style="color: #AD0000;
background-color: null;
font-style: inherit;">1.2</span>, globals <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">0</span>.<span class="fl" style="color: #AD0000;
background-color: null;
font-style: inherit;">14.0</span>, listenv <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">0</span>.<span class="fl" style="color: #AD0000;
background-color: null;
font-style: inherit;">8.0</span></span>
<span id="cb1-6"></span>
<span id="cb1-7"><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">**</span><span class="er" style="color: #AD0000;
background-color: null;
font-style: inherit;">*</span> Allocations</span>
<span id="cb1-8"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">availableCores</span>()<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">:</span></span>
<span id="cb1-9">system  nproc </span>
<span id="cb1-10">     <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">8</span>      <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">8</span> </span>
<span id="cb1-11"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">availableWorkers</span>()<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">:</span></span>
<span id="cb1-12"><span class="er" style="color: #AD0000;
background-color: null;
font-style: inherit;">$</span>system</span>
<span id="cb1-13">[<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span>] <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"localhost"</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"localhost"</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"localhost"</span></span>
<span id="cb1-14">[<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">4</span>] <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"localhost"</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"localhost"</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"localhost"</span></span>
<span id="cb1-15">[<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">7</span>] <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"localhost"</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"localhost"</span></span>
<span id="cb1-16"></span>
<span id="cb1-17"><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">**</span><span class="er" style="color: #AD0000;
background-color: null;
font-style: inherit;">*</span> Settings</span>
<span id="cb1-18"><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span> future.plan<span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">=</span><span class="er" style="color: #AD0000;
background-color: null;
font-style: inherit;">&lt;</span>not set<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">&gt;</span></span>
<span id="cb1-19"><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span> future.fork.multithreading.enable<span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">=</span><span class="er" style="color: #AD0000;
background-color: null;
font-style: inherit;">&lt;</span>not set<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">&gt;</span></span>
<span id="cb1-20"><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span> future.globals.maxSize<span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">=</span><span class="er" style="color: #AD0000;
background-color: null;
font-style: inherit;">&lt;</span>not set<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">&gt;</span></span>
<span id="cb1-21"><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span> future.globals.onReference<span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">=</span><span class="er" style="color: #AD0000;
background-color: null;
font-style: inherit;">&lt;</span>not set<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">&gt;</span></span>
<span id="cb1-22"><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span> future.resolve.recursive<span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">=</span><span class="er" style="color: #AD0000;
background-color: null;
font-style: inherit;">&lt;</span>not set<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">&gt;</span></span>
<span id="cb1-23"><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span> future.rng.onMisuse<span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">=</span><span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">'warning'</span></span>
<span id="cb1-24"><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span> future.wait.timeout<span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">=</span><span class="er" style="color: #AD0000;
background-color: null;
font-style: inherit;">&lt;</span>not set<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">&gt;</span></span>
<span id="cb1-25"><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span> future.wait.interval<span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">=</span><span class="er" style="color: #AD0000;
background-color: null;
font-style: inherit;">&lt;</span>not set<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">&gt;</span></span>
<span id="cb1-26"><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span> future.wait.alpha<span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">=</span><span class="er" style="color: #AD0000;
background-color: null;
font-style: inherit;">&lt;</span>not set<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">&gt;</span></span>
<span id="cb1-27"><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span> future.startup.script<span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">=</span><span class="er" style="color: #AD0000;
background-color: null;
font-style: inherit;">&lt;</span>not set<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">&gt;</span></span>
<span id="cb1-28"></span>
<span id="cb1-29"><span class="er" style="color: #AD0000;
background-color: null;
font-style: inherit;">***</span> Backends</span>
<span id="cb1-30">Number of workers<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">:</span> <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">2</span></span>
<span id="cb1-31">List of future strategies<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">:</span></span>
<span id="cb1-32"><span class="fl" style="color: #AD0000;
background-color: null;
font-style: inherit;">1.</span> multisession<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">:</span></span>
<span id="cb1-33">   <span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span> args<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">:</span> <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">function</span> (..., <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">workers =</span> <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">2</span>, <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">envir =</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">parent.frame</span>())</span>
<span id="cb1-34">   <span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span> tweaked<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">:</span> <span class="cn" style="color: #8f5902;
background-color: null;
font-style: inherit;">TRUE</span></span>
<span id="cb1-35">   <span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span> call<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">:</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">plan</span>(multisession, <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">workers =</span> <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">2</span>)</span>
<span id="cb1-36">   </span>
<span id="cb1-37"><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">**</span><span class="er" style="color: #AD0000;
background-color: null;
font-style: inherit;">*</span> Basic tests</span>
<span id="cb1-38">  worker   pid     r sysname          release</span>
<span id="cb1-39"><span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span>      <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span> <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">19291</span> <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">4</span>.<span class="fl" style="color: #AD0000;
background-color: null;
font-style: inherit;">1.2</span>   Linux <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">5</span>.<span class="fl" style="color: #AD0000;
background-color: null;
font-style: inherit;">4.0</span><span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">-91</span><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span>generic</span>
<span id="cb1-40"><span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">2</span>      <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">2</span> <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">19290</span> <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">4</span>.<span class="fl" style="color: #AD0000;
background-color: null;
font-style: inherit;">1.2</span>   Linux <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">5</span>.<span class="fl" style="color: #AD0000;
background-color: null;
font-style: inherit;">4.0</span><span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">-91</span><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span>generic</span>
<span id="cb1-41">                                               version</span>
<span id="cb1-42"><span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span> <span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">#102~18.04.1-Ubuntu SMP Thu Nov 11 14:46:36 UTC 2021</span></span>
<span id="cb1-43"><span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">2</span> <span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">#102~18.04.1-Ubuntu SMP Thu Nov 11 14:46:36 UTC 2021</span></span>
<span id="cb1-44">   nodename machine login  user effective_user</span>
<span id="cb1-45"><span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span> my<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span>laptop  x86_64 alice alice          alice</span>
<span id="cb1-46"><span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">2</span> my<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span>laptop  x86_64 alice alice          alice</span>
<span id="cb1-47">Number of unique PIDs<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">:</span> <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">2</span> (as expected)</span></code></pre></div>
</section>
<section id="working-around-utf-8-escaping-on-ms-windows" class="level3">
<h3 class="anchored" data-anchor-id="working-around-utf-8-escaping-on-ms-windows">Working around UTF-8 escaping on MS Windows</h3>
<p>Because of limitations in R itself, UTF-8 symbols outputted on MS Windows parallel workers would be <a href="https://github.com/HenrikBengtsson/future/issues/473">relayed as escaped symbols</a> when using futures. Now, the future framework, and, more specifically, <a href="https://future.futureverse.org/reference/value.html"><code>value()</code></a>, attempts to recover such MS Windows output to UTF-8 before outputting it.</p>
<p>For example, in <strong>future</strong> (&lt; 1.23.0) you would get the following:</p>
<div class="sourceCode" id="cb2" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb2-1">f <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">future</span>({ <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">cat</span>(<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"\u2713 Everything is OK"</span>) ; <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">42</span> })</span>
<span id="cb2-2">v <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">value</span>(f)</span>
<span id="cb2-3"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">#&gt; &lt;U+2713&gt; Everything is OK</span></span></code></pre></div>
<p>when, and only when, those futures are resolved on a MS Windows machine. In <strong>future</strong> (&gt;= 1.23.0), we work around this problem by looking for <code>&lt;U+NNNN&gt;</code> like patterns in the output and decode them as UTF-8 symbols;</p>
<div class="sourceCode" id="cb3" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb3-1">f <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">future</span>({ <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">cat</span>(<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"\u2713 Everything is OK"</span>) ; <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">42</span> })</span>
<span id="cb3-2">v <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">value</span>(f)</span>
<span id="cb3-3"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">#&gt; ✓ Everything is OK</span></span></code></pre></div>
<p><em>Comment</em>: From <a href="https://developer.r-project.org/Blog/public/2021/12/07/upcoming-changes-in-r-4.2-on-windows/index.html">R 4.2.0, R will have native support for UTF-8 also on MS Windows</a>. More testing and validation is needed to confirm this will work out of the box in R (&gt;= 4.2.0) when running R in the terminal, in the R GUI, in the RStudio Console, and so on. If so, <strong>future</strong> will be updated to only apply this workaround for R (&lt; 4.2.0).</p>
</section>
<section id="harmonization-of-future-futureassign-and-futurecall" class="level3">
<h3 class="anchored" data-anchor-id="harmonization-of-future-futureassign-and-futurecall">Harmonization of future(), futureAssign(), and futureCall()</h3>
<p>Prior to <strong>future</strong> 1.22.0, argument <code>seed</code> for <a href="https://future.futureverse.org/reference/future.html"><code>futureAssign()</code></a> and <a href="https://future.futureverse.org/reference/future.html"><code>futureCall()</code></a> defaulted to <code>TRUE</code>, whereas it defaulted to <code>FALSE</code> for <a href="https://future.futureverse.org/reference/future.html"><code>future()</code></a>. This was an oversight. In <strong>future</strong> (&gt;= 1.22.0), <code>seed = FALSE</code> is the default for all these functions.</p>
</section>
<section id="protecting-against-non-exportable-results" class="level3">
<h3 class="anchored" data-anchor-id="protecting-against-non-exportable-results">Protecting against non-exportable results</h3>
<p>Analogously to how globals may be scanned for <a href="https://future.futureverse.org/articles/future-4-non-exportable-objects.html">“non-exportable” objects</a> when option <code>future.globals.onReference</code> is set to <code>"error"</code> or <code>"warning"</code>, <code>value()</code> will now check for similar problems in the value returned from parallel workers. For example, in <strong>future</strong> (&lt; 1.23.0) we would get:</p>
<div class="sourceCode" id="cb4" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb4-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">library</span>(future)</span>
<span id="cb4-2"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">plan</span>(multisession, <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">workers =</span> <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">2</span>)</span>
<span id="cb4-3"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">options</span>(<span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">future.globals.onReference =</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"error"</span>)</span>
<span id="cb4-4"></span>
<span id="cb4-5">f <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">future</span>(xml2<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">read_xml</span>(<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"&lt;body&gt;&lt;/body&gt;"</span>))</span>
<span id="cb4-6">v <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">value</span>(f)</span>
<span id="cb4-7"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">print</span>(v)</span>
<span id="cb4-8"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">#&gt; Error in doc_type(x) : external pointer is not valid</span></span></code></pre></div>
<p>whereas in <strong>future</strong> (&gt;= 1.23.0) we get:</p>
<div class="sourceCode" id="cb5" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb5-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">library</span>(future)</span>
<span id="cb5-2"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">plan</span>(multisession, <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">workers =</span> <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">2</span>)</span>
<span id="cb5-3"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">options</span>(<span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">future.globals.onReference =</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"error"</span>)</span>
<span id="cb5-4"></span>
<span id="cb5-5">f <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">future</span>(xml2<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">read_xml</span>(<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"&lt;body&gt;&lt;/body&gt;"</span>))</span>
<span id="cb5-6">v <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">value</span>(f)</span>
<span id="cb5-7"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">#&gt; Error: Detected a non-exportable reference ('externalptr') in the value</span></span>
<span id="cb5-8"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">#&gt; (of class 'xml_document') of the resolved future</span></span></code></pre></div>
</section>
<section id="finer-control-of-what-type-of-conditions-are-captured-and-replayed" class="level3">
<h3 class="anchored" data-anchor-id="finer-control-of-what-type-of-conditions-are-captured-and-replayed">Finer control of what type of conditions are captured and replayed</h3>
<p>Besides specifying which condition classes to be captured and relayed, in <strong>future</strong> (&gt;= 1.22.0), it is possible to specify also condition classes to be ignored. For example,</p>
<div class="sourceCode" id="cb6" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb6-1">f <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">future</span>(..., <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">conditions =</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">structure</span>(<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"condition"</span>, <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">exclude =</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"message"</span>))</span></code></pre></div>
<p>captures all conditions but message conditions. The default is <code>conditions = "condition"</code>, which captures and relays any type of condition.</p>
</section>
</section>
<section id="performance-improvements" class="level2">
<h2 class="anchored" data-anchor-id="performance-improvements">Performance improvements</h2>
<p>I always prioritize correctness over performance in the <strong>future</strong> framework. So, whenever optimizing for performance, one always has to make sure we are not breaking things somewhere else. Thankfully, there are now <a href="https://www.futureverse.org/statistics.html">over 200 reverse-dependency packages on CRAN</a> and Bioconductor that I can validate against. They provide another comfy cushion against mistakes than what we already get from package unit tests and the <strong><a href="https://future.tests.futureverse.org">future.tests</a></strong> test suite. Below are some recent performance improvements made.</p>
<section id="less-latency-for-multicore-multisession-and-cluster-futures" class="level3">
<h3 class="anchored" data-anchor-id="less-latency-for-multicore-multisession-and-cluster-futures">Less latency for multicore, multisession, and cluster futures</h3>
<p>In <strong>future</strong> 1.22.0, the default timeout of <a href="https://future.futureverse.org/reference/resolved.html"><code>resolved()</code></a> was decreased from 0.20 seconds to 0.01 seconds for multicore, multisession, and cluster futures. This means that less time is now spent on checking for results from these future backends when they are not yet available. After making sure it is safe to do so, we might decrease the default timeout to zero in a later release.</p>
</section>
<section id="less-overhead-when-initiating-futures" class="level3">
<h3 class="anchored" data-anchor-id="less-overhead-when-initiating-futures">Less overhead when initiating futures</h3>
<p>The overhead of initiating futures was significantly reduced in <strong>future</strong> 1.22.0. For example, the round-trip time for <code>value(future(NULL))</code> is about twice as fast for sequential, cluster, and multisession futures. For multicore futures the round-trip speedup is about 20%.</p>
<p>The speedup comes from pre-compiling the future’s R expression into an R expression template, which then can quickly re-compiled into the final expression to be evaluated. Specifically, instead of calling <code>expr &lt;- base::bquote(tmpl)</code> for each future, which is computationally expensive, we take a two-step approach where we first call <code>tmpl_cmp &lt;- bquote_compile(tmpl)</code> once per session such that we only have to call the much faster <code>expr &lt;- bquote_apply(tmpl_cmp)</code> for each future.(*) This new pre-compile approach speeds up the construction of the final future expression from the original future expression ~10 times.</p>
<p>(*) These are <a href="https://github.com/HenrikBengtsson/future/blob/1064c4ec2c37a70fa8fff8887d0030a5f03c46da/R/000.bquote.R#L56-L131">internal functions</a> of the <strong>future</strong> package.</p>
</section>
<section id="environment-variables-are-only-used-when-package-is-loaded" class="level3">
<h3 class="anchored" data-anchor-id="environment-variables-are-only-used-when-package-is-loaded">Environment variables are only used when package is loaded</h3>
<p>All R <a href="https://future.futureverse.org/reference/future.options.html">options specific to the future framework</a> have defaults that fall back to corresponding environment variables. For example, the default for option <code>future.rng.onMisuse</code> can be set by environment variable <code>R_FUTURE_RNG_ONMISUSE</code>.</p>
<p>The purpose of the environment variables is to make it possible to configure the future framework before launching R, e.g.&nbsp;in shell startup scripts, or in shell scripts submitted to job schedulers in high-performance compute (HPC) environments. When R is already running, the best practice is to use the R options to configure the future framework.</p>
<p>In order to avoid the overhead from querying and parsing environment variables at runtime, but also to clarify how and when environment variables should be set, starting with <strong>future</strong> 1.22.0, <em><code>R_FUTURE_*</code> environment variables are only used when the <strong>future</strong> package is loaded</em>. Then, if set, they are used for setting the corresponding <code>future.*</code> option.</p>
</section>
</section>
<section id="cleanups-to-make-room-for-new-features" class="level2">
<h2 class="anchored" data-anchor-id="cleanups-to-make-room-for-new-features">Cleanups to make room for new features</h2>
<p>The <code>values()</code> function is defunct since <strong>future</strong> 1.23.0 in favor of <code>value()</code>. All CRAN and Bioconductor packages that depend on <strong>future</strong> have been updated since a long time. If you get the error:</p>
<div class="sourceCode" id="cb7" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb7-1">Error<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">:</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">values</span>() is defunct <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">in</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">future</span> (<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">&gt;=</span> <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span>.<span class="fl" style="color: #AD0000;
background-color: null;
font-style: inherit;">20.0</span>). Use <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">value</span>() instead.</span></code></pre></div>
<p>make sure to update your R packages. A few users of <strong><a href="https://furrr.futureverse.org">furrr</a></strong> have run into this error - updating to <strong>furrr</strong> (&gt;= 0.2.0) solved the problem.</p>
<p>Continuing, to further harmonize how developers use the Future API, we are moving away from odds-and-ends features, especially the ones that are holding us back from adding new features. The goal is to ensure that more code using futures can truly run anywhere, not just on a particular parallel backend that the developer work with.</p>
<p>In this spirit, we are slowly moving away from “persistent” workers. For example, in <strong>future</strong> (&gt;= 1.23.0), <code>plan(multisession, persistent = TRUE)</code> is no longer supported and will produce an error if attempted. The same will eventually happen also for <code>plan(cluster, persistent = TRUE)</code>, but not until we have <a href="https://www.futureverse.org/roadmap.html">support for caching “sticky” globals</a>, which is the main use case for persistent workers.</p>
<p>Another example is transparent futures, which are prepared for deprecation in <strong>future</strong> (&gt;= 1.23.0). If used, <code>plan(transparent)</code> produces a warning, which soon will be upgraded to a formal deprecation warning. In a later release, it will produce an error. Transparent futures were added during the early days in order to simplify troubleshooting of futures. A better approach these days is to use <code>plan(sequential, split = TRUE)</code>, which makes interactive troubleshooting tools such as <code>browser()</code> and <code>debug()</code> to work.</p>
</section>
<section id="significant-changes-preparing-for-the-future" class="level2">
<h2 class="anchored" data-anchor-id="significant-changes-preparing-for-the-future">Significant changes preparing for the future</h2>
<p>Prior to <strong>future</strong> 1.22.0, lazy futures were assigned to the currently set future backend immediately when created. For example, if we do:</p>
<div class="sourceCode" id="cb8" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb8-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">library</span>(future)</span>
<span id="cb8-2"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">plan</span>(multisession, <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">workers =</span> <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">2</span>)</span>
<span id="cb8-3"></span>
<span id="cb8-4">f <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">future</span>(<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">42</span>, <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">lazy =</span> <span class="cn" style="color: #8f5902;
background-color: null;
font-style: inherit;">TRUE</span>)</span></code></pre></div>
<p>with <strong>future</strong> (&lt; 1.22.0), we would get:</p>
<div class="sourceCode" id="cb9" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb9-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">class</span>(f)</span>
<span id="cb9-2"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">#&gt; [1] "MultisessionFuture" "ClusterFuture"      "MultiprocessFuture"</span></span>
<span id="cb9-3"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">#&gt; [4] "Future"             "environment" </span></span></code></pre></div>
<p>Starting with <strong>future</strong> 1.22.0, lazy futures remain generic futures until they are launched, which means they are not assigned a backend class until they have to. Now, the above example gives:</p>
<div class="sourceCode" id="cb10" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb10-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">class</span>(f)</span>
<span id="cb10-2"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">#&gt; [1] "Future"      "environment"</span></span></code></pre></div>
<p>This change opens up the door for storing futures themselves to file and sending them elsewhere. More precisely, this means we can start working towards a <em>queue of futures</em>, which then can be processed on whatever compute resources we have access to at the moment, e.g.&nbsp;some futures might be resolved on the local computer, others on machines on a local cluster, and when those fill up, we can burst out to cloud resources, or maybe process them via a community-driven peer-to-peer cluster.</p>
</section>
<section id="roadmap-ahead" class="level2">
<h2 class="anchored" data-anchor-id="roadmap-ahead">Roadmap ahead</h2>
<p>There are lots of new features on the roadmap related to the above and other things. I hope to make progress on several of them during 2022. If you’re curious about what’s coming up, see the <a href="https://www.futureverse.org/roadmap.html">Project Roadmap</a>, stay tuned on this blog (<a href="https://www.jottr.org/index.xml">feed</a>), or follow <a href="https://twitter.com/henrikbengtsson/">me on Twitter</a>.</p>
<p>Happy futuring!</p>
<p>Henrik</p>
</section>
<section id="links" class="level2">
<h2 class="anchored" data-anchor-id="links">Links</h2>
<ul>
<li><strong>future</strong> package: <a href="https://cran.r-project.org/package=future">CRAN</a>, <a href="https://github.com/HenrikBengtsson/future">GitHub</a>, <a href="https://future.futureverse.org">pkgdown</a></li>
<li><strong>parallelly</strong> package: <a href="https://cran.r-project.org/package=parallelly">CRAN</a>, <a href="https://github.com/HenrikBengtsson/parallelly">GitHub</a>, <a href="https://parallelly.futureverse.org">pkgdown</a></li>
</ul>


</section>

 ]]></description>
  <category>R</category>
  <category>package</category>
  <category>future</category>
  <category>parallel</category>
  <category>HPC</category>
  <category>parallel processing</category>
  <guid>https://www.jottr.org/posts/2022-01-07-future-during-2021/</guid>
  <pubDate>Fri, 07 Jan 2022 22:00:00 GMT</pubDate>
</item>
<item>
  <title>parallelly 1.29.0: New Skills and Less Communication Latency on Linux</title>
  <dc:creator>Henrik Bengtsson</dc:creator>
  <link>https://www.jottr.org/posts/2021-11-22-parallelly_1.29.0/</link>
  <description><![CDATA[ 






<div style="padding: 2ex; float: right;">
<center>
<img src="https://www.jottr.org/posts/2021-11-22-parallelly_1.29.0/resources/parallelly-logo.png" alt="The 'parallelly' hexlogo">
</center>
</div>
<p><strong><a href="https://parallelly.futureverse.org">parallelly</a></strong> 1.29.0 is on CRAN. The <strong>parallelly</strong> package enhances the <strong>parallel</strong> package - our built-in R package for parallel processing - by improving on existing features and by adding new ones. Somewhat simplified, <strong>parallelly</strong> provides the things that you would otherwise expect to find in the <strong>parallel</strong> package. The <strong><a href="https://future.futureverse.org">future</a></strong> package rely on the <strong>parallelly</strong> package internally for local and remote parallelization.</p>
<p>Since my <a href="../../2021/06/10/parallelly-1.26.0/">previous post on <strong>parallelly</strong></a> five months ago, the <strong>parallelly</strong> package had some bugs fixed, and it gained a few new features;</p>
<ul>
<li><p>new <code>isForkedChild()</code> to test if R runs in a forked process,</p></li>
<li><p>new <code>isNodeAlive()</code> to test if one or more cluster-node processes are running,</p></li>
<li><p><code>availableCores()</code> now respects also Bioconductor settings,</p></li>
<li><p><code>makeClusterPSOCK(..., rscript = "*")</code> automatically expands to the proper Rscript executable,</p></li>
<li><p><code>makeClusterPSOCK(…, rscript_envs = c(UNSET_ME = NA_character_))</code> unsets environment variables on cluster nodes, and</p></li>
<li><p><code>makeClusterPSOCK()</code> sets up clusters with less communication latency on Unix.</p></li>
</ul>
<p>Below is a detailed description of these new features.</p>
<section id="new-function-isforkedchild" class="level2">
<h2 class="anchored" data-anchor-id="new-function-isforkedchild">New function isForkedChild()</h2>
<p>If you run R on Unix and macOS, you can parallelize code using so called <em>forked</em> parallel processing. It is a very convenient way of parallelizing code, especially since forking is implemented at the core of the operating system and there is very little extra you have to do at the R level to get it to work. Compared with other parallelization solutions, forked processing has often less overhead, resulting in shorter turnaround times. To date, the most famous method for parallelizing using forks is <code>mclapply()</code> of the <strong>parallel</strong> package. For example,</p>
<div class="sourceCode" id="cb1" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb1-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">library</span>(parallel)</span>
<span id="cb1-2">y <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">mclapply</span>(X, some_slow_fcn, <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">mc.cores =</span> <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">4</span>)</span></code></pre></div>
<p>works just like <code>lapply(X, some_slow_fcn)</code> but will perform the same tasks in parallel using four (4) CPU cores. MS Windows does not support <a href="https://en.wikipedia.org/wiki/Fork_(system_call)">forked processing</a>; any attempt to use <code>mclapply()</code> there will cause it to silently fall back to a sequential <code>lapply()</code> call.</p>
<p>In the <strong>future</strong> ecosystem, you get forked parallelization with the <code>multicore</code> backend, e.g.</p>
<div class="sourceCode" id="cb2" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb2-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">library</span>(future.apply)</span>
<span id="cb2-2"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">plan</span>(multicore, <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">workers =</span> <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">4</span>)</span>
<span id="cb2-3">y <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">future_lapply</span>(X, some_slow_fcn)</span></code></pre></div>
<p>Unfortunately, we cannot parallelize all types of code using forks. If done, you might get an error, but in the worst case you crash (segmentation fault) your R process. For example, some graphical user interfaces (GUIs) do not play well with forked processing, e.g.&nbsp;the RStudio Console, but also other GUIs. Multi-threaded parallelization has also been reported to cause problems when run within forked parallelization. We sometime talk about <em>non-fork-safe code</em>, in contrast to <em>fork-safe</em> code, to refer to code that risks crashing the software if run in forked processes.</p>
<p>Here is what R-core developer Simon Urbanek and author of <code>mclapply()</code> wrote in the R-devel thread <a href="https://stat.ethz.ch/pipermail/r-devel/2020-April/079384.html">‘mclapply returns NULLs on MacOS when running GAM’</a> on 2020-04-28:</p>
<blockquote class="blockquote">
<p>Do NOT use <code>mcparallel()</code> in packages except as a non-default option that user can set for the reasons … explained [above]. Multicore is intended for HPC applications that need to use many cores for computing-heavy jobs, but it does not play well with RStudio and more importantly you don’t know the resource available so only the user can tell you when it is safe to use. Multi-core machines are often shared so using all detected cores is a very bad idea. The user should be able to explicitly enable it, but it should not be enabled by default.</p>
</blockquote>
<p>It is not always obvious to know whether a certain function call in R is fork safe, especially not if we haven’t written all the code ourselves. Because of this, it is more of a trial and error so see if works. However, when we know that a certain function call is <em>not</em> fork safe, it is useful to protect against using it in forked parallelization. In <strong>parallelly</strong> (&gt;= 1.28.0), we can use function <a href="https://parallelly.futureverse.org/reference/isForkedChild.html"><code>isForkedChild()</code></a> test whether or not R runs in a forked child process. For example, the author of <code>some_slow_fcn()</code> above, could protect against mistakes by:</p>
<div class="sourceCode" id="cb3" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb3-1">some_slow_fcn <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">function</span>(x) {</span>
<span id="cb3-2">  <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">if</span> (parallelly<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">isForkedChild</span>()) {</span>
<span id="cb3-3">    <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">stop</span>(<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"This function must not be used in *forked* parallel processing"</span>)</span>
<span id="cb3-4">  }</span>
<span id="cb3-5">  </span>
<span id="cb3-6">  y <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">non_fork_safe_code</span>(x)</span>
<span id="cb3-7">  ...</span>
<span id="cb3-8">}</span></code></pre></div>
<p>or, if they have an alternative, less preferred, non-fork-safe implementation, they could run that conditionally on R being executed in a forked child process:</p>
<div class="sourceCode" id="cb4" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb4-1">some_slow_fcn <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">function</span>(x) {</span>
<span id="cb4-2">  <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">if</span> (parallelly<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">isForkedChild</span>()) {</span>
<span id="cb4-3">    y <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">fork_safe_code</span>(x)</span>
<span id="cb4-4">  } <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">else</span> {</span>
<span id="cb4-5">    y <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">alternative_code</span>(x)</span>
<span id="cb4-6">  }</span>
<span id="cb4-7">  ...</span>
<span id="cb4-8">}</span></code></pre></div>
</section>
<section id="new-function-isnodealive" class="level2">
<h2 class="anchored" data-anchor-id="new-function-isnodealive">New function isNodeAlive()</h2>
<p>The new function <a href="https://parallelly.futureverse.org/reference/isNodeAlive.html"><code>isNodeAlive()</code></a> checks whether one or more nodes are alive. For instance,</p>
<div class="sourceCode" id="cb5" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb5-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">library</span>(parallelly)</span>
<span id="cb5-2">cl <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">makeClusterPSOCK</span>(<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">3</span>)</span>
<span id="cb5-3"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">isNodeAlive</span>(cl)</span>
<span id="cb5-4"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">#&gt; [1] TRUE TRUE TRUE</span></span></code></pre></div>
<p>Imagine the second parallel worker crashes, which we can emulate with</p>
<div class="sourceCode" id="cb6" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb6-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">clusterEvalQ</span>(cl[<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">2</span>], tools<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">pskill</span>(<span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">Sys.getpid</span>()))</span>
<span id="cb6-2"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">#&gt; Error in unserialize(node$con) : error reading from connection</span></span></code></pre></div>
<p>then we get:</p>
<div class="sourceCode" id="cb7" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb7-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">isNodeAlive</span>(cl)</span>
<span id="cb7-2"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">#&gt; [1]  TRUE FALSE  TRUE</span></span></code></pre></div>
<p>The <code>isNodeAlive()</code> function works by querying the operating system to see if those processes are still running, based their process IDs (PIDs) recorded by <code>makeClusterPSOCK()</code> when launched. If the workers’ PIDs are unknown, then <code>NA</code> is returned instead. For instance, contrary to <code>parallelly::makeClusterPSOCK()</code>, <code>parallel::makeCluster()</code> does not record the PIDs and we get missing values as the result;</p>
<div class="sourceCode" id="cb8" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb8-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">library</span>(parallelly)</span>
<span id="cb8-2">cl <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> parallel<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">makeCluster</span>(<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">3</span>)</span>
<span id="cb8-3"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">isNodeAlive</span>(cl)</span>
<span id="cb8-4"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">#&gt; [1] NA NA NA</span></span></code></pre></div>
<p>Similarly, if one of the parallel workers runs on a remote machine, we cannot easily query the remote machine for the PID existing or not. In such cases, <code>NA</code> is returned. Maybe we will be able to query also remote machines in a future version of <strong>parallelly</strong>, but for now, it is not possible.</p>
</section>
<section id="availablecores-respects-bioconductor-settings" class="level2">
<h2 class="anchored" data-anchor-id="availablecores-respects-bioconductor-settings">availableCores() respects Bioconductor settings</h2>
<p>Function <a href="https://parallelly.futureverse.org/reference/availableCores.html"><code>availableCores()</code></a> queries the hardware and the system environment to find out how many CPU cores it may run on. It does this by checking system settings, environment variables, and R options that may be set by the end-user, the system administrator, the parent R process, the operating system, a job scheduler, and so on. When you use <code>availableCores()</code>, you don’t have to worry about using more CPU resources than you were assigned, which helps guarantee that it runs nicely together with everything else on the same machine.</p>
<p>In <strong>parallelly</strong> (&gt;= 1.29.0), <code>availableCores()</code> is now also agile to Bioconductor-specific settings. For example, <strong><a href="https://bioconductor.org/packages/BiocParallel">BiocParallel</a></strong> 1.27.2 introduced environment variable <code>BIOCPARALLEL_WORKER_NUMBER</code>, which sets the default number of parallel workers when using <strong>BiocParallel</strong> for parallelization. Similarly, on Bioconductor check servers, they set environment variable <code>BBS_HOME</code>, which <strong>BiocParallel</strong> uses to limit the number of cores to four (4). Now <code>availableCores()</code> reflects also those settings, which, in turn, means that <strong>future</strong> settings like <code>plan(multisession)</code> will also automatically respect the Bioconductor settings.</p>
<p>Function <a href="https://parallelly.futureverse.org/reference/availableWorkers.html"><code>availableWorkers()</code></a>, which relies on <code>availableCores()</code> as a fallback, is therefore also agile to these Bioconductor environment variables.</p>
<!--
## Improvements to makeClusterPSOCK() arguments 'rscript' and 'rscript_envs'


Three improvements to [`makeClusterPSOCK()`] has been made:

 * A `*` value in argument `rscript` to `makeClusterPSOCK()` expands to
   the corrent `Rscript` executable
 
 * Argument `rscript_envs` of `makeClusterPSOCK()` can be used to unset
   environment variables onthe parallel workers
 
 * On Unix, the _communication latency_ between the main R session and
   the parallel workers is not much smaller when using
   `makeClusterPSOCK()`
-->
</section>
<section id="makeclusterpsock-rscript" class="level2">
<h2 class="anchored" data-anchor-id="makeclusterpsock-rscript">makeClusterPSOCK(…, rscript = “*“)</h2>
<p>Argument <code>rscript</code> of <code>makeClusterPSOCK()</code> can be used to control exactly which <code>Rscript</code> executable is used to launch the parallel workers, and also how that executable is launched. The default settings is often sufficient, but if you want to launch a worker, say, within a Linux container you can do so by adjusting <code>rscript</code>. The help page for <a href="https://parallelly.futureverse.org/reference/makeClusterPSOCK.html"><code>makeClusterPSOCK()</code></a> has several examples of this. It may also be used for other setups. For example, to launch two parallel workers on a remote Linux machine, such that their CPU priority is less than other processing running on that machine, we can use (*):</p>
<div class="sourceCode" id="cb9" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb9-1">workers <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">rep</span>(<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"remote.example.org"</span>, <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">times =</span> <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">2</span>)</span>
<span id="cb9-2">cl <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">makeClusterPSOCK</span>(workers, <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">rscript =</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">c</span>(<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"nice"</span>, <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"Rscript"</span>))</span></code></pre></div>
<p>This causes the two R workers to be launched using <code>nice Rscript ...</code>. The Unix command <code>nice</code> is what makes <code>Rscript</code> to run with a lower CPU priority. By running at a lower priority, we decrease the risk for our parallel tasks to have a negative impact on other software running on that machine, e.g.&nbsp;someone might use that machine for interactive work without us knowing. We can do the same thing on our local machine via:</p>
<div class="sourceCode" id="cb10" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb10-1">cl <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">makeClusterPSOCK</span>(<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">2</span>L,</span>
<span id="cb10-2">        <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">rscript =</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">c</span>(<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"nice"</span>, <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">file.path</span>(<span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">R.home</span>(<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"bin"</span>), <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"Rscript"</span>)))</span></code></pre></div>
<p>Here we specified the absolute path to <code>Rscript</code> to make sure we run the same version of R as the main R session, and not another <code>Rscript</code> that may be on the system <code>PATH</code>.</p>
<p>Starting with <strong>parallelly</strong> 1.29.0, we can replace the Rscript specification in the above two examples with <code>"*"</code>, as in:</p>
<div class="sourceCode" id="cb11" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb11-1">workers <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">rep</span>(<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"remote-machine.example.org, times = 2L)</span></span>
<span id="cb11-2"><span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">cl &lt;- makeClusterPSOCK(workers, rscript = c("</span>nice<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">", "</span><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">*</span><span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"))</span></span></code></pre></div>
<p>and</p>
<div class="sourceCode" id="cb12" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb12-1">cl <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">makeClusterPSOCK</span>(<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">2</span>L, <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">rscript =</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">c</span>(<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"nice"</span>, <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"*"</span>))</span></code></pre></div>
<p>When used, <code>makeClusterPSOCK()</code> will expand <code>"*"</code> to the proper Rscript specification depending on running remotely or not. To further emphasize the convenience of this, consider:</p>
<div class="sourceCode" id="cb13" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb13-1">workers <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">c</span>(<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"localhost"</span>, <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"remote-machine.example.org"</span>)</span>
<span id="cb13-2">cl <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">makeClusterPSOCK</span>(workers, <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">rscript =</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">c</span>(<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"nice"</span>, <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"*"</span>))</span></code></pre></div>
<p>which launches two parallel workers - one running on local machine and one running on the remote machine.</p>
<p>Note that, when using <strong><a href="https://future.futureverse.org">future</a></strong>, we can pass <code>rscript</code> to <code>plan(multisession)</code> and <code>plan(cluster)</code> to achieve the same thing, as in</p>
<div class="sourceCode" id="cb14" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb14-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">plan</span>(cluster, <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">workers =</span> workers, <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">rscript =</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">c</span>(<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"nice"</span>, <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"*"</span>))</span></code></pre></div>
<p>and</p>
<div class="sourceCode" id="cb15" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb15-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">plan</span>(multisession, <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">workers =</span> <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">2</span>L, <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">rscript =</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">c</span>(<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"nice"</span>, <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"*"</span>))</span></code></pre></div>
<p>(*) Here we use <code>nice</code> as an example, because it is a simple way to illustrate how <code>rscript</code> can be used. As a matter of fact, there is already an <a href="https://parallelly.futureverse.org/reference/makeClusterPSOCK.html">argument <code>renice</code></a>, which we can use to achieve the same without using the <code>rscript</code> argument.</p>
</section>
<section id="makeclusterpsock-rscript_envs-cunset_me-na_character_" class="level2">
<h2 class="anchored" data-anchor-id="makeclusterpsock-rscript_envs-cunset_me-na_character_">makeClusterPSOCK(…, rscript_envs = c(UNSET_ME = NA_character_))</h2>
<p>Argument <code>rscript_envs</code> of <code>makeClusterPSOCK()</code> can be used to set environment variables on cluster nodes, or copy existing ones from the main R session to the cluster nodes. For example,</p>
<div class="sourceCode" id="cb16" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb16-1">cl <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">makeClusterPSOCK</span>(<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">2</span>, <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">rscript_envs =</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">c</span>(<span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">PI =</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"3.14"</span>, <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"MY_EMAIL"</span>))</span></code></pre></div>
<p>will, during startup, set environment variable <code>PI</code> on each of the two cluster nodes to have value <code>3.14</code>. It will also set <code>MY_EMAIL</code> on them to the value of <code>Sys.getenv("MY_EMAIL")</code> in the current R session.</p>
<p>Starting with <strong>parallelly</strong> 1.29.0, we can now also <em>unset</em> environment variables, in case they are set on the cluster nodes. Any named element with a missing value causes the corresponding environment variable to be unset, e.g.</p>
<div class="sourceCode" id="cb17" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb17-1">cl <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">makeClusterPSOCK</span>(<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">2</span>, <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">rscript_envs =</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">c</span>(<span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">_R_CHECK_LENGTH_1_CONDITION_ =</span> <span class="cn" style="color: #8f5902;
background-color: null;
font-style: inherit;">NA_character_</span>))</span></code></pre></div>
<p>This results in passing <code>-e 'Sys.unsetenv("_R_CHECK_LENGTH_1_CONDITION_")'</code> to <code>Rscript</code> when launching each worker.</p>
</section>
<section id="makeclusterpsock-sets-up-clusters-with-less-communication-latency-on-unix" class="level2">
<h2 class="anchored" data-anchor-id="makeclusterpsock-sets-up-clusters-with-less-communication-latency-on-unix">makeClusterPSOCK() sets up clusters with less communication latency on Unix</h2>
<p>It turns out that, in R <em>on Unix</em>, there is <a href="https://stat.ethz.ch/pipermail/r-devel/2020-November/080060.html">a significant <em>latency</em> in the communication between the parallel workers and the main R session</a> (**). Starting in R (&gt;= 4.1.0), it is possible to decrease this latency by setting a dedicated R option <em>on each of the workers</em>, e.g.</p>
<div class="sourceCode" id="cb18" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb18-1">rscript_args <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">c</span>(<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"-e"</span>, <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">shQuote</span>(<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"options(socketOptions = 'no-delay')"</span>)</span>
<span id="cb18-2">cl <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> parallel<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">makeCluster</span>(workers, <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">rscript_args =</span> rscript_args))</span></code></pre></div>
<p>This is quite verbose, so I’ve made this the new default in <strong>parallelly</strong> (&gt;= 1.29.0), i.e.&nbsp;you can keep using:</p>
<div class="sourceCode" id="cb19" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb19-1">cl <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> parallelly<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">makeClusterPSOCK</span>(workers)</span></code></pre></div>
<p>to benefit from the above. See help for <a href="https://parallelly.futureverse.org/reference/makeClusterPSOCK.html"><code>makeClusterPSOCK()</code></a> for options on how to change this new default.</p>
<p>Here is an example that illustrates the difference in latency with and without the new settings;</p>
<div class="sourceCode" id="cb20" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb20-1">cl_parallel   <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> parallel<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">makeCluster</span>(<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span>)</span>
<span id="cb20-2">cl_parallelly <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> parallelly<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">makeClusterPSOCK</span>(<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span>)</span>
<span id="cb20-3"></span>
<span id="cb20-4">res <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> bench<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">mark</span>(<span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">iterations =</span> <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1000</span>L,</span>
<span id="cb20-5">    <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">parallel =</span> parallel<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">clusterEvalQ</span>(cl_parallel, iris),</span>
<span id="cb20-6">  <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">parallelly =</span> parallel<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">clusterEvalQ</span>(cl_parallelly, iris)</span>
<span id="cb20-7">)</span>
<span id="cb20-8"></span>
<span id="cb20-9">res[, <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">c</span>(<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">:</span><span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">4</span>,<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">9</span>)]</span>
<span id="cb20-10"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">#&gt; # A tibble: 2 × 5</span></span>
<span id="cb20-11"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">#&gt;   expression      min   median `itr/sec` total_time</span></span>
<span id="cb20-12"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">#&gt;   &lt;bch:expr&gt; &lt;bch:tm&gt; &lt;bch:tm&gt;     &lt;dbl&gt;   &lt;bch:tm&gt;</span></span>
<span id="cb20-13"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">#&gt; 1 parallel      277µs     44ms      22.5      44.4s</span></span>
<span id="cb20-14"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">#&gt; 2 parallelly    380µs    582µs    1670.     598.3ms</span></span></code></pre></div>
<p>From this, we see that the total latency overhead for 1,000 parallel tasks went from 44 seconds down to 0.60 seconds, which is ~75 times less on average. Does this mean your parallel code will run faster? No, it is just the communication <em>latency</em> that has decreased. But, why waste time on <em>waiting</em> on your results when you don’t have to? This is why I changed the defaults in <strong>parallelly</strong>. It will also bring the experience on Unix on par with MS Windows and macOS.</p>
<p>Note that the relatively high latency affects only Unix. MS Windows and macOS do not suffer from this extra latency. For example, on MS Windows 10 that runs in a virtual machine on the same Linux computer as above, I get:</p>
<div class="sourceCode" id="cb21" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb21-1"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">#&gt; # A tibble: 2 × 5</span></span>
<span id="cb21-2"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">#&gt;   expression      min   median `itr/sec` total_time</span></span>
<span id="cb21-3"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">#&gt;   &lt;bch:expr&gt; &lt;bch:tm&gt; &lt;bch:tm&gt;     &lt;dbl&gt;   &lt;bch:tm&gt;</span></span>
<span id="cb21-4"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">#&gt; 1 parallel      191us    314us     2993.      333ms</span></span>
<span id="cb21-5"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">#&gt; 2 parallelly    164us    311us     3227.      310ms</span></span></code></pre></div>
<p>If you’re using <strong><a href="https://future.futureverse.org">future</a></strong> with <code>plan(multisession)</code> or <code>plan(cluster)</code>, you’re already benefitting from the performance gain, because those rely on <code>parallelly::makeClusterPSOCK()</code> internally.</p>
<!--
avoid a quite large latency in the communication between parallel workers and the main R session
 
```r
gg <- plot(res) + labs(x = element_blank()) + theme(text = element_text(size = 20)) + theme(legend.position = "none")
ggsave("parallelly_faster_turnarounds-figure.png", plot = gg, width = 7.0, height = 5.0)
```

<center>
<img src="resources/parallelly_faster_turnarounds-figure.png" alt="..." style="width: 65%;"/><br/>
</center>
<small><em>Figure: ...<br/></em></small>
-->
<p>(**) <em>Technical details</em>: Options <code>socketOptions</code> sets the default value of argument <code>options</code> of <code>base::socketConnection()</code>. The default is <code>NULL</code>, but if we set it to <code>"no-delay"</code>, the created TCP socket connections are configured to use the <code>TCP_NODELAY</code> flag. When using <code>TCP_NODELAY</code>, a TCP connection will no longer use the so called <a href="https://www.wikipedia.org/wiki/Nagle%27s_algorithm">Nagle’s algorithm</a>, which otherwise is used to reduces the number of TCP packets needed to be sent over the network by making sure TCP fills up each packet before sending it off. When using the new <code>"no-delay"</code>, this buffering is disabled and packets are sent as soon as data come in. Credits for this improvement should go to Jeff Keller, who identified and <a href="https://stat.ethz.ch/pipermail/r-devel/2020-November/080060.html">reported the problem to R-devel</a>, to Iñaki Úcar who pitched in, and to Simon Urbanek, who implemented <a href="https://github.com/wch/r-source/commit/82369f73fc297981e64cac8c9a696d05116f0797">support for <code>socketConnection(..., options = "no-delay")</code></a> for R 4.1.0.</p>
</section>
<section id="bug-fixes" class="level2">
<h2 class="anchored" data-anchor-id="bug-fixes">Bug fixes</h2>
<p>Finally, the most important bug fixes since <strong>parallelly</strong> 1.26.0 are:</p>
<ul>
<li><p><code>availableCores()</code> would produce an error on Linux systems without <code>nproc</code> installed.</p></li>
<li><p><code>makeClusterPSOCK()</code> failed with “Error in freePort(port) : Unknown value on argument ‘port’: ‘auto’” if environment variable <code>R_PARALLEL_PORT</code> was set to a port number.</p></li>
<li><p>In R environments not supporting <code>setup_strategy = "parallel"</code>, <code>makeClusterPSOCK()</code> failed to fall back to <code>setup_strategy = "sequential"</code>.</p></li>
</ul>
<p>For all other bug fixes and updates, please see <a href="https://parallelly.futureverse.org/news/index.html">NEWS</a>.</p>
<!--
<center>
<img src="resources/parallelly_faster_turnarounds.png" alt="..." style="width: 65%;"/><br/>
</center>
<small><em>Figure: Our parallel results are now turned around much faster on Linux than before.<br/></em></small>
-->
<p>Over and out!</p>
</section>
<section id="links" class="level2">
<h2 class="anchored" data-anchor-id="links">Links</h2>
<ul>
<li><strong>parallelly</strong> package: <a href="https://cran.r-project.org/package=parallelly">CRAN</a>, <a href="https://github.com/HenrikBengtsson/parallelly">GitHub</a>, <a href="https://parallelly.futureverse.org">pkgdown</a></li>
<li><strong>future</strong> package: <a href="https://cran.r-project.org/package=future">CRAN</a>, <a href="https://github.com/HenrikBengtsson/future">GitHub</a>, <a href="https://future.futureverse.org">pkgdown</a></li>
<li><strong>future.apply</strong> package: <a href="https://cran.r-project.org/package=future.apply">CRAN</a>, <a href="https://github.com/HenrikBengtsson/future.apply">GitHub</a>, <a href="https://future.apply.futureverse.org">pkgdown</a></li>
</ul>
<!--
nworkers <- 4L
cl_parallel   <- parallel::makeCluster(nworkers)
cl_parallelly <- parallelly::makeClusterPSOCK(nworkers)

plan(cluster, workers = cl_parallel)
stats <- bench::mark(iterations = 100L,
    parallel = { f <- cluster(iris, workers = cl_parallel); value(f) },
  parallelly = { f <- cluster(iris, workers = cl_parallelly); value(f) }
)

plan(cluster, workers = cl_parallelly)
stats2 <- bench::mark(iterations = 10L,
  parallelly = { f <- future(iris); value(f) }
)

stats <- rbind(stats1, stats2)
-->


</section>

 ]]></description>
  <category>R</category>
  <category>package</category>
  <category>future</category>
  <category>parallel</category>
  <category>parallelly</category>
  <category>HPC</category>
  <category>parallel processing</category>
  <guid>https://www.jottr.org/posts/2021-11-22-parallelly_1.29.0/</guid>
  <pubDate>Tue, 23 Nov 2021 05:00:00 GMT</pubDate>
</item>
<item>
  <title>matrixStats: Consistent Support for Name Attributes via GSoC Project</title>
  <dc:creator>Angelina Panagopoulou</dc:creator>
  <link>https://www.jottr.org/posts/2021-08-23-matrixStats-GSoC_2021/</link>
  <description><![CDATA[ 






<center>
<img src="https://www.jottr.org/posts/2021-08-23-matrixStats-GSoC_2021/resources/2048px-GSoC_logo.svg.png" alt="Google Summer of Code logo" style="width: 40%; margin: 2ex;"> <!-- Image source: https://commons.wikimedia.org/wiki/File:GSoC_logo.svg -->
</center>
<p>We are glad to announce recent CRAN releases of <strong><a href="https://cran.r-project.org/package=matrixStats">matrixStats</a></strong> with support for handling and returning name attributes. This feature is added to make <strong>matrixStats</strong> functions handle names in the same manner as the corresponding base R functions. In particular, the behavior of <strong>matrixStats</strong> functions is now the same as the <code>apply()</code> function in R, resolving previous lack of, or inconsistent, handling of row and column names. The added support for <code>names</code> and <code>dimnames</code> attributes has already reached a wide, active user base, while at the same time we expect to attract users and developers who lack this feature and therefore could not use <strong>matrixStats</strong> package for their needs.</p>
<p>The <strong>matrixStats</strong> package provides high-performing functions operating on rows and columns of matrices. These functions are optimized such that both memory use and processing time are minimized. In order to minimize the overhead of handling name attributes, the naming support is implemented in native (C) code, where possible. In <strong>matrixStats</strong> (&gt;= 0.60.0), handling of row and column names is optional. This is done to allow for maximum performance where needed. In addition, in order to avoid breaking some scripts and packages that rely on the previous semi-inconsistent behavior of functions, special care has been taken to ensure backward compatibility by default for the time being. We have validated the correctness of these newly implemented features by extending existing package tests to check name attributes, measuring the code coverage with the <strong><a href="https://cran.r-project.org/package=covr">covr</a></strong> package, and checking all 358 reverse-dependency packages using the <strong><a href="https://github.com/r-lib/revdepcheck">revdepcheck</a></strong> package.</p>
<section id="example" class="level2">
<h2 class="anchored" data-anchor-id="example">Example</h2>
<p><code>useNames</code> is an argument added to each of the <strong>matrixStats</strong> functions that gained support naming. It takes values <code>TRUE</code>, <code>FALSE</code>, or <code>NA</code>. For backward compatible reasons, the default value of <code>useNames</code> is <code>NA</code>, meaning the default behavior from earlier versions of <strong>matrixStats</strong> is preserved. If <code>TRUE</code>, <code>names</code> or <code>dimnames</code> attribute of result is set, otherwise, if <code>FALSE</code>, the results do not have name attributes set. For example, consider the following 5-by-3 matrix with row and column names:</p>
<div class="sourceCode" id="cb1" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb1-1"><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">&gt;</span> x <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">matrix</span>(<span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">rnorm</span>(<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">5</span> <span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">*</span> <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">3</span>), <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">nrow =</span> <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">5</span>, <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">ncol =</span> <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">3</span>, <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">dimnames =</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">list</span>(letters[<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">:</span><span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">5</span>], LETTERS[<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">:</span><span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">3</span>]))</span>
<span id="cb1-2"><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">&gt;</span> x</span>
<span id="cb1-3">            A          B          C</span>
<span id="cb1-4">a  <span class="fl" style="color: #AD0000;
background-color: null;
font-style: inherit;">0.30292612</span>  <span class="fl" style="color: #AD0000;
background-color: null;
font-style: inherit;">1.3825644</span> <span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span><span class="fl" style="color: #AD0000;
background-color: null;
font-style: inherit;">0.2125219</span></span>
<span id="cb1-5">b  <span class="fl" style="color: #AD0000;
background-color: null;
font-style: inherit;">0.15812229</span>  <span class="fl" style="color: #AD0000;
background-color: null;
font-style: inherit;">2.7719647</span>  <span class="fl" style="color: #AD0000;
background-color: null;
font-style: inherit;">1.6237263</span></span>
<span id="cb1-6">c <span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span><span class="fl" style="color: #AD0000;
background-color: null;
font-style: inherit;">0.09881700</span> <span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span><span class="fl" style="color: #AD0000;
background-color: null;
font-style: inherit;">0.6468119</span> <span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span><span class="fl" style="color: #AD0000;
background-color: null;
font-style: inherit;">0.6481911</span></span>
<span id="cb1-7">d  <span class="fl" style="color: #AD0000;
background-color: null;
font-style: inherit;">0.38520941</span> <span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span><span class="fl" style="color: #AD0000;
background-color: null;
font-style: inherit;">0.8466505</span> <span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span><span class="fl" style="color: #AD0000;
background-color: null;
font-style: inherit;">0.4779964</span></span>
<span id="cb1-8">e <span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span><span class="fl" style="color: #AD0000;
background-color: null;
font-style: inherit;">0.01599926</span> <span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span><span class="fl" style="color: #AD0000;
background-color: null;
font-style: inherit;">0.8907434</span>  <span class="fl" style="color: #AD0000;
background-color: null;
font-style: inherit;">0.6334347</span></span></code></pre></div>
<p>If we use the base R method to calculate row medians, we see that the names attribute of the results reflects the row names of the input matrix:</p>
<div class="sourceCode" id="cb2" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb2-1"><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">&gt;</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">library</span>(stats)</span>
<span id="cb2-2"><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">&gt;</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">apply</span>(x, <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">MARGIN =</span> <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span>, <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">FUN =</span> median)</span>
<span id="cb2-3">          a           b           c           d           e </span>
<span id="cb2-4"> <span class="fl" style="color: #AD0000;
background-color: null;
font-style: inherit;">0.30292612</span>  <span class="fl" style="color: #AD0000;
background-color: null;
font-style: inherit;">1.62372626</span> <span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span><span class="fl" style="color: #AD0000;
background-color: null;
font-style: inherit;">0.64681187</span> <span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span><span class="fl" style="color: #AD0000;
background-color: null;
font-style: inherit;">0.47799635</span> <span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span><span class="fl" style="color: #AD0000;
background-color: null;
font-style: inherit;">0.01599926</span> </span></code></pre></div>
<p>If we use <strong>matrixStats</strong> function <code>rowMedians()</code> with argument <code>useNames = TRUE</code> set, we get the same result as above:</p>
<div class="sourceCode" id="cb3" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb3-1"><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">&gt;</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">library</span>(matrixStats)</span>
<span id="cb3-2"><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">&gt;</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">rowMedians</span>(x, <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">useNames =</span> <span class="cn" style="color: #8f5902;
background-color: null;
font-style: inherit;">TRUE</span>)</span>
<span id="cb3-3">          a           b           c           d           e </span>
<span id="cb3-4"> <span class="fl" style="color: #AD0000;
background-color: null;
font-style: inherit;">0.30292612</span>  <span class="fl" style="color: #AD0000;
background-color: null;
font-style: inherit;">1.62372626</span> <span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span><span class="fl" style="color: #AD0000;
background-color: null;
font-style: inherit;">0.64681187</span> <span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span><span class="fl" style="color: #AD0000;
background-color: null;
font-style: inherit;">0.47799635</span> <span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span><span class="fl" style="color: #AD0000;
background-color: null;
font-style: inherit;">0.01599926</span></span></code></pre></div>
<p>If the name attributes are not of interest, we can use <code>useNames = FALSE</code> as in:</p>
<div class="sourceCode" id="cb4" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb4-1"><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">&gt;</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">rowMedians</span>(x, <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">useNames =</span> <span class="cn" style="color: #8f5902;
background-color: null;
font-style: inherit;">FALSE</span>)</span>
<span id="cb4-2">[<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span>]  <span class="fl" style="color: #AD0000;
background-color: null;
font-style: inherit;">0.30292612</span>  <span class="fl" style="color: #AD0000;
background-color: null;
font-style: inherit;">1.62372626</span> <span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span><span class="fl" style="color: #AD0000;
background-color: null;
font-style: inherit;">0.64681187</span> <span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span><span class="fl" style="color: #AD0000;
background-color: null;
font-style: inherit;">0.47799635</span> <span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span><span class="fl" style="color: #AD0000;
background-color: null;
font-style: inherit;">0.01599926</span></span></code></pre></div>
<p>Doing so will also avoid the overhead, time and memory, that otherwise comes from processing name attributes.</p>
<p>If we don’t specify <code>useNames</code> explicitly, the default is currently <code>useNames = NA</code>, which corresponds to the non-documented behavior that existed in <strong>matrixStats</strong> (&lt; 0.60.0). For several functions, that corresponded to setting <code>useNames = FALSE</code>, however for other functions it corresponds to setting <code>useNames = TRUE</code>, and for others it might have set, say, row names but not column names. In our example, the default happens to be the same as <code>useNames = FALSE</code>:</p>
<div class="sourceCode" id="cb5" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb5-1"><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">&gt;</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">rowMedians</span>(x) <span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># default as in matrixStats (&lt; 0.60.0)</span></span>
<span id="cb5-2">[<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span>]  <span class="fl" style="color: #AD0000;
background-color: null;
font-style: inherit;">0.30292612</span>  <span class="fl" style="color: #AD0000;
background-color: null;
font-style: inherit;">1.62372626</span> <span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span><span class="fl" style="color: #AD0000;
background-color: null;
font-style: inherit;">0.64681187</span> <span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span><span class="fl" style="color: #AD0000;
background-color: null;
font-style: inherit;">0.47799635</span> <span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span><span class="fl" style="color: #AD0000;
background-color: null;
font-style: inherit;">0.01599926</span></span></code></pre></div>
</section>
<section id="future-plan" class="level2">
<h2 class="anchored" data-anchor-id="future-plan">Future Plan</h2>
<p>The future plan is to change the default value of <code>useNames</code> to <code>TRUE</code> or <code>FALSE</code> and eventually deprecate the backward-compatible behavior of <code>useNames = NA</code>. The default value of <code>useNames</code> is a design choice that requires further investigation. On the one hand, <code>useNames = TRUE</code> as the default is more convenient, but creates an additional performance and memory overhead when name attributes are not needed. On the other hand, make <code>FALSE</code> the default is appropriate for users and packages that rely on the maximum performance. Whatever the new default will become, we will make sure to work with package maintainers to minimize the risk for breaking existing code.</p>
</section>
<section id="google-summer-of-code-2021" class="level2">
<h2 class="anchored" data-anchor-id="google-summer-of-code-2021">Google Summer of Code 2021</h2>
<p>The project that introduces the consistent support for name attributes on the <strong>matrixStats</strong> package is a part of the <a href="https://github.com/rstats-gsoc/gsoc2021/wiki">R Project’s participation in the Google Summer of Code 2021</a>.</p>
<section id="links" class="level3">
<h3 class="anchored" data-anchor-id="links">Links</h3>
<ul>
<li><a href="https://github.com/rstats-gsoc/gsoc2021/wiki/matrixStats">The matrixStats GSoC 2021 project</a></li>
<li><a href="https://cran.r-project.org/web/packages/matrixStats/index.html">matrixStats CRAN page</a></li>
<li><a href="https://github.com/HenrikBengtsson/matrixStats">matrixStats GitHub page</a></li>
<li><a href="https://github.com/HenrikBengtsson/matrixStats/commits?author=AngelPn">All commits during GSoC 2021 - author Angelina Panagopoulou</a></li>
</ul>
</section>
<section id="authors" class="level3">
<h3 class="anchored" data-anchor-id="authors">Authors</h3>
<ul>
<li><a href="https://github.com/AngelPn">Angelina Panagopoulou</a> - <em>Student Developer</em>: I am an undergraduate in the Department of Informatics &amp; Telecommunications (DIT) in University of Athens.</li>
<li><a href="https://github.com/yaccos">Jakob Peder Pettersen</a> - <em>Mentor</em>: PhD Student, Department of Biotechnology and Food Science, Norwegian University of Science and Technology (NTNU). Jakob is a part of the <a href="https://almaaslab.nt.ntnu.no/">Almaas Lab</a> and does research on genome-scale metabolic modeling and behavior of microbial communities.</li>
<li><a href="https://github.com/HenrikBengtsson/">Henrik Bengtsson</a> - <em>Co-Mentor</em>: Associate Professor, Department of Epidemiology and Biostatistics, University of California San Francisco (UCSF). He is the author and maintainer of a large number of CRAN and Bioconductor packages including <strong>matrixStats</strong>.</li>
</ul>
</section>
<section id="contributions" class="level3">
<h3 class="anchored" data-anchor-id="contributions">Contributions</h3>
<p><strong>Phase I</strong></p>
<ul>
<li>All functions implements <code>useNames = NA/FALSE/TRUE</code> using R code and tests are written.</li>
<li>Identify reverse dependency packages that rely on <code>useNames = NA/FALSE/TRUE</code>.</li>
<li>New release on CRAN with <code>useNames = NA</code>. This allow useRs and package maintainers to complain if anything breaks.</li>
</ul>
<p><strong>Phase II</strong></p>
<ul>
<li>Changed C code structure such that <code>validateIndices()</code> always return <code>R_xlen_t*</code>. Clean up unnecessary macros.
<ul>
<li>Outcome: shorter compile times, smaller compiled package/library, fewer exported symbols.</li>
</ul></li>
<li>Simplify C API for <code>setNames()/setDimnames()</code>.</li>
<li>Implemented <code>useNames = NA/FALSE/TRUE</code> in C code where possible and cleanup work too.</li>
</ul>
</section>
<section id="summary" class="level3">
<h3 class="anchored" data-anchor-id="summary">Summary</h3>
<p>We have completed all goals that we had initially planned. The release 0.60.0 of <strong>matrixStats</strong> on CRAN included the contributions of GSoC Phase I (“implementation in R”) and a new release of version 0.60.1 includes the contributions of Phase II (“implementation in C”).</p>
</section>
<section id="experience" class="level3">
<h3 class="anchored" data-anchor-id="experience">Experience</h3>
<p>When I first heard about the Google Summer of Code, I really wanted to participate in it, but I thought that maybe I do not have the prerequisite knowledge yet. And it was true. It was difficult for me to find a project that I had at least half of the mentioned prerequisites. So, I started looking for a project based on what I would be interested in doing during the summer. This project was an opportunity for me to learn a new programming language, the R language, and also to get in touch with advanced R. I am grateful for all the learning opportunities: programming in R, developing an R package, using a variety of tools that make developing R packages easier and more productive, working with GitHub tools, interacting with the open source community. My mentors had an understanding of the lack of experience and really helped me achieve this. Participating in Google Summer of Code 2021 as student developer is definitely worth it and I recommend every student who wants to open source contribute to give it a try.</p>
</section>
</section>
<section id="acknowledgements" class="level2">
<h2 class="anchored" data-anchor-id="acknowledgements">Acknowledgements</h2>
<ul>
<li>The Google Summer of Code program for bringing more student developers into open source software development.</li>
<li>Jacob Pettersen for being a great project leader and for providing guidance and willingness to impart his knowledge. Henrik Bengtsson whose insight and knowledge into the subject matter steered me through R package development. I am very grateful for the immense amount of useful discussions and valuable feedback.</li>
<li>The members of the R community for building this warming community.</li>
</ul>


</section>

 ]]></description>
  <category>R</category>
  <category>package</category>
  <category>matrixStats</category>
  <category>project</category>
  <category>GSoC</category>
  <category>performance</category>
  <category>memory</category>
  <category>matrix</category>
  <category>name attributes</category>
  <guid>https://www.jottr.org/posts/2021-08-23-matrixStats-GSoC_2021/</guid>
  <pubDate>Sun, 22 Aug 2021 22:10:00 GMT</pubDate>
</item>
<item>
  <title>progressr 0.8.0: RStudio’s Progress Bar, Shiny Progress Updates, and Absolute Progress</title>
  <dc:creator>Henrik Bengtsson</dc:creator>
  <link>https://www.jottr.org/posts/2021-06-11-progressr_0.8.0/</link>
  <description><![CDATA[ 






<p><strong><a href="https://progressr.futureverse.org">progressr</a></strong> 0.8.0 is on CRAN. It comes with some new features:</p>
<ul>
<li>A new ‘rstudio’ handler that reports on progress via the RStudio job interface in RStudio</li>
<li><code>withProgressShiny()</code> now updates the <code>detail</code> part, instead of the <code>message</code> part</li>
<li>In addition to signalling relative amounts of progress, it’s now also possible to signal total amounts</li>
</ul>
<p>If you’re curious what <strong>progressr</strong> is about, have a look at my <a href="../../2020/07/04/progressr-erum2020-slides/">e-Rum 2020 presentation</a>.</p>
<section id="progress-updates-in-rstudios-job-interface" class="level2">
<h2 class="anchored" data-anchor-id="progress-updates-in-rstudios-job-interface">Progress updates in RStudio’s job interface</h2>
<p>If you’re using RStudio Console, you can now report on progress in the RStudio’s job interface as long as the progress originates from a <strong>progressr</strong>-signalling function. I’ve shown an example of this in Figure&nbsp;1.</p>
<figure style="margin-top: 3ex;" class="figure">
<img src="https://www.jottr.org/posts/2021-06-11-progressr_0.8.0/resources/progressr-rstudio.png" alt="A screenshot of the upper part of the RStudio Console panel.  Below the title bar, which says 'R 4.1.0 ~/', there is a row with the text 'Console 05:50:51 PM' left of a green progress bar at 30% followed by the text '0:03'.  Below these two lines are the R commands called this far, which are the same as in the below example. Following the commands, is output 'M: Added value 1',  'M: Added value 2', and  'M: Added value 3', from the first steps that have completed this far." class="figure-img">
<figcaption>
Figure 1: The RStudio job interface can show progress bars and we can use it with <strong>progressr</strong>. The progress bar title - “Console 05:50:51 PM” - shows at what time the progress began. The ‘0:03’ shows for how long the progress has been running - here 3 seconds.
</figcaption>
</figure>
<p>To try this yourself, run the below in the RStudio Console.</p>
<div class="sourceCode" id="cb1" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb1-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">library</span>(progressr)</span>
<span id="cb1-2"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">handlers</span>(<span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">global =</span> <span class="cn" style="color: #8f5902;
background-color: null;
font-style: inherit;">TRUE</span>)</span>
<span id="cb1-3"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">handlers</span>(<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"rstudio"</span>)</span>
<span id="cb1-4">y <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">slow_sum</span>(<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">:</span><span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">10</span>)</span></code></pre></div>
<p>The progress bar disappears when the calculation completes.</p>
</section>
<section id="tweaks-to-withprogressshiny" class="level2">
<h2 class="anchored" data-anchor-id="tweaks-to-withprogressshiny">Tweaks to withProgressShiny()</h2>
<p>The <code>withProgressShiny()</code> function, which is a <strong>progressr</strong>-aware version of <code>withProgress()</code>, gained argument <code>inputs</code>. It defaults to <code>inputs = list(message = NULL, detail = "message")</code>, which says that a progress message should update the ‘detail’ part of the Shiny progress panel. For example,</p>
<div class="sourceCode" id="cb2" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb2-1">X <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">:</span><span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">10</span></span>
<span id="cb2-2"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">withProgressShiny</span>(<span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">message =</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"Calculation in progress"</span>,</span>
<span id="cb2-3">                  <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">detail =</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"Starting ..."</span>,</span>
<span id="cb2-4">                  <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">value =</span> <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">0</span>, {</span>
<span id="cb2-5">  p <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">progressor</span>(<span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">along =</span> X)</span>
<span id="cb2-6">  y <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">lapply</span>(X, <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">FUN=</span><span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">function</span>(x) {</span>
<span id="cb2-7">    <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">Sys.sleep</span>(<span class="fl" style="color: #AD0000;
background-color: null;
font-style: inherit;">0.25</span>)</span>
<span id="cb2-8">    <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">p</span>(<span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">sprintf</span>(<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"x=%d"</span>, x))</span>
<span id="cb2-9">  })</span>
<span id="cb2-10">})</span></code></pre></div>
<p>will start out as in the left panel of Figure&nbsp;2, and, as soon as the first progress signal is received, the ‘detail’ part is updated with <code>x=1</code> as shown in the right panel.</p>
<figure style="margin-top: 3ex;" class="figure">
<table style="margin: 1ex;">
<tbody><tr style="margin: 1ex;">
<td>
<img src="https://www.jottr.org/posts/2021-06-11-progressr_0.8.0/resources/withProgressShiny_A_x=0.png" alt="A Shiny progress bar panel with a progress bar at 0% on top, with 'Calculation in progress' written in a bold large font, with 'Starting ...' written in a normal small font below." class="figure-img">
</td>
<td>
<img src="https://www.jottr.org/posts/2021-06-11-progressr_0.8.0/resources/withProgressShiny_A_x=1.png" alt="A Shiny progress bar panel with a progress bar at 10% on top, with 'Calculation in progress' written in a bold large font, with 'x=1' written in a normal small font below." class="figure-img">
</td>
</tr>
</tbody></table>
<figcaption>
Figure 2: A Shiny progress panel that start out with the ‘message’ part displaying “Calculation in progress” and the ‘detail’ part displaying “Starting …” (left), and whose ‘detail’ part is updated to “x=1” (right) as soon the first progress update comes in.
</figcaption>
</figure>
<p>Prior to this new release, the default behavior was to update the ‘message’ part of the Shiny progress panel. To revert to the old behavior, set argument <code>inputs</code> as in:</p>
<div class="sourceCode" id="cb3" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb3-1">X <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">:</span><span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">10</span></span>
<span id="cb3-2"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">withProgressShiny</span>(<span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">message =</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"Starting ..."</span>,</span>
<span id="cb3-3">                  <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">detail =</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"Calculation in progress"</span>,</span>
<span id="cb3-4">                  <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">value =</span> <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">0</span>, {</span>
<span id="cb3-5">  p <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">progressor</span>(<span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">along =</span> X)</span>
<span id="cb3-6">  y <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">lapply</span>(X, <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">FUN=</span><span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">function</span>(x) {</span>
<span id="cb3-7">    <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">Sys.sleep</span>(<span class="fl" style="color: #AD0000;
background-color: null;
font-style: inherit;">0.25</span>)</span>
<span id="cb3-8">    <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">p</span>(<span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">sprintf</span>(<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"x=%d"</span>, x))</span>
<span id="cb3-9">  })</span>
<span id="cb3-10">}, <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">inputs =</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">list</span>(<span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">message =</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"message"</span>, <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">detail =</span> <span class="cn" style="color: #8f5902;
background-color: null;
font-style: inherit;">NULL</span>))</span></code></pre></div>
<p>This results in what you see in Figure&nbsp;3. I think that the new behavior, as shown in Figure&nbsp;2, looks better and makes more sense.</p>
<figure style="margin-top: 3ex;" class="figure">
<table style="margin: 1ex;">
<tbody><tr style="margin: 1ex;">
<td>
<img src="https://www.jottr.org/posts/2021-06-11-progressr_0.8.0/resources/withProgressShiny_B_x=0.png" alt="A Shiny progress bar panel with a progress bar at 0% on top, with 'Starting ...' written in a bold large font, with 'Calculation in progress' written to the right of it and wrapping onto the next row." class="figure-img">
</td>
<td>
<img src="https://www.jottr.org/posts/2021-06-11-progressr_0.8.0/resources/withProgressShiny_B_x=1.png" alt="A Shiny progress bar panel with a progress bar at 10% on top, with 'x=1' written in a bold large font, with 'Calculation in progress' written to the right of it." class="figure-img">
</td>
</tr>
</tbody></table>
<figcaption>
Figure 3: A Shiny progress panel that start out with the ‘message’ part displaying “Starting …” and the ‘detail’ part displaying “Calculation in progress” (left), and whose ‘message’ part is updated to “x=1” (right) as soon the first progress update comes in.
</figcaption>
</figure>
</section>
<section id="update-to-a-specific-amount-of-total-progress" class="level2">
<h2 class="anchored" data-anchor-id="update-to-a-specific-amount-of-total-progress">Update to a specific amount of total progress</h2>
<p>When using <strong>progressr</strong>, we start out by creating a progressor function that we then call to signal progress. For example, if we do:</p>
<div class="sourceCode" id="cb4" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb4-1">my_slow_fun <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">function</span>() {</span>
<span id="cb4-2">  p <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> progressr<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">progressor</span>(<span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">steps =</span> <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">10</span>)</span>
<span id="cb4-3">  count <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">0</span></span>
<span id="cb4-4">  <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">for</span> (i <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">in</span> <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">:</span><span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">10</span>) {</span>
<span id="cb4-5">    count <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> count <span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">+</span> <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span></span>
<span id="cb4-6">    <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">Sys.sleep</span>(<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span>)</span>
<span id="cb4-7">    <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">p</span>(<span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">sprintf</span>(<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"count=%d"</span>, count))</span>
<span id="cb4-8">  }</span>
<span id="cb4-9">  count</span>
<span id="cb4-10">}<span class="er" style="color: #AD0000;
background-color: null;
font-style: inherit;">)</span></span></code></pre></div>
<p>each call to <code>p()</code> corresponds to <code>p(amount = 1)</code>, which signals that our function have moved <code>amount = 1</code> steps closer to the total amount <code>steps = 10</code>. We can take smaller or bigger steps by specifying another <code>amount</code>.</p>
<p>In this new version, I’ve introduced a new beta feature that allows us to signal progress that says where we are in <em>absolute terms</em>. With this, we can do things like:</p>
<div class="sourceCode" id="cb5" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb5-1">my_slow_fun <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">function</span>() {</span>
<span id="cb5-2">  p <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> progressr<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">progressor</span>(<span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">steps =</span> <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">10</span>)</span>
<span id="cb5-3">  count <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">0</span></span>
<span id="cb5-4">  <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">for</span> (i <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">in</span> <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">:</span><span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">5</span>) {</span>
<span id="cb5-5">    count <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> count <span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">+</span> <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span></span>
<span id="cb5-6">    <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">Sys.sleep</span>(<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span>)</span>
<span id="cb5-7">    <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">if</span> (<span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">runif</span>(<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span>) <span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">&lt;</span> <span class="fl" style="color: #AD0000;
background-color: null;
font-style: inherit;">0.5</span>) <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">break</span></span>
<span id="cb5-8">    <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">p</span>(<span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">sprintf</span>(<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"count=%d"</span>, count))</span>
<span id="cb5-9">  }</span>
<span id="cb5-10">  <span class="do" style="color: #5E5E5E;
background-color: null;
font-style: italic;">## In case we broke out of the loop early,</span></span>
<span id="cb5-11">  <span class="do" style="color: #5E5E5E;
background-color: null;
font-style: italic;">## make sure to update to 5/10 progress</span></span>
<span id="cb5-12">  <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">p</span>(<span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">step =</span> <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">5</span>)</span>
<span id="cb5-13">  <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">for</span> (i <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">in</span> <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">:</span><span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">5</span>) {</span>
<span id="cb5-14">    count <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> count <span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">+</span> <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span></span>
<span id="cb5-15">    <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">Sys.sleep</span>(<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span>)</span>
<span id="cb5-16">    <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">p</span>(<span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">sprintf</span>(<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"count=%d"</span>, count))</span>
<span id="cb5-17">  }</span>
<span id="cb5-18">  count</span>
<span id="cb5-19">}</span></code></pre></div>
<p>When calling <code>my_slow_fun()</code>, we might see progress being reported as:</p>
<pre><code>- [------------------------------------------------]   0% 
\ [===&gt;-------------------------------------]  10% count=1
| [=======&gt;---------------------------------]  20% count=2
\ [===================&gt;---------------------]  50% count=3
...</code></pre>
<p>Note how it took a leap from 20% to 50% when <code>count == 2</code>. If we run it again, the move to 50% might happen at another iteration.</p>
</section>
<section id="wrapping-up" class="level2">
<h2 class="anchored" data-anchor-id="wrapping-up">Wrapping up</h2>
<p>There are also a few bug fixes, which you can read about in <a href="https://progressr.futureverse.org/news/index.html">NEWS</a>. And a usual, all of this work also when you run in parallel using the <a href="https://futureverse.org">future framework</a>.</p>
<p>Make progress!</p>
</section>
<section id="links" class="level2">
<h2 class="anchored" data-anchor-id="links">Links</h2>
<ul>
<li><strong>progressr</strong> package: <a href="https://cran.r-project.org/package=progressr">CRAN</a>, <a href="https://github.com/HenrikBengtsson/progressr">GitHub</a>, <a href="https://progressr.futureverse.org">pkgdown</a></li>
</ul>


</section>

 ]]></description>
  <category>R</category>
  <category>package</category>
  <category>progressr</category>
  <category>parallel</category>
  <category>shiny</category>
  <category>rstudio</category>
  <guid>https://www.jottr.org/posts/2021-06-11-progressr_0.8.0/</guid>
  <pubDate>Sat, 12 Jun 2021 02:00:00 GMT</pubDate>
</item>
<item>
  <title>parallelly 1.26.0: Fast, Concurrent Setup of Parallel Workers (Finally)</title>
  <dc:creator>Henrik Bengtsson</dc:creator>
  <link>https://www.jottr.org/posts/2021-06-10-parallelly_1.26.0/</link>
  <description><![CDATA[ 






<p><strong><a href="https://parallelly.futureverse.org">parallelly</a></strong> 1.26.0 is on CRAN. It comes with one major improvement and one new function:</p>
<ul>
<li><p>The setup of parallel workers is now <em>much faster</em>, which comes from using a concurrent, instead of sequential, setup strategy</p></li>
<li><p>The new <code>freePort()</code> can be used to find a TCP port that is currently available</p></li>
</ul>
<section id="faster-setup-of-local-parallel-workers" class="level2">
<h2 class="anchored" data-anchor-id="faster-setup-of-local-parallel-workers">Faster setup of local, parallel workers</h2>
<p>In R 4.0.0, which was released in May 2020, <code>parallel::makeCluster(n)</code> gained the power of setting up the <code>n</code> local cluster nodes all at the same time, which greatly reduces to total setup time. Previously, because it was setting up the workers one after the other, which involved a lot of waiting for each worker to get ready. You can read about the details in the <a href="https://developer.r-project.org/Blog/public/2020/03/17/socket-connections-update/index.html">Socket Connections Update</a> blog post by Tomas Kalibera and Luke Tierney on 2020-03-17.</p>
<center>
<img src="https://www.jottr.org/posts/2021-06-10-parallelly_1.26.0/resources/parallelly_faster_setup_of_cluster.png" alt="An X-Y graph with 'Total setup time (s)' on the vertical axis ranging from 0 to 55, and 'Number of cores' on the horizontal axis ranging from 0 to 128. Two smooth curves, which look very linear with intersection at the origin and unnoticeable variance, are drawn for the two setup strategies 'sequential' and 'parallel'. The 'sequential' line is much steeper." style="width: 65%;"><br>
</center>
<p><small><em>Figure: The total setup time versus the number of local cluster workers for the “sequential” setup strategy (red) and the new “parallel” strategy (turquoise). Data were collected on a 128-core Linux machine.<br></em></small></p>
<p>With this release of <strong>parallelly</strong>, <code>parallelly::makeClusterPSOCK(n)</code> gained the same skills. I benchmarked the new, default “parallel” setup strategy against the previous “sequential” strategy on a CentOS 7 Linux machine with 128 CPU cores and 512 GiB RAM while the machine was idle. I ran these benchmarks five times, which are summarized as smooth curves in the above figure. The variance between the replicate runs is tiny and the smooth curves appear almost linear. Assuming a linear relationship between setup time and number of cluster workers, a linear fit of gives a speedup of approximately 50 times on this machine. It took 52 seconds to set up 122 (sic!) workers when using the “sequential” approach, whereas it took only 1.1 seconds with the “parallel” approach. Not surprisingly, rerunning these benchmarks with <code>parallel::makePSOCKcluster()</code> instead gives nearly identical results.</p>
<p>Importantly, the “parallel” setup strategy, which is the new default, can only be used when setting up parallel workers running on the local machine. When setting up workers on external or remote machines, the “sequential” setup strategy will still be used.</p>
<p>If you’re using <strong><a href="https://future.futureverse.org">future</a></strong> and use</p>
<div class="sourceCode" id="cb1" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb1-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">plan</span>(multisession)</span></code></pre></div>
<p>you’ll immediately benefit from this performance gain, because it relies on <code>parallelly::makeClusterPSOCK()</code> internally.</p>
<p>All credit for this improvement in <strong>parallelly</strong> and <code>parallelly::makeClusterPSOCK()</code> should go to Tomas Kalibera and Luke Tierney, who implemented support for this in R 4.0.0.</p>
<p><em>Edit 2021-06-11 and 2021-07-01</em>: There’s a bug in R (&gt;= 4.0.0 &amp;&amp; &lt;= 4.1.0) causing the new <code>setup_strategy = "parallel"</code> to fail in the RStudio Console on some systems. If you’re running <em>RStudio Console</em> and get “Error in makeClusterPSOCK(workers, …) : Cluster setup failed. 8 of 8 workers failed to connect.”, update to <strong>parallelly</strong> 1.26.1 released on 2021-06-30:</p>
<div class="sourceCode" id="cb2" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb2-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">install.packages</span>(<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"parallelly"</span>)</span></code></pre></div>
<p>which will work around this problem. Alternatively, you can manually set:</p>
<div class="sourceCode" id="cb3" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb3-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">options</span>(<span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">parallelly.makeNodePSOCK.setup_strategy =</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"sequential"</span>)</span></code></pre></div>
<p><em>Comment</em>: Note that I could only test with up to 122 parallel workers, and not 128, which is the number of CPU cores available on the test machine. The reason for this is that each worker consumes one R connection in the main R session, and R has a limit in the number of connection it can have open at any time. The typical R installation can only have 128 connections open, and three are always occupied by the standard input (stdin), the standard output (stdout), and the standard error (stderr). Thus, the absolute maximum number of workers I could use 125. However, because I used the <strong><a href="https://progressr.futureverse.org">progressr</a></strong> package to report on progress, and a few other things that consumed a few more connections, I could only test up to 122 workers. You can read more about this limit in <a href="https://parallelly.futureverse.org/reference/availableConnections.html"><code>?parallelly::freeConnections</code></a>, which also gives a reference for how to increase this limit by recompling R from source.</p>
</section>
<section id="find-an-available-tcp-port" class="level2">
<h2 class="anchored" data-anchor-id="find-an-available-tcp-port">Find an available TCP port</h2>
<p>I’ve also added <code>freePort()</code>, which will find a random port in [1024,65535] that is currently not occupied by another process on the machine. For example,</p>
<div class="sourceCode" id="cb4" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb4-1"><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">&gt;</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">freePort</span>()</span>
<span id="cb4-2">[<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span>] <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">30386</span></span>
<span id="cb4-3"><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">&gt;</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">freePort</span>()</span>
<span id="cb4-4">[<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span>] <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">37882</span></span></code></pre></div>
<p>Using this function to pick a TCP port at random lowers the risk of trying to use a port already occupied as when using just <code>sample(1024:65535, size=1)</code>.</p>
<p>Just like <code>parallel::makePSOCKcluster()</code>, <code>parallelly::makeClusterPSOCK()</code> still uses <code>sample(11000:11999, size=1)</code> to find a random port. I want <code>freePort()</code> to get some more mileage and CRAN validation before switching over, but the plan is to use <code>freePort()</code> by default in the next release of <strong>parallelly</strong>.</p>
<p>Over and out!</p>
</section>
<section id="links" class="level2">
<h2 class="anchored" data-anchor-id="links">Links</h2>
<ul>
<li><strong>parallelly</strong> package: <a href="https://cran.r-project.org/package=parallelly">CRAN</a>, <a href="https://github.com/HenrikBengtsson/parallelly">GitHub</a>, <a href="https://parallelly.futureverse.org">pkgdown</a></li>
<li><strong>future</strong> package: <a href="https://cran.r-project.org/package=future">CRAN</a>, <a href="https://github.com/HenrikBengtsson/future">GitHub</a>, <a href="https://future.futureverse.org">pkgdown</a></li>
<li><strong>progressr</strong> package: <a href="https://cran.r-project.org/package=progressr">CRAN</a>, <a href="https://github.com/HenrikBengtsson/progressr">GitHub</a>, <a href="https://progressr.futureverse.org">pkgdown</a></li>
</ul>


</section>

 ]]></description>
  <category>R</category>
  <category>package</category>
  <category>future</category>
  <category>parallel</category>
  <category>parallelly</category>
  <category>HPC</category>
  <category>parallel processing</category>
  <guid>https://www.jottr.org/posts/2021-06-10-parallelly_1.26.0/</guid>
  <pubDate>Thu, 10 Jun 2021 22:00:00 GMT</pubDate>
</item>
<item>
  <title>parallelly 1.25.0: availableCores(omit=n) and, Finally, Built-in SSH Support for MS Windows 10 Users</title>
  <dc:creator>Henrik Bengtsson</dc:creator>
  <link>https://www.jottr.org/posts/2021-04-30-parallelly_1.25.0/</link>
  <description><![CDATA[ 






<center>
<img src="https://www.jottr.org/posts/2021-04-30-parallelly_1.25.0/resources/nasa-climate-ice-core-small.jpg" alt="A 25-cm long ice core is held in front of the camera on a sunny day. The background is an endless snow-covered flat landscape and a bright blue sky." style="width: 65%;"><br> <small><em>A piece of an ice core - more pleasing to look at than yet another illustration of a CPU core<br> <small>(Image credit: Ludovic Brucker, NASA’s Goddard Space Flight Center)</small> </em></small>
</center>
<p><strong><a href="https://cran.r-project.org/package=parallelly">parallelly</a></strong> 1.25.0 is on CRAN. It comes with two major improvements:</p>
<ul>
<li><p>You can now use <code>availableCores(omit = n)</code> to ask for all but <code>n</code> CPU cores</p></li>
<li><p><code>makeClusterPSOCK()</code> can finally use the built-in SSH client on MS Windows 10 to set up remote workers</p></li>
</ul>
<section id="availablecoresomit-n-is-your-new-friend" class="level1">
<h1>availableCores(omit = n) is your new friend</h1>
<p>When running R code in parallel, many choose to parallelize on as many CPU cores as possible, e.g.</p>
<div class="sourceCode" id="cb1" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb1-1">ncores <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> parallel<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">detectCores</span>()</span></code></pre></div>
<p>It’s also common to leave out a few cores so that we can still use the computer for other basic tasks, e.g.&nbsp;checking email, editing files, and browsing the web. This is often done by something like:</p>
<div class="sourceCode" id="cb2" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb2-1">ncores <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> parallel<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">detectCores</span>() <span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span> <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span></span></code></pre></div>
<p>which will return seven on a machine with eight CPU cores. If you look around, you also find that some leave two cores aside for other tasks;</p>
<div class="sourceCode" id="cb3" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb3-1">ncores <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> parallel<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">detectCores</span>() <span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span> <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">2</span></span></code></pre></div>
<p>I’m sorry to be the party killer, but <em>none of the above is guaranteed to work everywhere</em>. It might work on your computer but not on your collaborator’s computer, or in the cloud, or on continuous integration (CI) services, etc. There are two problems with the above approaches. The help page of <code>parallel::detectCores()</code> describes the first problem:</p>
<blockquote class="blockquote">
<p><strong>Value</strong><br>
An integer, <code>NA</code> if the answer is unknown.</p>
</blockquote>
<p>Yup, <code>detectCores()</code> might return <code>NA</code>. Ouf!</p>
<p>The second problem is that your code might run on a machine that has only one or two CPU cores. That means that <code>parallel::detectCores() - 1</code> may return zero, and <code>parallel::detectCores() - 2</code> may even return a negative one. You might think such machines no longer exists, but they do. The most common cases these days are virtual machines (VMs) running in the cloud. Note, if you’re a package developer, GitHub Actions, Travis CI, and AppVeyor CI are all running in VMs with two cores.</p>
<p>So, to make sure your code will run everywhere, you need to do something like:</p>
<div class="sourceCode" id="cb4" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb4-1">ncores <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">max</span>(parallel<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">detectCores</span>() <span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span> <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span>, <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span>, <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">na.rm =</span> <span class="cn" style="color: #8f5902;
background-color: null;
font-style: inherit;">TRUE</span>)</span></code></pre></div>
<p>With that approach, we know that <code>ncores</code> is at least one and never a missing value. I don’t know about you, but I often do thinkos where I mix up <code>min()</code> and <code>max()</code>, which I’m sure we don’t want. So, let me introduce you to your new friend:</p>
<div class="sourceCode" id="cb5" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb5-1">ncores <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> parallelly<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">availableCores</span>(<span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">omit =</span> <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span>)</span></code></pre></div>
<p>Just use that and you’ll be fine everywhere - it’ll always give you a value of one or greater. It’s neater and less error prone. Also, in contrast to <code>parallel::detectCores()</code>, <code>parallelly::availableCores()</code> respects various CPU settings and configurations that the system wants your to follow.</p>
</section>
<section id="makeclusterpsock-to-remote-machines-works-out-of-the-box-also-ms-windows-10" class="level1">
<h1>makeClusterPSOCK() to remote machines works out-of-the-box also MS Windows 10</h1>
<p>If you’re into parallelizing across multiple machines, either on your local network, or remotely, say in the cloud, you can use:</p>
<div class="sourceCode" id="cb6" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb6-1">workers <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> parallelly<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">makeClusterPSOCK</span>(<span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">c</span>(<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"n1.example.org"</span>, <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"n2.example.org"</span>))</span></code></pre></div>
<p>to spawn two R workers running in the background on those two machines. We can use these workers with different R parallel backends, e.g.&nbsp;with bare-bone <strong>parallel</strong></p>
<div class="sourceCode" id="cb7" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb7-1">y <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> parallel<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">parLapply</span>(workers, X, slow_fcn)</span></code></pre></div>
<p>with <strong>foreach</strong> and the classical <strong>doParallel</strong> adapter,</p>
<div class="sourceCode" id="cb8" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb8-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">library</span>(foreach)</span>
<span id="cb8-2">doParallel<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">registerDoParallel</span>(workers)</span>
<span id="cb8-3">y <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">foreach</span>(<span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">x =</span> X) <span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">%dopar%</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">slow_fcn</span>(x)</span></code></pre></div>
<p>and, obviously, my favorite, the <strong>future</strong> framework, which comes with lots of alternatives, e.g.</p>
<div class="sourceCode" id="cb9" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb9-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">library</span>(future)</span>
<span id="cb9-2"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">plan</span>(cluster, <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">workers =</span> workers)</span>
<span id="cb9-3"></span>
<span id="cb9-4">y <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> future.apply<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">future_lapply</span>(X, slow_fcn)</span>
<span id="cb9-5"></span>
<span id="cb9-6">y <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> furrr<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">future_map</span>(X, slow_fcn)</span>
<span id="cb9-7"></span>
<span id="cb9-8"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">library</span>(foreach)</span>
<span id="cb9-9">doFuture<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">registerDoFuture</span>()</span>
<span id="cb9-10">y <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">foreach</span>(<span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">x =</span> X) <span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">%dopar%</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">slow_fcn</span>(x)</span>
<span id="cb9-11"></span>
<span id="cb9-12">y <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> BiocParallel<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">::</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">bplapply</span>(X, slow_fcn)</span></code></pre></div>
<p>Now, in order to set up remote workers out of the box as shown above, you need to make sure you can do the following from the terminal:</p>
<div class="sourceCode" id="cb10" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb10-1">{local}<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">$</span> ssh n1.example.org Rscript <span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">--</span>version</span>
<span id="cb10-2">R scripting front<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span>end version <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">4</span>.<span class="fl" style="color: #AD0000;
background-color: null;
font-style: inherit;">0.4</span> (<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">2021-02-15</span>)</span></code></pre></div>
<p>If you can get to that point, you can also use those two remote machines to parallel from your local computer, which, at least I think, is pretty cool. To get to that point, you basically need to configure SSH locally and remotely so that you can log in without having to enter a password, which you do by using SSH keys. It does <em>not</em> require admin rights, and it’s not that hard to do when you know how to do it. Search the web for “SSH key authentication” for instructions, but the gist is that you create a public-private key pair locally and you copy the public one to the remote machine. The setup is the same for Linux, macOS, and MS Windows 10.</p>
<p>What’s new in <strong>parallelly</strong> 1.25.0 is that <em>MS Windows 10 users no longer have to install the PuTTY SSH client</em> - the Unix-compatible <code>ssh</code> client that comes with all MS Windows 10 installations works out of the box.</p>
<p>The reason why we couldn’t use the built-in Windows 10 client before is that it has an <a href="https://github.com/PowerShell/Win32-OpenSSH/issues/1265">bug preventing us from using it for reverse tunneling</a>, which is needed for remote, parallel processing. However, someone found a workaround, so that bug is no longer a blocker. Thus, now <code>makeClusterPSOCK()</code> works as we always wanted it to.</p>
<section id="take-homes" class="level2">
<h2 class="anchored" data-anchor-id="take-homes">Take-homes</h2>
<ul>
<li><p>Use <code>parallelly::availableCores()</code></p></li>
<li><p>Remote parallelization from MS Windows 10 is now as easy as from Linux and macOS</p></li>
</ul>
<p>For all updates, including what bugs have been fixed, see the <a href="https://parallelly.futureverse.org/news/index.html">NEWS</a> of <strong>parallelly</strong>.</p>
<p>Over and out!</p>
</section>
<section id="links" class="level2">
<h2 class="anchored" data-anchor-id="links">Links</h2>
<ul>
<li><p><strong>parallelly</strong> package: <a href="https://cran.r-project.org/package=parallelly">CRAN</a>, <a href="https://github.com/HenrikBengtsson/parallelly">GitHub</a>, <a href="https://parallelly.futureverse.org">pkgdown</a></p></li>
<li><p><strong>future</strong> package: <a href="https://cran.r-project.org/package=future">CRAN</a>, <a href="https://github.com/HenrikBengtsson/future">GitHub</a>, <a href="https://future.futureverse.org">pkgdown</a></p></li>
<li><p><strong>future.apply</strong> package: <a href="https://cran.r-project.org/package=future.apply">CRAN</a>, <a href="https://github.com/HenrikBengtsson/future.apply">GitHub</a>, <a href="https://future.apply.futureverse.org">pkgdown</a></p></li>
<li><p><strong>furrr</strong> package: <a href="https://cran.r-project.org/package=furrr">CRAN</a>, <a href="https://github.com/HenrikBengtsson/furrr">GitHub</a>, <a href="https://furrr.futureverse.org">pkgdown</a></p></li>
</ul>
<p>PS. If you’re interested in learning more about ice cores and how they are used to track changes in our atmosphere and climate, see <a href="https://climate.nasa.gov/news/2616/core-questions-an-introduction-to-ice-cores/">Core questions: An introduction to ice cores</a> by Jessica Stoller-Conrad, NASA’s Jet Propulsion Laboratory.</p>


</section>
</section>

 ]]></description>
  <category>R</category>
  <category>package</category>
  <category>future</category>
  <category>parallel</category>
  <category>parallelly</category>
  <category>HPC</category>
  <category>parallel processing</category>
  <category>remote-parallel-processing</category>
  <category>SSH</category>
  <category>MS Windows</category>
  <guid>https://www.jottr.org/posts/2021-04-30-parallelly_1.25.0/</guid>
  <pubDate>Fri, 30 Apr 2021 22:00:00 GMT</pubDate>
</item>
<item>
  <title>Using Kubernetes and the Future Package to Easily Parallelize R in the Cloud</title>
  <dc:creator>Chris Paciorek</dc:creator>
  <link>https://www.jottr.org/posts/2021-04-08-future-kubernetes/</link>
  <description><![CDATA[ 






<p>In this post, I’ll demonstrate that you can easily use the <strong><a href="https://cran.r-project.org/package=future">future</a></strong> package in R on a cluster of machines running in the cloud, specifically on a Kubernetes cluster.</p>
<p>This allows you to easily doing parallel computing in R in the cloud. One advantage of doing this in the cloud is the ability to easily scale the number and type of (virtual) machines across which you run your parallel computation.</p>
<section id="why-use-kubernetes-to-start-a-cluster-in-the-cloud" class="level2">
<h2 class="anchored" data-anchor-id="why-use-kubernetes-to-start-a-cluster-in-the-cloud">Why use Kubernetes to start a cluster in the cloud?</h2>
<p>Kubernetes is a platform for managing containers. You can think of the containers as lightweight Linux machines on which you can do your computation. By using the Kubernetes service of a cloud provider such as Google Cloud Platform (GCP) or Amazon Web Services (AWS), you can easily start up a cluster of (virtual) machines.</p>
<p>There have been (and are) approaches to starting up a cluster of machines on AWS easily from the command line on your laptop. Some tools that are no longer actively maintained are <a href="http://star.mit.edu/cluster">StarCluster</a> and <a href="https://cfncluster.readthedocs.io/en/latest">CfnCluster</a>. And there is now something called <a href="https://docs.aws.amazon.com/parallelcluster/latest/ug/getting_started.html">AWS ParallelCluster</a>. But doing it via Kubernetes allows you to build upon an industry standard platform that can be used on various cloud providers. A similar effort (which I heavily borrowed from in developing the setup described here) allows one to run a <a href="https://docs.dask.org/en/latest/setup/kubernetes-helm.html">Python Dask cluster</a> accessed via a Jupyter notebook.</p>
<p>Many of the cloud providers have Kubernetes services (and it’s also possible you’d have access to a Kubernetes service running at your institution or company). In particular, I’ve experimented with <a href="https://cloud.google.com/kubernetes-engine">Google Kubernetes Engine (GKE)</a> and <a href="https://aws.amazon.com/eks">Amazon’s Elastic Kubernetes Service (EKS)</a>. This post will demonstrate setting up your cluster using Google’s GKE, but see my GitHub <a href="https://github.com/paciorek/future-kubernetes">future-kubernetes</a> repository for details on doing it on Amazon’s EKS. Note that while I’ve gotten things to work on EKS, there have been <a href="https://github.com/paciorek/future-kubernetes#AWS-troubleshooting">various headaches</a> that I haven’t encountered on GKE.</p>
<p>I’m not a Kubernetes expert, nor a GCP or AWS expert (that might explain the headaches I just mentioned), but one upside is that hopefully I’ll go through all the details at a level someone who is not an expert can follow along. In fact, part of my goal in setting this up has been to learn more about Kubernetes, which I’ve done, but note that there’s <em>a lot</em> to it.</p>
<p>More details about the setup, including how it was developed and troubleshooting tips can be found in my <a href="https://github.com/paciorek/future-kubernetes">future-kubernetes</a> repository.</p>
</section>
<section id="how-it-works-briefly" class="level2">
<h2 class="anchored" data-anchor-id="how-it-works-briefly">How it works (briefly)</h2>
<p>This diagram in Figure 1 outlines the pieces of the setup.</p>
<figure class="figure">
<img src="https://www.jottr.org/posts/2021-04-08-future-kubernetes/resources/k8s.png" alt="Overview of using future on a Kubernetes cluster" width="700" class="figure-img">
<figcaption style="font-style: italic;">
Figure 1. Overview of using future on a Kubernetes cluster
</figcaption>
</figure>
<p>Work on a Kubernetes cluster is divided amongst <em>pods</em>, which carry out the components of your work and can communicate with each other. A pod is basically a Linux container. (Strictly speaking a pod can contain multiple containers and shared resources for those containers, but for our purposes, it’s simplest just to think of a pod as being a Linux container.) The pods run on the nodes in the Kubernetes cluster, where each Kubernetes node runs on a compute instance of the cloud provider. These instances are themselves virtual machines running on the cloud provider’s actual hardware. (I.e., somewhere out there, behind all the layers of abstraction, there are actual real computers running on endless aisles of computer racks in some windowless warehouse!) One of the nice things about Kubernetes is that if a pod dies, Kubernetes will automatically restart it.</p>
<p>The basic steps are:</p>
<ol type="1">
<li>Start your Kubernetes cluster on the cloud provider’s Kubernetes service</li>
<li>Start the pods using Helm, the Kubernetes package manager</li>
<li>Connect to the RStudio Server session running on the cluster from your browser</li>
<li>Run your future-based computation</li>
<li>Terminate the Kubernetes cluster</li>
</ol>
<p>We use the Kubernetes package manager, Helm, to run the pods of interest:</p>
<ul>
<li>one (scheduler) pod for a main process that runs RStudio Server and communicates with the workers</li>
<li>multiple (worker) pods, each with one R worker process to act as the workers managed by the <strong>future</strong> package</li>
</ul>
<p>Helm manages the pods and related <em>services</em>. An example of a service is to open a port on the scheduler pod so the R worker processes can connect to that port, allowing the scheduler pod RStudio Server process to communicate with the worker R processes. I have a <a href="https://github.com/paciorek/future-helm-chart">Helm chart</a> that does this; it borrows heavily from the <a href="https://github.com/dask/helm-chart">Dask Helm chart</a> for the Dask package for Python.</p>
<p>Each pod runs a Docker container. I use my own <a href="https://github.com/paciorek/future-kubernetes-docker">Docker container</a> that layers a bit on top of the <a href="https://rocker-project.org">Rocker</a> container that contains R and RStudio Server.</p>
</section>
<section id="step-1-start-the-kubernetes-cluster" class="level2">
<h2 class="anchored" data-anchor-id="step-1-start-the-kubernetes-cluster">Step 1: Start the Kubernetes cluster</h2>
<p>Here I assume you have already installed:</p>
<ul>
<li>the command line interface to Google Cloud,</li>
<li>the <code>kubectl</code> interface for interacting with Kubernetes, and</li>
<li><code>helm</code> for installing Helm charts (i.e., Kubernetes packages).</li>
</ul>
<p>Installation details can be found in the <a href="https://github.com/paciorek/future-kubernetes">future-kubernetes</a> repository.</p>
<p>First we’ll start our cluster (the first part of Step 1 in Figure 1):</p>
<div class="sourceCode" id="cb1" style="background: #f1f3f5;"><pre class="sourceCode sh code-with-copy"><code class="sourceCode bash"><span id="cb1-1"><span class="ex" style="color: null;
background-color: null;
font-style: inherit;">gcloud</span> container clusters create <span class="dt" style="color: #AD0000;
background-color: null;
font-style: inherit;">\</span></span>
<span id="cb1-2">    <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">--machine-type</span> n1-standard-1 <span class="dt" style="color: #AD0000;
background-color: null;
font-style: inherit;">\</span></span>
<span id="cb1-3">    <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">--num-nodes</span> 4 <span class="dt" style="color: #AD0000;
background-color: null;
font-style: inherit;">\</span></span>
<span id="cb1-4">    <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">--zone</span> us-west1-a <span class="dt" style="color: #AD0000;
background-color: null;
font-style: inherit;">\</span></span>
<span id="cb1-5">    <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">--cluster-version</span> latest <span class="dt" style="color: #AD0000;
background-color: null;
font-style: inherit;">\</span></span>
<span id="cb1-6">    my-cluster</span></code></pre></div>
<p>I’ve asked for four virtual machines (nodes), using the basic (and cheap) <code>n1-standard-1</code> instance type (which has a single CPU per virtual machine) from Google Cloud Platform.</p>
<p>You’ll want to specify the total number of cores on the virtual machines to be equal to the number of R workers that you want to start and that you specify in the Helm chart (as discussed below). Here we ask for four one-cpu nodes, and our Helm chart starts four workers, so all is well. See the Modifications section below on how to start up a different number of workers.</p>
<p>Since the RStudio Server process that you interact with wouldn’t generally be doing heavy computation at the same time as the workers, it’s OK that the RStudio scheduler pod and a worker pod would end up using the same virtual machine.</p>
</section>
<section id="step-2-install-the-helm-chart-to-set-up-your-pods" class="level2">
<h2 class="anchored" data-anchor-id="step-2-install-the-helm-chart-to-set-up-your-pods">Step 2: Install the Helm chart to set up your pods</h2>
<p>Next we need to get our pods going by installing the Helm chart (i.e., package) on the cluster; the installed chart is called a <em>release</em>. As discussed above, the Helm chart tells Kubernetes what pods to start and how they are configured.</p>
<p>First we need to give our account permissions to perform administrative actions:</p>
<div class="sourceCode" id="cb2" style="background: #f1f3f5;"><pre class="sourceCode sh code-with-copy"><code class="sourceCode bash"><span id="cb2-1"><span class="ex" style="color: null;
background-color: null;
font-style: inherit;">kubectl</span> create clusterrolebinding cluster-admin-binding <span class="dt" style="color: #AD0000;
background-color: null;
font-style: inherit;">\</span></span>
<span id="cb2-2">    <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">--clusterrole</span><span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span>cluster-admin</span></code></pre></div>
<p>Now let’s install the release. This code assumes the use of Helm version 3 or greater (for older versions <a href="https://github.com/paciorek/future-kubernetes">see my full instructions</a>).</p>
<div class="sourceCode" id="cb3" style="background: #f1f3f5;"><pre class="sourceCode sh code-with-copy"><code class="sourceCode bash"><span id="cb3-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">git</span> clone https://github.com/paciorek/future-helm-chart   <span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># download the materials</span></span>
<span id="cb3-2"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">tar</span> <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">-czf</span> future-helm.tgz <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">-C</span> future-helm-chart .           <span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># create a zipped archive (tarball) that `helm install` needs</span></span>
<span id="cb3-3"><span class="ex" style="color: null;
background-color: null;
font-style: inherit;">helm</span> install <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">--wait</span> test ./future-helm.tgz                <span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># install (start the pods)</span></span></code></pre></div>
<p>You’ll need to name your release; I’ve used ‘test’ above.</p>
<p>The <code>--wait</code> flag tells helm to wait until all the pods have started. Once that happens, you’ll see a message about the release and how to connect to the RStudio interface, which we’ll discuss further in the next section.</p>
<p>We can check the pods are running:</p>
<div class="sourceCode" id="cb4" style="background: #f1f3f5;"><pre class="sourceCode sh code-with-copy"><code class="sourceCode bash"><span id="cb4-1"><span class="ex" style="color: null;
background-color: null;
font-style: inherit;">kubectl</span> get pods</span></code></pre></div>
<p>You should see something like this (the alphanumeric characters at the ends of the names will differ in your case):</p>
<pre><code>NAME                                READY   STATUS    RESTARTS   AGE
future-scheduler-6476fd9c44-mvmz6   1/1     Running   0          116s
future-worker-54db85cb7b-47qsd      1/1     Running   0          115s
future-worker-54db85cb7b-4xf4x      1/1     Running   0          115s
future-worker-54db85cb7b-rj6bj      1/1     Running   0          116s
future-worker-54db85cb7b-wvp4n      1/1     Running   0          115s</code></pre>
<p>As expected, we have one scheduler and four workers.</p>
</section>
<section id="step-3-connect-to-rstudio-server-running-in-the-cluster" class="level2">
<h2 class="anchored" data-anchor-id="step-3-connect-to-rstudio-server-running-in-the-cluster">Step 3: Connect to RStudio Server running in the cluster</h2>
<p>Next we’ll connect to the RStudio instance running via RStudio Server on our main (scheduler) pod, using the browser on our laptop (Step 3 in Figure 1).</p>
<p>After installing the Helm chart, you should have seen a printout with some instructions on how to do this. First you need to connect a port on your laptop to the RStudio port on the main pod (running of course in the cloud):</p>
<div class="sourceCode" id="cb6" style="background: #f1f3f5;"><pre class="sourceCode sh code-with-copy"><code class="sourceCode bash"><span id="cb6-1"><span class="bu" style="color: null;
background-color: null;
font-style: inherit;">export</span> <span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">RSTUDIO_SERVER_IP</span><span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span><span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"127.0.0.1"</span></span>
<span id="cb6-2"><span class="bu" style="color: null;
background-color: null;
font-style: inherit;">export</span> <span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">RSTUDIO_SERVER_PORT</span><span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span>8787</span>
<span id="cb6-3"><span class="ex" style="color: null;
background-color: null;
font-style: inherit;">kubectl</span> port-forward <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">--namespace</span> default svc/future-scheduler <span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">$RSTUDIO_SERVER_PORT</span>:8787 <span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">&amp;</span></span></code></pre></div>
<p>You can now connect from your browser to the RStudio Server instance by going to the URL: <a href="http://127.0.0.1:8787" class="uri">http://127.0.0.1:8787</a>.</p>
<p>Enter <code>rstudio</code> as the username and <code>future</code> as the password to login to RStudio.</p>
<p>What’s happening is that port 8787 on your laptop is forwarding to the port on the main pod on which RStudio Server is listening (which is also port 8787). So you can just act as if RStudio Server is accessible directly on your laptop.</p>
<p>One nice thing about this is that there is no public IP address for someone to maliciously use to connect to your cluster. Instead the access is handled securely entirely through <code>kubectl</code> running on your laptop. However, it also means that you couldn’t easily share your cluster with a collaborator. For details on configuring things so there is a public IP, please see <a href="https://github.com/paciorek/future-kubernetes#connecting-to-the-rstudio-instance-when-starting-the-cluster-from-a-remote-machine">my repository</a>.</p>
<p>Note that there is nothing magical about running your computation via RStudio. You could connect to the main pod and simply run R in it and then use the <strong>future</strong> package.</p>
</section>
<section id="step-4-run-your-future-based-parallel-r-code" class="level2">
<h2 class="anchored" data-anchor-id="step-4-run-your-future-based-parallel-r-code">Step 4: Run your future-based parallel R code</h2>
<p>Now we’ll start up our future cluster and run our computation (Step 4 in Figure 1):</p>
<div class="sourceCode" id="cb7" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb7-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">library</span>(future)</span>
<span id="cb7-2"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">plan</span>(cluster, <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">manual =</span> <span class="cn" style="color: #8f5902;
background-color: null;
font-style: inherit;">TRUE</span>, <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">quiet =</span> <span class="cn" style="color: #8f5902;
background-color: null;
font-style: inherit;">TRUE</span>)</span></code></pre></div>
<p>The key thing is that we set <code>manual = TRUE</code> above. This ensures that the functions from the <strong>future</strong> package don’t try to start R processes on the workers, as those R processes have already been started by Kubernetes and are waiting to connect to the main (RStudio Server) process.</p>
<p>Note that we don’t need to say how many future workers we want. This is because the Helm chart sets an environment variable in the scheduler pod’s <code>Renviron</code> file based on the number of worker pod replicas. Since that variable is used by the <strong>future</strong> package (via <code>parallelly::availableCores()</code>) as the default number of future workers, this ensures that there are only as many future workers as you have worker pods. However, if you modify the number of worker pods after installing the Helm chart, you may need to set the <code>workers</code> argument to <code>plan()</code> manually. (And note that if you were to specify more future workers than R worker processes (i.e., pods) you would get an error and if you were to specify fewer, you wouldn’t be using all the resources that you are paying for.)</p>
<p>Now we can use the various tools in the <strong>future</strong> package as we would if on our own machine or working on a Linux cluster.</p>
<p>Let’s run our parallelized operations. I’m going to do the world’s least interesting calculation of calculating the mean of many (10 million) random numbers forty separate times in parallel. Not interesting, but presumably if you’re reading this you have your own interesting computation in mind and hopefully know how to do it using future’s tools such as <strong><a href="https://cran.r-project.org/package=future.apply">future.apply</a></strong> and <strong><a href="https://cran.r-project.org/package=foreach">foreach</a></strong> with <strong><a href="https://cran.r-project.org/package=doFuture">doFuture</a></strong>.</p>
<div class="sourceCode" id="cb8" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb8-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">library</span>(future.apply)</span>
<span id="cb8-2">output <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">&lt;-</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">future_sapply</span>(<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">:</span><span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">40</span>, <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">function</span>(i) <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">mean</span>(<span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">rnorm</span>(<span class="fl" style="color: #AD0000;
background-color: null;
font-style: inherit;">1e7</span>)), <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">future.seed =</span> <span class="cn" style="color: #8f5902;
background-color: null;
font-style: inherit;">TRUE</span>)</span></code></pre></div>
<p>Note that all of this assumes you’re working interactively, but you can always reconnect to the RStudio Server instance after closing the browser, and any long-running code should continue running even if you close the browser.</p>
<p>Figure 2 shows a screenshot of the RStudio interface.</p>
<figure class="figure">
<img src="https://www.jottr.org/posts/2021-04-08-future-kubernetes/resources/rstudio.png" alt="RStudio interface, demonstrating use of future commands" width="700" class="figure-img">
<figcaption style="font-style: italic;">
Figure 2. Screenshot of the RStudio interface
</figcaption>
</figure>
<section id="working-with-files" class="level3">
<h3 class="anchored" data-anchor-id="working-with-files">Working with files</h3>
<p>Note that <code>/home/rstudio</code> will be your default working directory in RStudio and the RStudio Server process will be running as the user <code>rstudio</code>.</p>
<p>You can use <code>/tmp</code> and <code>/home/rstudio</code> for files, both within RStudio and within code running on the workers, but note that files (even in <code>/home/rstudio</code>) are not shared between workers nor between the workers and the RStudio Server pod.</p>
<p>To make data available to your RStudio process or get output data back to your laptop, you can use <code>kubectl cp</code> to copy files between your laptop and the RStudio Server pod. Here’s an example of copying to/from <code>/home/rstudio</code>:</p>
<div class="sourceCode" id="cb9" style="background: #f1f3f5;"><pre class="sourceCode sh code-with-copy"><code class="sourceCode bash"><span id="cb9-1"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">## create a variable with the name of the scheduler pod</span></span>
<span id="cb9-2"><span class="bu" style="color: null;
background-color: null;
font-style: inherit;">export</span> <span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">SCHEDULER</span><span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span><span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">$(</span><span class="ex" style="color: null;
background-color: null;
font-style: inherit;">kubectl</span> get pod <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">--namespace</span> default <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">-o</span> jsonpath=<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">'{.items[?(@.metadata.labels.component=="scheduler")].metadata.name}'</span><span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">)</span></span>
<span id="cb9-3"></span>
<span id="cb9-4"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">## copy a file to the scheduler pod</span></span>
<span id="cb9-5"><span class="ex" style="color: null;
background-color: null;
font-style: inherit;">kubectl</span> cp my_laptop_file <span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">${SCHEDULER}</span>:home/rstudio/</span>
<span id="cb9-6"></span>
<span id="cb9-7"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">## copy a file from the scheduler pod</span></span>
<span id="cb9-8"><span class="ex" style="color: null;
background-color: null;
font-style: inherit;">kubectl</span> cp <span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">${SCHEDULER}</span>:home/rstudio/my_output_file .</span></code></pre></div>
<p>Of course you can also interact with the web from your RStudio process, so you could download data to the RStudio process from the internet.</p>
</section>
</section>
<section id="step-5-cleaning-up" class="level2">
<h2 class="anchored" data-anchor-id="step-5-cleaning-up">Step 5: Cleaning up</h2>
<p>Make sure to shut down your Kubernetes cluster, so you don’t keep getting charged.</p>
<div class="sourceCode" id="cb10" style="background: #f1f3f5;"><pre class="sourceCode sh code-with-copy"><code class="sourceCode bash"><span id="cb10-1"><span class="ex" style="color: null;
background-color: null;
font-style: inherit;">gcloud</span> container clusters delete my-cluster <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">--zone</span><span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span>us-west1-a</span></code></pre></div>
</section>
<section id="modifications" class="level2">
<h2 class="anchored" data-anchor-id="modifications">Modifications</h2>
<p>You can modify the Helm chart in advance, before installing it. For example you might want to install other R packages for use in your parallel code or change the number of workers.</p>
<p>To add additional R packages, go into the <code>future-helm-chart</code> directory (which you created using the directions above in Step 2) and edit the <a href="https://github.com/paciorek/future-helm-chart/blob/master/values.yaml">values.yaml</a> file. Simply modify the lines that look like this:</p>
<div class="sourceCode" id="cb11" style="background: #f1f3f5;"><pre class="sourceCode yaml code-with-copy"><code class="sourceCode yaml"><span id="cb11-1"><span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">  </span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">env</span><span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">:</span></span>
<span id="cb11-2"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">  #  - name: EXTRA_R_PACKAGES</span></span>
<span id="cb11-3"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">  #    value: data.table</span></span></code></pre></div>
<p>by removing the “#” comment characters and putting the R packages you want installed in place of <code>data.table</code>, with the names of the packages separated by spaces, e.g.,</p>
<div class="sourceCode" id="cb12" style="background: #f1f3f5;"><pre class="sourceCode yaml code-with-copy"><code class="sourceCode yaml"><span id="cb12-1"><span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">  </span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">env</span><span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">:</span></span>
<span id="cb12-2"><span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">    </span><span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">-</span><span class="at" style="color: #657422;
background-color: null;
font-style: inherit;"> </span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">name</span><span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">:</span><span class="at" style="color: #657422;
background-color: null;
font-style: inherit;"> EXTRA_R_PACKAGES</span></span>
<span id="cb12-3"><span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">      </span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">value</span><span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">:</span><span class="at" style="color: #657422;
background-color: null;
font-style: inherit;"> foreach doFuture</span></span></code></pre></div>
<p>In many cases you may want these packages installed on both the scheduler pod (where RStudio Server runs) and on the workers. If so, make sure to modify the lines above in both the <code>scheduler</code> and <code>worker</code> stanzas.</p>
<p>To modify the number of workers, modify the <code>replicas</code> line in the <code>worker</code> stanza of the <a href="https://github.com/paciorek/future-helm-chart/blob/master/values.yaml">values.yaml</a> file.</p>
<p>Then rebuild the Helm chart:</p>
<div class="sourceCode" id="cb13" style="background: #f1f3f5;"><pre class="sourceCode sh code-with-copy"><code class="sourceCode bash"><span id="cb13-1"><span class="bu" style="color: null;
background-color: null;
font-style: inherit;">cd</span> future-helm-chart  <span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">## ensure you are in the directory containing `values.yaml`</span></span>
<span id="cb13-2"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">tar</span> <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">-czf</span> ../future-helm.tgz .</span></code></pre></div>
<p>and install as done previously.</p>
<p>Note that doing the above to increase the number of workers would probably only make sense if you also modify the number of virtual machines you start your Kubernetes cluster with such that the total number of cores across the cloud provider compute instances matches the number of worker replicas.</p>
<p>You may also be able to modify a running cluster. For example you could use <code>gcloud container clusters resize</code>. I haven’t experimented with this.</p>
<p>To modify if your Helm chart is already installed (i.e., your release is running), one simple option is to reinstall the Helm chart as discussed below. You may also need to kill the <code>port-forward</code> process discussed in Step 3.</p>
<p>For some changes, you can also also update a running release without uninstalling it by “patching” the running release or scaling resources. I won’t go into details here.</p>
</section>
<section id="troubleshooting" class="level2">
<h2 class="anchored" data-anchor-id="troubleshooting">Troubleshooting</h2>
<p>Things can definitely go wrong in getting all the pods to start up and communicate with each other. Here are some suggestions for monitoring what is going on and troubleshooting.</p>
<p>First, you can use <code>kubectl</code> to check the pods are running:</p>
<div class="sourceCode" id="cb14" style="background: #f1f3f5;"><pre class="sourceCode sh code-with-copy"><code class="sourceCode bash"><span id="cb14-1"><span class="ex" style="color: null;
background-color: null;
font-style: inherit;">kubectl</span> get pods</span></code></pre></div>
<section id="connect-to-a-pod" class="level3">
<h3 class="anchored" data-anchor-id="connect-to-a-pod">Connect to a pod</h3>
<p>To connect to a pod, which allows you to check on installed software, check on what the pod is doing, and other troubleshooting, you can do the following</p>
<div class="sourceCode" id="cb15" style="background: #f1f3f5;"><pre class="sourceCode sh code-with-copy"><code class="sourceCode bash"><span id="cb15-1"><span class="bu" style="color: null;
background-color: null;
font-style: inherit;">export</span> <span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">SCHEDULER</span><span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span><span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">$(</span><span class="ex" style="color: null;
background-color: null;
font-style: inherit;">kubectl</span> get pod <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">--namespace</span> default <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">-o</span> jsonpath=<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">'{.items[?(@.metadata.labels.component=="scheduler")].metadata.name}'</span><span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">)</span></span>
<span id="cb15-2"><span class="bu" style="color: null;
background-color: null;
font-style: inherit;">export</span> <span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">WORKERS</span><span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span><span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">$(</span><span class="ex" style="color: null;
background-color: null;
font-style: inherit;">kubectl</span> get pod <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">--namespace</span> default <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">-o</span> jsonpath=<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">'{.items[?(@.metadata.labels.component=="worker")].metadata.name}'</span><span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">)</span></span>
<span id="cb15-3"></span>
<span id="cb15-4"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">## access the scheduler pod:</span></span>
<span id="cb15-5"><span class="ex" style="color: null;
background-color: null;
font-style: inherit;">kubectl</span> exec <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">-it</span> <span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">${SCHEDULER}</span>  <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">--</span> /bin/bash</span>
<span id="cb15-6"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">## access a worker pod:</span></span>
<span id="cb15-7"><span class="bu" style="color: null;
background-color: null;
font-style: inherit;">echo</span> <span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">$WORKERS</span></span>
<span id="cb15-8"><span class="ex" style="color: null;
background-color: null;
font-style: inherit;">kubectl</span> exec <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">-it</span> <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">&lt;</span>insert_name_of_a_worker<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">&gt;</span> -- /bin/bash</span></code></pre></div>
<p>Alternatively just determine the name of the pod with <code>kubectl get pods</code> and then run the <code>kubectl exec -it ...</code> invocation above.</p>
<p>Note that once you are in a pod, you can install software in the usual fashion of a Linux machine (in this case using <code>apt</code> commands such as <code>apt-get install</code>).</p>
</section>
<section id="connect-to-a-virtual-machine" class="level3">
<h3 class="anchored" data-anchor-id="connect-to-a-virtual-machine">Connect to a virtual machine</h3>
<p>Or to connect directly to an underlying VM, you can first determine the name of the VM and then use the <code>gcloud</code> tools to connect to it.</p>
<div class="sourceCode" id="cb16" style="background: #f1f3f5;"><pre class="sourceCode sh code-with-copy"><code class="sourceCode bash"><span id="cb16-1"><span class="ex" style="color: null;
background-color: null;
font-style: inherit;">kubectl</span> get nodes</span>
<span id="cb16-2"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">## now, connect to one of the nodes, 'gke-my-cluster-default-pool-8b490768-2q9v' in this case:</span></span>
<span id="cb16-3"><span class="ex" style="color: null;
background-color: null;
font-style: inherit;">gcloud</span> compute ssh gke-my-cluster-default-pool-8b490768-2q9v <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">--zone</span> us-west1-a</span></code></pre></div>
</section>
<section id="check-your-running-code" class="level3">
<h3 class="anchored" data-anchor-id="check-your-running-code">Check your running code</h3>
<p>To check that your code is actually running in parallel, one can run the following test and see that the result returns the names of distinct worker pods.</p>
<div class="sourceCode" id="cb17" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb17-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">library</span>(future.apply)</span>
<span id="cb17-2"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">future_sapply</span>(<span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">seq_len</span>(<span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">nbrOfWorkers</span>()), <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">function</span>(i) <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">Sys.info</span>()[[<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"nodename"</span>]])</span></code></pre></div>
<p>You should see something like this:</p>
<pre><code>[1] future-worker-54db85cb7b-47qsd future-worker-54db85cb7b-4xf4x
[3] future-worker-54db85cb7b-rj6bj future-worker-54db85cb7b-wvp4n</code></pre>
<p>One can also connect to the pods or to the underlying virtual nodes (as discussed above) and run Unix commands such as <code>top</code> and <code>free</code> to understand CPU and memory usage.</p>
</section>
<section id="reinstall-the-helm-release" class="level3">
<h3 class="anchored" data-anchor-id="reinstall-the-helm-release">Reinstall the Helm release</h3>
<p>You can restart your release (i.e., restarting the pods, without restarting the whole Kubernetes cluster):</p>
<div class="sourceCode" id="cb19" style="background: #f1f3f5;"><pre class="sourceCode sh code-with-copy"><code class="sourceCode bash"><span id="cb19-1"><span class="ex" style="color: null;
background-color: null;
font-style: inherit;">helm</span> uninstall test</span>
<span id="cb19-2"><span class="ex" style="color: null;
background-color: null;
font-style: inherit;">helm</span> install <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">--wait</span> test ./future-helm.tgz </span></code></pre></div>
<p>Note that you may need to restart the entire Kubernetes cluster if you’re having difficulties that reinstalling the release doesn’t fix.</p>
</section>
</section>
<section id="how-does-it-work" class="level2">
<h2 class="anchored" data-anchor-id="how-does-it-work">How does it work?</h2>
<p>I’ve provided many of the details of how it works in my <a href="https://github.com/paciorek/future-kubernetes">future-kubernetes</a> repository.</p>
<p>The key pieces are:</p>
<ol type="1">
<li>The <a href="https://github.com/paciorek/future-helm-chart">Helm chart</a> with the instructions for how to start the pods and any associated services.</li>
<li>The <a href="https://github.com/paciorek/future-kubernetes-docker">Rocker-based Docker container(s)</a> that the pods run.</li>
</ol>
<p>That’s all there is to it … plus <a href="https://github.com/paciorek/future-kubernetes">these instructions</a>.</p>
<p>Briefly:</p>
<ol type="1">
<li>Based on the Helm chart, Kubernetes starts up the ‘main’ or ‘scheduler’ pod running RStudio Server and multiple worker pods each running an R process. All of the pods are running the Rocker-based Docker container</li>
<li>The RStudio Server main process and the workers use socket connections (via the R function <code>socketConnection()</code>) to communicate:
<ul>
<li>the worker processes start R processes that are instructed to regularly make a socket connection using a particular port on the main scheduler pod</li>
<li>when you run <code>future::plan()</code> (which calls <code>makeClusterPSOCK()</code>) in RStudio, the RStudio Server process attempts to make socket connections to the workers using that same port</li>
</ul></li>
<li>Once the socket connections are established, command of the RStudio session returns to you and you can run your future-based parallel R code.</li>
</ol>
<p>One thing I haven’t had time to work through is how to easily scale the number of workers after the Kubernetes cluster is running and the Helm chart installed, or even how to auto-scale – starting up workers as needed based on the number of workers requested via <code>plan()</code>.</p>
</section>
<section id="wrap-up" class="level2">
<h2 class="anchored" data-anchor-id="wrap-up">Wrap up</h2>
<p>If you’re interested in extending or improving this or collaborating in some fashion, please feel free to get in touch with me via the <a href="https://github.com/paciorek/future-kubernetes/issues">‘future-kubernetes’ issue tracker</a> or by email.</p>
<p>And if you’re interested in using R with Kubernetes, note that RStudio provides an integration of RStudio Server Pro with Kubernetes that should allow one to run future-based workflows in parallel.</p>
<p>/Chris</p>
</section>
<section id="links" class="level2">
<h2 class="anchored" data-anchor-id="links">Links</h2>
<ul>
<li>future-kubernetes repository:
<ul>
<li>GitHub page: https://github.com/paciorek/future-kubernetes</li>
</ul></li>
<li>future-kubernetes Helm chart:
<ul>
<li>GitHub page: https://github.com/paciorek/future-helm-chart</li>
</ul></li>
<li>future-kubernetes Docker container:
<ul>
<li>GitHub page: https://github.com/paciorek/future-kubernetes-docker</li>
</ul></li>
<li>future package:
<ul>
<li>CRAN page: https://cran.r-project.org/package=future</li>
<li>GitHub page: https://github.com/HenrikBengtsson/future</li>
</ul></li>
</ul>


</section>

 ]]></description>
  <category>R</category>
  <category>package</category>
  <category>future</category>
  <category>Kubernetes</category>
  <category>Helm</category>
  <category>Dask</category>
  <category>Linux containers</category>
  <category>Docker</category>
  <category>Rocker</category>
  <category>RStudio Server</category>
  <category>parallel processing</category>
  <category>Google Kubernetes Engine</category>
  <category>GKE</category>
  <category>cloud</category>
  <category>guest post</category>
  <guid>https://www.jottr.org/posts/2021-04-08-future-kubernetes/</guid>
  <pubDate>Fri, 09 Apr 2021 02:00:00 GMT</pubDate>
</item>
<item>
  <title>future.BatchJobs - End-of-Life Announcement</title>
  <dc:creator>Henrik Bengtsson</dc:creator>
  <link>https://www.jottr.org/posts/2021-01-08-future.BatchJobs-end-of-file/</link>
  <description><![CDATA[ 






<div style="width: 40%; margin: 2ex; float: right;">
<center>
<img src="https://www.jottr.org/posts/2021-01-08-future.BatchJobs-end-of-file/resources/sign_out_of_service_do_not_use.png" alt="Sign: Out of Service - Do not use!" style="width: 100%;">
</center>
</div>
<p>This is an announcement that <strong><a href="https://cran.r-project.org/package=future.BatchJobs">future.BatchJobs</a></strong> - <em>A Future API for Parallel and Distributed Processing using BatchJobs</em> has been archived on CRAN. The package has been deprecated for years with a recommendation of using <strong><a href="https://cran.r-project.org/package=future.batchtools">future.batchtools</a></strong> instead. The latter has been on CRAN since June 2017 and builds upon the <strong><a href="https://cran.r-project.org/package=batchtools">batchtools</a></strong> package, which itself supersedes the <strong><a href="https://cran.r-project.org/package=BatchJobs">BatchJobs</a></strong> package.</p>
<p>To wrap up the three-and-a-half year long life of <strong><a href="https://cran.r-project.org/package=future.BatchJobs">future.BatchJobs</a></strong>, the very last version, 0.17.0, reached CRAN on 2021-01-04 and passed on CRAN checks as of 2020-01-08, when the the package was requested to be formally archived. All versions ever existing on CRAN can be found at <a href="https://cran.r-project.org/src/contrib/Archive/future.BatchJobs/" class="uri">https://cran.r-project.org/src/contrib/Archive/future.BatchJobs/</a>.</p>
<p>Archiving the <strong>future.BatchJobs</strong> package will speed up new releases of the <strong>future</strong> package. In the past, some of the <strong>future</strong> releases required internal updates to reverse packages dependencies such as <strong>future.BatchJobs</strong> to be rolled out on CRAN first in order for <strong>future</strong> to pass the CRAN incoming checks.</p>
<section id="postscript" class="level2">
<h2 class="anchored" data-anchor-id="postscript">Postscript</h2>
<p>The <a href="https://cran.r-project.org/package=future.BatchJobs" class="uri">https://cran.r-project.org/package=future.BatchJobs</a> page mentions:</p>
<blockquote class="blockquote">
<p>Archived on 2021-01-08 at the request of the maintainer.</p>
<p>Consider using package ‘<a href="https://cran.r-project.org/package=future.batchtools">future.batchtools</a>’ instead.</p>
</blockquote>
<p>I’m happy to see that we can suggest another package on our archived package pages. All I did to get this was to mention it in my email to CRAN:</p>
<blockquote class="blockquote">
<p>Hi,</p>
<p>please archive the ‘future.BatchJobs’ package. It has zero reverse dependencies. The package has been labelled deprecated for a long time now and has been superseded by the ‘future.batchtools’ package.</p>
<p>Thank you,<br>
Henrik</p>
</blockquote>
</section>
<section id="links" class="level2">
<h2 class="anchored" data-anchor-id="links">Links</h2>
<ul>
<li>future package:
<ul>
<li>CRAN page: https://cran.r-project.org/package=future</li>
<li>GitHub page: https://github.com/HenrikBengtsson/future</li>
</ul></li>
<li>future.BatchJobs package:
<ul>
<li>CRAN page: https://cran.r-project.org/package=future.BatchJobs</li>
<li>All CRAN versions: https://cran.r-project.org/src/contrib/Archive/future.BatchJobs/</li>
<li>GitHub page: https://github.com/HenrikBengtsson/future.BatchJobs</li>
</ul></li>
<li>future.batchtools package:
<ul>
<li>CRAN page: https://cran.r-project.org/package=future.batchtools</li>
<li>GitHub page: https://github.com/HenrikBengtsson/future.batchtools</li>
</ul></li>
</ul>


</section>

 ]]></description>
  <category>R</category>
  <category>package</category>
  <category>future.BatchJobs</category>
  <category>future.batchtools</category>
  <category>future</category>
  <category>parallel processing</category>
  <category>HPC</category>
  <category>CRAN</category>
  <category>archive</category>
  <category>deprecation</category>
  <guid>https://www.jottr.org/posts/2021-01-08-future.BatchJobs-end-of-file/</guid>
  <pubDate>Fri, 08 Jan 2021 17:00:00 GMT</pubDate>
</item>
</channel>
</rss>
